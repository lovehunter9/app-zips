apiVersion: v1
kind: Service
metadata:
  name: audiominutesxdemo
  namespace: "{{ .Release.Namespace }}"
  labels:
    io.kompose.service: audiominutesxdemo
spec:
  type: ClusterIP
  selector:
    io.kompose.service: audiominutesxdemo
  ports:
    - name: web
      protocol: TCP
      port: 8080
      targetPort: 8080

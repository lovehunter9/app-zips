apiVersion: v2
appVersion: "1.0.16"
description: Audio Minutes X Demo — a Feishu-Minutes-style meeting/audio notes app. Upload audio/video, get a speaker-diarized, word-level-timestamped transcript with two-way player↔transcript sync (click a word to seek, playback highlights the current word). All models run through the LLM Gateway.
name: audiominutesxdemo
type: application
version: 1.0.16

# 音频批量（STT / 对齐）方案

> 定稿日期：2026-07-23。本文汇总"批量转写、批量对齐 + 超长音频处理"的架构决策，
> 供后续实施。**批量本身尚未落代码**；本文是实施依据。上传体积守卫（压缩）已落两个 demo（见 §7）。

---

## 1. 目标与动机

- 会议音频常见 **1–3 小时**（实测已到 3h22m）。当前"分段转写 + 分段对齐"对一条长音频要发**几十~几百次** HTTP。
- 真实故障**全在网络/连接层**（align `ECONNRESET`、连接饥饿、入口 504、大文件 413），GPU 从没崩。
- 所以核心杠杆是 **砍请求数**：合并为少数几个批量请求，减少往返、连接建立、以及"每多一次请求就多一次失败机会"的叠乘失败面。
- **质量绝不妥协**（尤其 STT）。

---

## 2. 铁律与原则

1. **一个接口严禁既转写又对齐**——批量按 op 各自独立（`transcriptions` 批量只转写，`align` 批量只对齐）。
2. **共享层保持通用透明**：`llm-init`、`llm-gateway` 只做透明转发/路由，**不为任何具体模型改代码**。
   模型自己的"魔改/尿性"（含批量）一律沉到**它自己的 chart 基座（wrapper）**——加/改一个模型只动那个基座。
3. **不做能力广播**：不在 init/gateway 里加 `*_batch` 标志。用批量的应用**自己知道所选基座的尿性，或自行嗅探**
   （带 `segments` 打过去，支持的回 `{results:[…]}`、不支持的老 wrapper 忽略它回 `{text}`／`{units}`——看返回形状即知）。
4. **一切音频切点按能量/静音（VAD），严禁掐秒平均**（增强除外，见 §8）。
5. **质量不妥协**：批量载荷用**无损**格式；绝不压到比源更低。

---

## 3. 四层职责边界（已核实）

| 层 | 干什么 | 批量相关改动 |
|---|---|---|
| **Chart wrapper**（Python/FastAPI，逐引擎，跑在 `WRAPPER_PORT`） | 实现单条算子 `POST /v1/audio/<op>`，拥有模型推理 | **唯一改动点**：识别 `segments` → 批量模式 |
| **llm-init**（每个模型 pod 内，`httputil.ReverseProxy`，按 `audioPathSupport` 表按路径门控） | 把 `/v1/audio/*` 原样转发给 wrapper（不解析 body）+ 控制面 | **零改动**（同路径、同 body 透传） |
| **llm-gateway**（中心，`audioPassthroughHandler` 逐 op） | 抠 `model` 定 provider、按 capability 门控、verbatim 转发；计量=每请求一条审计日志行（REST 音频 tokens/cost=0，**不记时长**） | **零功能改动**（批量记 1 条日志，无计费影响） |
| **Demo 应用** | 编排流水线、按基座尿性调用 | 组装批量请求、按字节分批 |

---

## 4. 超长音频 = 三层正交问题

分段（有多少活）与批量（每次传多少）是**两件事**；超长音频把它们掰开：

| 层 | 是什么 | 约束 |
|---|---|---|
| **① DIAR** | 全局一遍，**不可分段**（说话人身份全局） | 整段上传 ≤32M（压缩，§7）；超长另有墙钟/需 chunk+stitch（§10） |
| **② 分段** | 纯数据：diar + 能量切点 → 窗口 `[{start,end,text?}]` | 无网络，纯 CPU；N 可达数百 |
| **③ 批量** | 把 N 个窗打包成 M 个请求 | **单一阈值：累计上传字节 ≤ 31.8 MiB**（§6） |

---

## 5. 批量接口契约（同端点双模式，逻辑在 wrapper）

- **触发**：请求含可选字段 `segments`（JSON）→ 批量；无 `segments` → **老单条行为逐字节不变**。
  这保证 `/transcriptions` 对 OpenAI **完全向后兼容**（标准客户端永不发 `segments`，永远走单条、拿 `{text}`）。
- **整段音频只上传一遍** + `segments`；wrapper **解码一次 → 按段切（能量切点）→ 顺序逐段跑 → 逐项容错**。

### 转写 `POST /v1/audio/transcriptions`
```
单条(标准)：file=<音频> model=… [response_format=json] [language]      → {"text": "..."}
批量：      file=<整段音频, 传一遍> segments=[{start,end},…]           → {"results":[{"text":…}|{"error":…}]}
```

### 对齐 `POST /v1/audio/align`（本就是自定义端点，原生双模式）
```
单条：file=<切片> model=… text=<该片文本> [language]                    → {"units":[…], "language":…}
批量：file=<整段音频, 传一遍> segments=[{start,end,text,[language]},…]  → {"results":[{units,language}|{error}]}
```

- **逐项容错**：一段失败不废整批（与 translate 的全或无不同——音频每项很重，逐项容错才稳）。
- **无上限广播**；`≤64` 之类的内部保护由 wrapper 自行决定（可选）。

---

## 6. 批量载荷与分批（**唯一阈值：字节**）

- **载荷格式 = FLAC（无损）**。理由：
  - 无损 → **不妥协质量**（也绝不低于源）；
  - `libsndfile`/`soundfile`/`torchaudio` **通吃**（含只认 libsndfile 的 whisper/vLLM 引擎）；
  - 语音 16k 单声道 FLAC ≈ WAV 的一半（~16 KB/s），比 WAV 每批多装一倍。
- **分批规则（就这一条）**：逐片累加，**累计上传字节逼近 31.8 MiB 就封批、开下一批**。
  字节在拼 multipart 时**精确可算**（每片 `blob.size` 直接加），与码率/编码无关，直接对着 32M 那道墙。
- **不设墙钟/时长上限**：RTF 估不准、Olares(GPU) 比本地快、且集群内直连本就绕开入口超时；
  真遇到某批超时再拿**真实数据**处置。
- 参考量级：FLAC ~16KB/s → 一批 ~30min；3h22m ≈ **~7 批** STT + ~7 批 align（对比数百次单调，且零质量损失）。

> 为什么按字节而不是分钟：源码率会变（WAV 32KB/s、mp3@64k 8KB/s…），分钟是会漂的代理量；
> 32M 那道墙的真实单位是**字节**，而字节我们拼包时当场就知道。

---

## 7. 上传体积守卫（压缩）—— **已落两个 demo**

- 网关前端 nginx `client_max_body_size 32M`（`llm-gateway/deploy/frontend.nginx.conf.template`）。
  实测 31.3M 过、200M 挂。**阈值 = 31.8 MiB（33,344,716 字节，离硬顶 ~200KB）**。
- **能不压就不压**：文件 ≤31.8MiB 原样传；**>31.8MiB 才**转成"**按时长算码率**的 16k 单声道 mp3"（保证落到 ~30M 内）。
- **minutesx2**（服务端 `server.js`）：`gwAudioOp` / `gwAudioEnhance` 上传前经 `prepUpload` 守卫；用完删临时文件。
  → diar 整段（3h22m）自动压到 ~20kbps mp3 → 不再 413。
- **studiox2**（客户端 `api.ts` + 服务端 `/api/transcode`）：`edgeSafe` 阈值 8MB→31.8MiB，
  **已压缩但仍超阈值**的大文件（如 194MB mp3）也重新压；`toMp3_16kMono` 固定 64k → **按时长动态码率**。
- 注意：**diar 整段压缩用 mp3**（diar/pyannote 不敏感、实测吃 mp3）；**STT/align 切片保无损**（FLAC/WAV），互不影响。

---

## 8. 能量切点（铁律）+ 待彻查清单

- **规则**：任何音频切点按能量/静音（VAD 或本地 RMS）。**增强(enhance)除外**。
- 已定位主犯：`minutesx2/server.js buildWindows` 的 `SPLIT_MAX` **均匀切**
  （`n=ceil(dur/30); step=dur/n; 等分`，把 >30s 长发言硬切等份）→ 改为在段内找**低能量/静音点**下刀。
- **待彻查**：全流程所有 `step=dur/n`、`i*step`、等分音频逻辑。
  （translate / RAG 是**文本**按标点切，不属"能量"范畴，不动。）

---

## 9. Demo 模式（本次：新增"分段批量"，不动旧逻辑）

- **本次**：两个 demo **保留**现有"整段""分段"逻辑，**新增第三个模式"分段批量"**。
  - 三者**互斥单选**(选一个自动取消另两个)，**默认选"分段批量"**。
  - 选"整段"或"分段" → 走**原来的**整段/分段逻辑，一行不改。
  - 选"分段批量" → 走新路径：`diar → 分段 → FLAC 切片 → 按字节分批 → 调批量端点`。
- **翻译不再单独处理**：minutes 无整段/分段选项，之前"强制分段"已改为"强制批量(/translate*)"，本次不动。
- **推迟(后续)**：待"分段批量"稳定后，再评估是否删掉整段专用的
  `alignLong`/窗塌检测/重对齐还债/插值兜底/整段分句/逐窗报告 一整套。本次**不删**。
- 窗内**句子切分（纯本地、文本级）**用于字幕/显示——窗(处理粒度)≠句(显示粒度)，各模式共用。

---

## 10. 未决 / 风险

1. **DIAR 超长**：3h+ 的 diar 即使压过体积不 413，pyannote 单遍也可能很慢（自身墙钟）。
   出路：**集群内直连绕入口** / 最终 **chunk+stitch**（重叠分块 diar + 用 pyannote `speaker_embed` 跨块聚类合并）——独立难题，暂不做。
2. **mp3 引擎兼容**：整段压缩用 mp3，仅 diar 走它（实测 OK）；STT/align 切片一律 FLAC，规避 libsndfile 不认 mp3。
3. **计量粒度**：批量在网关记 1 条审计行（非 N 条）——无计费影响；真需精确再在网关加**通用** per-item 计量（唯一可能回头碰网关处）。

---

## 11. 实施顺序（建议）

1. **wrapper**：给 `transcriptions` / `align` 加 `segments` 批量分支（解码一次 → 能量切 → 顺序跑 → 逐项容错）。逐引擎，但可共享一段实现。
2. **demo**：分段产窗 → **FLAC 无损切片 → 按字节 ≤31.8MiB 贪心分批 → 调批量端点**；`buildWindows` 均匀切改能量切；转正"永远分段"、删整段机器。
3. **彻查**所有音频切点改能量（§8）。
4. 用 2h / 3h22m 音频端到端验收（413 消失、请求数从数百降到十几、质量不降）。
5. 稳定后重建实例（拉 llm-init audiobase5 等）。

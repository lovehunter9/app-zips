FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache bash && \
    npm install -g @upstash/context7-mcp@2.1.4

ENV MCP_TRANSPORT=http
ENV PORT=8080

EXPOSE 8080

CMD ["context7-mcp", "--transport", "http", "--port", "8080"]

# 工作日志 2026-06-18 — 音频模型基座 × LLM Gateway 集成(mode 统一)

> 交接给:周一(2026-06-22)复工的自己。
> 这次的核心:把 `MODEL_CAPABILITY` 这条多余的轴**彻底删掉**,全系统(chart / gateway / market)
> 统一成**一个** `mode` 字段。两个仓库各提了一个 commit(见末尾)。

## 一、本次做了什么

### A. chart `audiolabwv3`(仓库 `app-zips`)
- env `MODEL_CAPABILITY` → **`MODEL_MODE`**(15 个选项原样保留:stt / stt_stream / tts /
  tts_clone / tts_dialogue / vad / diar / diar_stream / translate / enhance / embed /
  align / audio_llm / audio_s2s / sound_fx)。
- `engine.yaml`:变量 `$modelMode`、容器名 `audio-<mode>-<model>`、label `audio.capability` →
  **`audio.mode`**、引擎镜像映射 / 占位镜像 / env 全部按 `MODEL_MODE`。
  - stt / vad → `beclab/fedirz-faster-whisper-server:0.6.0-rc.3-cuda`(vad 复用 + `/wrappers/vad.py`)
  - diar → `beclab/maximsachs-pyannote_fastapi:4.0.4`(+ `/wrappers/diar.py`)
  - 未接线的 mode → `beclab/voice-engine-<mode>:notbuilt` 占位(一眼看出没建)
- `wrappers.yaml`:wrapper 返回 JSON 的 `capability` key → `mode`。
- `Chart.yaml` / `audiolabwv3-CLONE.md`(含 `kubectl -L audio.mode` 与 custom-columns)同步。
- 已用 `helm template` 渲染验证 stt/vad/diar/tts(占位)四种 mode 全部正确落镜像+容器名+label。
- 已 `helm package` 重打 `audiolabwv3-1.0.0.tgz`(版本仍锁 1.0.0;tgz 不入库,周一现打或用现有的上传)。

### B. gateway(仓库 `llm-gateway`,分支 `feat/market-real-llm-gateway-supported`)
- **mode 枚举全量扩容**:迁移 `000018_widen_modes_audio` 把三张表
  (`provider_models` / `tenant_default_models` / `system_default_models`)的 mode CHECK
  在原 10 值基础上加入 13 个音频 mode(stt/tts 原本就有)。
  - `providers/model.go`:加 13 个 `Mode*` 常量 + 扩 `AllModes`。
  - `defaults/model.go`:`AllModes` 同步扩(音频 mode 可被 `default-<mode>` 选取)。
- **STT 数据面**:`POST /v1/audio/transcriptions`(`internal/api/v1/audio.go`)。
  - multipart 原样透传:adapter 接口加 `AudioTranscriptions`;`openai_compatible` 用 `doRaw`
    透传 boundary;anthropic / gemini / azure 返回 `ErrAudioUnsupported`(→ 422)。
  - 错误码 `audio_mode_mismatch` / `audio_unsupported_for_provider`;spend mode `stt`。
- **market 自动 seed**:`olares_sync` 在 upsert provider 成功后,按 Market `model_metadata`
  (model_name + mode)自动建 `provider_models` 行(`ON CONFLICT DO NOTHING`,不覆盖管理员改动)。
- 注释已按要求精简(描述性注释 1–3 行)。
- `go build` + 受影响包测试(providers / defaults / api/v1 / adapters)**全绿**。

## 二、周一(6/22)复工步骤

1. **重装基座 + 模型 APP**(因为 env 改名是破坏性的,旧实例 `MODEL_CAPABILITY` 失效):
   - 上传新的 `audiolabwv3-1.0.0.tgz`(没有就在 `app-zips` 里 `helm package ./audiolabwv3`)。
   - 按 `audiolabwv3-CLONE.md` 重新 clone 三类:STT / VAD / Diarize。
     - Diarize 门禁两步别忘:账号配 HF read token + 网页同意 `pyannote/speaker-diarization-community-1` 条款。
2. **找我验证三类引擎本体跑通**(脚本在 `audiolabwv3-CLONE.md` 第四节,改头两行 NS/DEPLOY 即可)。
3. 验通后**再打 GATEWAY 新镜像**(分支 `feat/market-real-llm-gateway-supported`),迁移 000018
   会在启动时自动跑,不用手动改库。
4. 跑**整个 STT 经 Gateway 的端到端集成**:
   - 确认 market 自动 seed 了 STT 的 `provider_models` 行(或管理员手动 add 一个 mode=stt 的 model)。
   - `POST /v1/audio/transcriptions`(multipart:file + model),期望透传到引擎并返回 `{"text": ...}`。

## 三、坑 / Backlog(别再踩)
- **破坏性改名**:`MODEL_CAPABILITY` → `MODEL_MODE`。旧 clone 必须重建,模板会回退默认 `stt`。
- **ARM64 + GPU 的 Diarize**:现 pyannote 镜像仅 amd64,DGX Spark(GB10/ARM64)跑不了。多架构
  起点 `voice-engine-pyannote/`(CPU 版 Dockerfile+build.sh);打多架构 GPU 镜像时装齐
  ffmpeg/torchcodec,可去掉 `diar.py` 里的自解码 workaround。
- **门禁模型**:不配 token / 不同意条款 → llm-init 401、实例卡 `wait-models` 最终 Stopped。
- **gateway 数据面目前只有 STT 路由**;vad/diar 的 mode 枚举位已占好、可注册可 seed,但
  `/v1/audio/vad`、`/v1/audio/diarization` 的网关路由还没建(下一阶段)。
- gateway 里 `third_party/lldap-clients-go/go.sum` 是无关改动,**没**进本次 commit。

## 2026-06-22 进展
- chart 体检 ✓:`helm lint`(带 values)0 failed;stt/vad/diar 渲染镜像+容器名+`audio.mode` 全对。
- gateway 分支 `e9927c7` ✓:`go build` + providers/defaults/api·v1/adapters 测试全绿。
- **三类引擎本体重建后全部验通**(实例:stt `audiolabwv304708e` / vad `audiolabwv32d0079` / diar `audiolabwv3e65afc`):
  - STT → 完整 jfk 文本;VAD → `mode:vad` + segments;Diar → `device:cuda` + 2 speakers/13 段。
- 下一步:打 Gateway 新镜像 → STT 经网关端到端。端到端关键风险仍是 Market `model_metadata` 是否带 `mode=stt`
  (决定 auto-seed 是否生效;不生效则管理员手动 add 一个 mode=stt 的 model)。

## 2026-06-22 进展(续)— 公网入口根因 + chart 修复
- 网关镜像 `lovehunter9/llm-gateway-backend:v2.0.6-test3`(amd64;Dockerfile 加 `$BUILDPLATFORM`
  原生交叉编译,修 arm64 Mac QEMU 崩溃)。迁移撞号:仓库已有 `000018_users_source_lldap`,
  我们的迁移改号 **000018 → 000019**;起来后 `migrations applied 18→19` 正常。
- **Market 自动发现不通**:`olares_sync` 打的是 `GET /app-store/api/v2/llm-providers`,真底座 404;
  且真端点 `POST /apps/llm-gateway-supported` 的响应不带 base_url / model_metadata。=> 自动发现+
  auto-seed 是公司侧 mock/真实没对齐的独立坑,今天不依赖它。
- **公网 URL 根因**:引擎之前放在 `sharedEntrances`(`port:0` + `invisible:true`),平台**没分配 URL**
  (app 的 `status.entranceStatuses` 里只有下载面板 `audiolabclient`)。参考 `apps/ollama`(模型服务
  走 `entrances` + 真实端口)。**已改 chart**:引擎改成可见常规入口 `audiolabapi`(→ `sharedentrances-api:80`
  → 引擎),拿公网 URL;base_url = `https://<id>.<user>.olares.com/v1`。已重打 tgz(1.0.0)。
- 待办:重装 STT → 取 audiolabapi 公网 URL → 网关 UI 注册 provider(openai_compatible)+ model(mode=stt)
  → STT 端到端。风险:authLevel internal 若挡住网关 server-to-server,改 public 再来一次。

## 四、本次 commit
- `llm-gateway` @ `feat/market-real-llm-gateway-supported`:audio mode 统一 + STT 数据面 + 自动 seed。
- `app-zips`:audiolabwv3 chart `MODEL_CAPABILITY` → `MODEL_MODE` + 文档/日志。

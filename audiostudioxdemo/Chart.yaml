apiVersion: v2
appVersion: "1.0.7"
description: Audio Studio X Demo — a browser app that runs the M1 audio capabilities (STT, translate, VAD, diarization, enhance, speaker embedding) plus real-time streaming captions (stt_stream, with optional timecoded subtitles) through the LLM Gateway.
name: audiostudioxdemo
type: application
version: 1.0.7

# 音频基座（audiobasexv3）实例台账

> 每次 re-clone 后更新。RULE-1（无就地编辑,必须 uninstall→delete→upload→clone；title+env 复现则
> hash/URL 不变）见 `.cursor/skills/audiolabxv3-instances/SKILL.md`。RULE-2:clone 表单只有
> **title + MODEL_SOURCE + MODEL_NAME + MODEL_SUPPORTS + AUDIO_REQUIRED_GPU_MEMORY** 五项,别的一律不 `--env`。
> 公网 URL olares-cli 查不到——问用户（title+env 不变则 URL 不变,无需重指 provider）。

## clone 复现清单（9 个 audiobase 实例；translate 的 `llamacpphymt218bggufv3` 是另一条 llm-init 分支,不在此列）

llm-init 镜像：`docker.io/lovehunter9/llm-init:v1.3.2-audiobase6`（4 个 audiobase 基座 chart 均已指向;rebase 到最新 main 后重打）。
网关镜像(用户 roll)：`lovehunter9/llm-gateway-backend:v2.0.7-audiobase3` + `lovehunter9/llm-gateway-frontend:v2.0.7-audiobase3`。

| chart | title(精确,hash 输入) | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU | 当前 app-name |
|---|---|---|---|---|---|---|
| `audiovllmbasexv3` | `Audio vLLM Qwen3-ASR` | `hf://Qwen/Qwen3-ASR-1.7B` | `Qwen/Qwen3-ASR-1.7B` | `stt,stt_stream` | `12Gi` | audiovllmbasexv3fd501a |
| `audiovllmbasexv3` | `Audio vLLM Align` | `hf://Qwen/Qwen3-ForcedAligner-0.6B` | `Qwen/Qwen3-ForcedAligner-0.6B` | `align` | `4Gi` | audiovllmbasexv36ab52b |
| `audiovllmbasexv3` | `Audio vLLM Whisper Large V3` | `hf://openai/whisper-large-v3` | `openai/whisper-large-v3` | `stt` | `8Gi` | audiovllmbasexv39ecddb |
| `audiopyannotebasexv3` | `Audio Pyannote VAD` | `hf://onnx-community/silero-vad` | `silero-v5` | `vad` | `0` | audiopyannotebasexv3029777 |
| `audiopyannotebasexv3` | `Audio Pyannote Diar` | `hf://pyannote/speaker-diarization-community-1` | `pyannote-community-1` | `diar` | `4Gi` | audiopyannotebasexv3f98a8d |
| `audiopyannotebasexv3` | `Audio Pyannote Embed` | `hf://pyannote/embedding` | `pyannote-embedding` | `speaker_embed` | `0` | audiopyannotebasexv38b9c8a |
| `audiopyannotebasexv3` | `Audio Pyannote Enhance` | `hf://speechbrain/mtl-mimic-voicebank` | `mtl-mimic-voicebank` | `enhance` | `2Gi` | audiopyannotebasexv3544ba0 |
| `audiofasterwhisperbasexv3` | `Audio Faster-Whisper Large V3` | `hf://Systran/faster-whisper-large-v3` | `Systran/faster-whisper-large-v3` | `stt` | `6Gi` | audiofasterwhisperbasexv311be67 |
| `audionemobasexv3` | `Audio NeMo Diar Stream` | `hf://nvidia/diar_streaming_sortformer_4spk-v2.1` | `diar-streaming-sortformer` | `diar_stream` | `6Gi` | audionemobasexv3b7ca1e |

> gated repo(pyannote diar/embed、nvidia sortformer)靠用户 Olares HuggingFace 集成 token 自动注入 HF_TOKEN,**不 `--env`**。

## RULE-1 re-clone 周期（每个 chart 各走一遍）

```bash
# 1) uninstall 该 chart 的所有 clone
olares-cli market uninstall <appname> ...   # 每个 clone
# 2) 删 chart（需其所有 clone 已 uninstall）
olares-cli market delete <chart>
# 3) 重打包 + 上传（模板改了必须重打包）
helm package <chartdir>            # 产出 <chart>-1.0.0.tgz
olares-cli market upload <chart>-1.0.0.tgz
# 4) 按上表 title+env 逐个 clone（--watch）
olares-cli market clone <chart> -s upload --title "<T>" \
  --env MODEL_SOURCE=<..> --env MODEL_NAME=<..> --env MODEL_SUPPORTS=<..> \
  --env AUDIO_REQUIRED_GPU_MEMORY=<..> --watch
```

## 变更历史
- 2026-07-23：从会话 transcript 挖出全部 9 个 clone env 并首次落盘（此前无 audiobasexv3 台账）。
  触发 re-clone 的改动：llm-init→audiobase5（去 /v1/align + GPU diag）+ wrapper 加 `segments` 批量分支。

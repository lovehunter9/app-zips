# 音频基座实例台账（当前世代：x2v3）

> 每次 re-clone 后更新。RULE-1（无就地编辑，必须 uninstall→delete→package→upload→clone；
> title+env 复现则 hash/URL 不变）见 `.cursor/skills/audiolabxv3-instances/SKILL.md`。
> RULE-2：clone 表单只有
> **title + MODEL_SOURCE + MODEL_NAME + MODEL_SUPPORTS + AUDIO_REQUIRED_GPU_MEMORY** 五项，别的一律不 `--env`。
> 公网 URL olares-cli 查不到——问用户（title+env 不变则 URL 不变，无需重指 provider）。

## 基座 chart ↔ 引擎镜像

引擎镜像由 `beclab/audio-engines`（分支 `feat/audio-stt`）CI 打出，wrapper 已烤进镜像。
llm-init 统一 `docker.io/lovehunter9/llm-init:v1.3.3-test1`（四个 chart 一致）。

| chart | 引擎镜像（chart 默认值） | 覆盖能力 |
|---|---|---|
| `audioqwenbasex2v3` | `lovehunter9/audio-qwen:stt-async-test2` | `stt`、`stt_stream`、`align`（stt 家族与 align 互斥，各自一个实例） |
| `audiofasterwhisperbasex2v3` | `lovehunter9/audio-fasterwhisper:stt-async-test2` | `stt`（含 `segments` 批量、`/v1/audio/translations` 语音转英文） |
| `audiopyannotebasex2v3` | `lovehunter9/audio-pyannote:stt-async-test2` | `vad`、`diar`、`speaker_embed`、`enhance` |
| `audionemobasex2v3` | `lovehunter9/audio-nemo:stt-async-test2` | `diar_stream` |

> 四个基座**版本号从此取齐**（同一 tag）。镜像的打法也统一了：依赖层单独发布成
> `deps-<配方哈希>`，运行时镜像 = 依赖层 + 一层 wrapper（crane 在 registry 内追加），
> 所以改 wrapper 出新 tag 只要几十秒、镜像站增量约 164KB。详见工作日志「二之九」。

> 这一代镜像起支持**异步任务制**：能力 POST 带表单字段 `async=1` 即返回 202 + 任务 id，
> 用 `GET /v1/audio/tasks/{id}`（`/result`、`DELETE` 取消）拿进度与结果；不带就是老的同步行为。
> 四个 chart 另加了可选 env 透传（不设不渲染）：`LOAD_TIMEOUT_S`、`TASK_TTL_S`，
> qwen 另有 `VLLM_CAPTURE_SIZES`、`VLLM_ENFORCE_EAGER`。

> 镜像 tag 变了就必须走一遍 RULE-1（clone 时把 tag 固化进了实例的 chart 副本）。

## clone 复现清单（9 个实例）

| chart | title（精确，hash 输入） | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU | 当前 app-name | 公网 URL（用户提供，2026-07-26） |
|---|---|---|---|---|---|---|---|
| `audioqwenbasex2v3` | `Audio Qwen X2 Qwen3-ASR` | `hf://Qwen/Qwen3-ASR-1.7B` | `Qwen/Qwen3-ASR-1.7B` | `stt,stt_stream` | `12Gi` | `audioqwenbasex2v39319c8` | https://92461791.olarestest003.olares.com |
| `audioqwenbasex2v3` | `Audio Qwen X2 Forced Aligner` | `hf://Qwen/Qwen3-ForcedAligner-0.6B` | `Qwen/Qwen3-ForcedAligner-0.6B` | `align` | `4Gi` | `audioqwenbasex2v3e71e2d` | https://55b24eda.olarestest003.olares.com |
| `audiofasterwhisperbasex2v3` | `Audio FW X2 Systran Large V3` | `hf://Systran/faster-whisper-large-v3` | `Systran/faster-whisper-large-v3` | `stt` | `6Gi` | `audiofasterwhisperbasex2v34ba5ad` | https://6d84d002.olarestest003.olares.com |
| `audiofasterwhisperbasex2v3` | `Audio FW X2 OpenAI Large V3` | `hf://openai/whisper-large-v3` | `openai/whisper-large-v3` | `stt` | `8Gi` | `audiofasterwhisperbasex2v3ffa81e` | https://5740267c.olarestest003.olares.com |
| `audiopyannotebasex2v3` | `Audio Pyannote X2 VAD` | `hf://onnx-community/silero-vad` | `silero-v5` | `vad` | `0` | `audiopyannotebasex2v3b8411d` | https://c86c5f42.olarestest003.olares.com |
| `audiopyannotebasex2v3` | `Audio Pyannote X2 Diar` | `hf://pyannote/speaker-diarization-community-1` | `pyannote-community-1` | `diar` | `4Gi` | `audiopyannotebasex2v3084328` | https://d50ab1f4.olarestest003.olares.com |
| `audiopyannotebasex2v3` | `Audio Pyannote X2 Embed` | `hf://pyannote/embedding` | `pyannote-embedding` | `speaker_embed` | `0` | `audiopyannotebasex2v34eafad` | https://0f6ad40d.olarestest003.olares.com |
| `audiopyannotebasex2v3` | `Audio Pyannote X2 Enhance` | `hf://speechbrain/mtl-mimic-voicebank` | `mtl-mimic-voicebank` | `enhance` | `2Gi` | `audiopyannotebasex2v309a372` | https://978daed8.olarestest003.olares.com |
| `audionemobasex2v3` | `Audio NeMo X2 Diar Stream` | `hf://nvidia/diar_streaming_sortformer_4spk-v2.1` | `diar-streaming-sortformer` | `diar_stream` | `6Gi` | `audionemobasex2v31dc21c` | https://0c2f44f3.olarestest003.olares.com |

> gated repo（pyannote diar/embed、nvidia sortformer）靠用户 Olares HuggingFace 集成 token 自动注入 HF_TOKEN，**不 `--env`**。
> title 上限 30 字符，超了 clone 会被拒；上表全部合规。
> 公网 URL 是**用户给的**（AI 查不到）。title+env 精确复现则 app hash 不变，URL 也不变——
> 这一轮九个 URL 里只有 fw 两个是真的变了（它们改了 title），其余照旧。

## 功能验证结论（2026-07-26 凌晨，九个实例逐个跑过）

| 实例 | 验过的东西 |
|---|---|
| Qwen3-ASR | 中/英单段转写、`segments` 三段批量、WS 流式（partial 滚动 + final 收敛） |
| Forced Aligner | `/v1/audio/align` 出字级时间戳（`language` 缺省走 auto，不再 500） |
| FW Systran | 单段、`verbose_json`（带 segments/logprob）、三段批量、`/v1/audio/translations` 中→英 |
| FW OpenAI | 单段、三段批量、`/v1/audio/translations` 中→英；首次加载自动转 CT2（33s） |
| VAD | `/v1/audio/vad` 出语音段与参数回显 |
| Diar | `/v1/audio/diarization` 两人四段 |
| Embed | `/v1/audio/embeddings` 512 维向量 |
| Enhance | `/v1/audio/enhance` 回 16k 单声道，时长与输入一致；`format=wav|flac|ogg` 三种都验过 |
| Diar Stream | WS 流式，final 四段与 pyannote 离线分离结果对齐（互为印证） |

> 九个 `/v1/models` 的自报（id / mode / supports / endpoints）全部正确：WS 端点标 `WS`，
> `GET /v1/models` 自身在列，`/v1/audio/translations` 只出现在 fw 两个实例上。
> 公网边缘偶发 SSL 断连（curl 35 / 空响应），重试即过，与基座无关。

## RULE-1 re-clone 周期（每个 chart 各走一遍）

```bash
# 1) uninstall 该 chart 的所有 clone
olares-cli market uninstall <appname> --watch    # 每个 clone
# 2) 删 clone 的 chart 副本，再删基座 chart
olares-cli market delete <clone-appname>
olares-cli market delete <chart>
# 3) 重打包 + 上传（模板改了必须重打包）
helm package <chartdir>                          # 产出 <chart>-1.0.0.tgz
olares-cli market upload <chart>-1.0.0.tgz
# 4) 按上表 title+env 逐个 clone
olares-cli market clone <chart> -s upload --title "<T>" \
  --env MODEL_SOURCE=<..> --env MODEL_NAME=<..> --env MODEL_SUPPORTS=<..> \
  --env AUDIO_REQUIRED_GPU_MEMORY=<..> --watch
```

## 变更历史

- 2026-07-28 16:1x：**llm-init 跟上上游 main**（main 落后 12 个提交，其中 translate/MTran 那一套
  是新的）→ 我们的 `feat/audio-stt` rebase 到 `c9b2c19`，只有 `internal/config/config_test.go`
  一处冲突（两边各自新增测试函数，都保留），构建 / vet / `go test ./...` 全绿。
  镜像 `lovehunter9/llm-init:v1.3.3-test1`（本地 buildx 推，Docker Desktop 需
  `docker --context=default buildx …`，否则 buildx 连 socket 报 `_ping` 500）。
  四个 chart 的 llm-init 镜像同步换成它，走完 RULE-1 重建九个实例：**全部 running、
  app-name 逐字一致、URL 不变**。自验：`/v1/models` 任务端点仍在（VAD 6 个、qwen 7 个），
  VAD 异步 202→`succeeded` 与同步返回一致。

- 2026-07-28 15:2x–15:4x：**四个基座 tag 取齐 `stt-async-test2`**，九个实例走完 RULE-1
  全部重建，**app-name 与上表逐字一致 → 九个公网 URL 全不变、gateway 零改动**。
  镜像改成"依赖层 + 追加 wrapper"（工作日志「二之九」）：造依赖一次 2m45s–6m23s，
  追加 wrapper 6–12s。
  clone 全部一次成功、无 `downloadFailed`：nemo 21s、pyannote 四个各 ~30s、qwen 两个共 2m11s、
  fw 两个共 1m。**昨晚那个"镜像站没同步"确实是当时的真实状态，不是长期不通**；
  另记一条教训：`https://mirrors-internal.joinolares.cn/v2/<repo>/manifests/<tag>` 的
  **匿名探测不能当判据**（`audio-fasterwhisper:audio-stt-test5` 探出 404，节点却一直拉得到），
  以后别再靠它决定要不要 clone，直接 clone 看结果。
  自验（公网）：三个实例 `/v1/models` 里任务四端点齐全（`GET /v1/audio/tasks`、
  `/{id}`、`/{id}/result`、`DELETE /{id}`），WS 只在 qwen、`translations` 只在 fw；
  VAD 实跑一次 `async=1` → 202 排队 → `succeeded`（`progress.stage=done, ratio=1.0`）。

- 2026-07-27 深夜：**四个基座同时换代**（qwen `test6` / pyannote `test5` / fasterwhisper `test5`
  / nemo `test3`，四个 CI 全绿：nemo 34s、pyannote 与 fw 各 5m9s、qwen 9m21s）。
  引擎侧加异步任务制 + 加载看门狗 + qwen CUDA graph 捕获收窄，详见工作日志「二之八」。
  RULE-1 走了一遍：9 个实例 uninstall → 副本随之消失 → 删 4 个基座 chart → 重打包上传 →
  按上表 title+env 重 clone 9 个。**9 个 app-name 与上表逐字一致 → 公网 URL 全部不变，
  gateway provider 零改动。** 起容器与功能自验由用户直接在实例上看。

- 2026-07-27：pyannote 基座换 `audio-stt-test3`（diar 批量化 + OOM 回退、enhance no_grad /
  可选 AMP / `format=wav|flac|ogg`），四个 pyannote 实例走了一遍 RULE-1，**app-name 与 URL 均未变**。
  引擎侧实测：diar 300s→17s（3h22m 推算 ≈11.5 分钟，仍超 300s 硬顶）；
  enhance 300s→1.0s（3h22m 推算 ≈40 秒，瓶颈是 388MB 的 WAV 响应体，Opus 可压到 ≈1/10）。
  其余三个基座镜像未变、chart 未动。

- 2026-07-25：切到 x2v3 世代（引擎镜像自建，wrapper 烤进镜像；vllm 基座拆成 qwen 基座）。
  `openai/whisper-large-v3` 从 vLLM 改挂 faster-whisper 基座（wrapper 首次加载时转 CT2 并缓存）。
  fw 两个实例改名为 `Audio FW X2 Systran/OpenAI Large V3`（原 `... X2 LV3` 是为压 30 字符硬缩的，弃用）。
- 2026-07-23：从会话 transcript 挖出全部 9 个 clone env 并首次落盘（xv3 世代，已下线）。

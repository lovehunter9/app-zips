# 音频能力重构 · 交接说明（2026-07-24 晚）

> 给后继对话：这是从"引擎+Wrapper 塞进 chart"过渡到"引擎烤镜像 + llm-init 去特殊化"的重构交接。
> **先把下面第 0 节该读的都读了，再动手。** 全程用简体中文（见 language 规则）。

---

## 0. 上手必读（务必先读）

**规则 / SKILL（不读会翻车）**
- `.cursor/rules/language.mdc` —— 全程简体中文。
- `.cursor/rules/local-service-restart.mdc` —— 本地起/重启 demo 端口服务的铁律（沙盒外 + full_network + 受管理后台 shell + 验网不验监听）。
- `.cursor/skills/chart-version-lock/SKILL.md` —— 未经明确指示不许改 chart 版本号。
- `.cursor/skills/audiolabxv3-instances/SKILL.md` —— audiobase 实例台账/克隆配方；**公网 URL 必须问用户**（olares-cli 查不到）。
- `.cursor/skills/local-dev-server/SKILL.md` —— 起 8090/8080 demo。
- `~/.agents/skills/olares-*`（market/cluster/dashboard/files/publish/shared 等）—— 需要动 Olares 时读。

**本次相关文档（app-zips 根目录）**
- `音频能力_示例模型与CLONE清单_2026-07-24.md` —— 能力→示例模型→基座→CLONE ENV；开头有两个 PR 地址 + 4 个基座临时地址。
- `音频能力_基座实例台账.md` —— 9 个 audiobase 实例的精确 clone 复现 + RULE-1 重 clone 周期。
- `音频能力_TTS调研与搁置_2026-07-24.md` —— Kokoro/CosyVoice 调研，TTS 本波搁置。
- `音频能力_llm-init改动方案.md` / `音频能力_批量STT与对齐_方案_2026-07-23.md` / `音频能力落地_当前实现说明.md` / `音频能力_新一套_执行清单.md` / `translate-api.md`。
- 工作日志 `音频能力_工作日志_2026-07-2*.md`。

**过去对话**：agent-transcripts 里当前这条会话的完整记录（关键词搜索：stt-audio-base / migration / 000023 / de-special）。

---

## 1. 已交付状态（重构的起点）

- **llm-init PR #46**（分支 `stt-audio-base`，commit `a075678`）：`ENGINE_KIND=audio` 反代基座，CI 全绿。
- **llm-gateway PR #12**（分支 `stt-audio-base`，commits `39a2bd4` audio + `fbeefac` chore(lint)）：audio data plane（mode=audio + MODEL_SUPPORTS），迁移 `000023_audio_mode`，CI 全绿。lint commit 顺带清了 main 既有 lint 债（gofmt / 6 个噪声 gocritic-style 禁用 / misspell 豁免 analogue|dialogue|behaviour / 2 处补 doc / appendAssign）。
- **llm-gateway `feat/audio-base`**（测试分支）：audio(`39a2bd4`) + 一次性迁移回退脚本（单独 commit，`deploy/manual/audio_mode_rebase_21_to_23.sql`）+ **TEMP translate**（单独 commit，永不进 PR）。
- **测试镜像已 push**：`lovehunter9/llm-gateway-backend:v2.0.9-audiobase1`、`…-frontend:v2.0.9-audiobase1`（linux/amd64）。已在测试机验证迁移 20→23 正常。
- **app-zips**：4 个音频基座 chart（`audio{vllm,fasterwhisper,pyannote,nemo}basexv3`）注释已精简；2 个 TTS 草稿 chart（`audiokokorobasexv3`/`audiocosyvoicebasexv3`）搁置留档。

---

## 2. 老板 review 的新方向（= 本次重构目标）

**一句话**：音频**不是特例**，它就是"又一种被反代的引擎"。llm-init 只把 `/v1/*` 透传给引擎；能力/健康/模型列表/错误**全部由引擎自报**；引擎连同 wrapper **烤进镜像**、由独立工程维护，chart 极简。**代码与注释从头到尾都不许显得音频特殊。**

### 2.1 四项已敲定的决定

1. **GPU 显存 UI（保留）**：改为引擎暴露**统一 `/metrics`**（通用命名，别用 `audio_` 前缀显得特殊），llm-init 通用中继读取显示；vLLM/llamacpp 既有原生指标**本次不动**（历史既有，非音频特殊）。
2. **`/v1/models`（引擎给）**：必须由引擎（模型应用）提供，**删掉 llm-init 的伪造 `writeAudioModels`**，像对 vLLM 一样直接反代。
3. **`MODEL_SUPPORTS` / `MODEL_MODE=audio` / `knownAudioSupports` / `audio_precheck`（llm-init 里删）**：llm-init 不再拿它做门禁/合成/预检；`MODEL_SUPPORTS` 仅作 **env 透传给引擎**。⚠️ **网关侧的 `mode=audio`+supports 门禁是网关自己的语义，保留不动**。接受"配错能力时晚一点由引擎报错，而非下载前预检"。
4. **端点清单 UI（保留）**：改为**引擎在 `/v1/models`（或旁述接口）返回自己的 `supports`/`endpoints`**，llm-init dashboard **通用渲染**"引擎自报的清单；没有就回退默认 OpenAI 路由"。**删掉 llm-init 里硬编码的 `audioCapRoutes` + supports 门禁**。

### 2.2 llm-init 去特殊化清单（删掉散落的 `if kind==EngineAudio`）

看老板给的 5 张图，逐项：
- **图1** `respHeaderTimeout` 音频拉到 3600s → **删**（无充分证据；LLM 也超 60s 却不拉高；真需要长响应 → 引擎侧流式，或作为全局非音频专属策略另议）。
- **图2** `WaitAlive`/`Ready` 的 audio 分支 → **删**，用与其它反代引擎一致的 `/v1/models` 探测。
- **图3** `audioHealthPaths` + `audioHealthAlive`（探 /healthz、/health）→ **从 llm-init 删**（health 不放 llm-init，与 LLM 一致；LLM 干脆不在 llm-init 做 health）。
- **图4** `audioPathSupport` + `audioSupportGated` + `OpenAIHandler` 里的边缘 gate/synth → **删**，纯透传给引擎、引擎自己决定/报错。
- **图5** `config_load_engine.go` 的 `defaultAudioURL`：**常量保留**（与 `defaultEmbedURL` 同级），**删多余注释**。

**目标形态**：llm-init 里 audio ≈ `EngineEmbed`（一个 proxy 引擎 + 默认 URL + factory 的 proxy case），**零音频散点分支**。

### 2.3 引擎镜像契约（新基座工程要实现）

每个引擎镜像**必须**提供：
- `GET /v1/models` —— 含 `id` + `mode` + `supports` + `endpoints`（能力/端点自报，喂给决定 2、4）。
- 统一 `GET /metrics` —— 通用显存/占用指标（喂给决定 1）。
- `GET /health` —— 引擎自己的健康。
- 能力判断与错误（不支持的 op 自己回 404/错误）。

---

## 3. 新基座工程（待与用户讨论后再建）

- 独立 repo：把现在塞在 chart `ConfigMap` 里的 Python wrapper **全部搬进去**，**构建期**装依赖，把引擎 + wrapper 烤成**一个镜像**（不再运行时 pip、不再 sentinel-wait 那套 hack），有正经 build/test/CI。
- chart 退化成"部署一个现成镜像 + llm-init 反代"，极简。
- **下游（gateway、两个 demo）接口不变**——这波是把复杂度从 llm-init/chart 挪进引擎镜像 + 去特殊化，对外输出不动。

---

## 4. 分支与下一步

- **用户已建新分支**（基于刚交 PR 的 `stt-audio-base`，**主战场是 llm-init**——图1–5 全是 llm-init 的 `internal/adapter/proxy/adapter.go`、`internal/config/config_load_engine.go`、`internal/controlplane/endpoints.go`）。**分支名请向用户确认**。
- 用户明确授权：**那个 commit 的内容可以全部推翻重来**。
- **本次任务**：先出一版 **llm-init 去特殊化 + UI 改为引擎自报** 的代码给用户看。**基座新工程等讨论完再建**。
- gateway #12 基本完成；如老板要同样原则清理 gateway 的音频 handler，是后续跟进，不是当前焦点。

---

## 5. TEMP translate 现状（别误删）

- llm-init 与 llm-gateway 的 **main 目前都没有** translate 文本翻译接口：gateway 的 PR #5 已 CLOSED，llm-init 的 PR #42 仍 OPEN 未合。
- 所以 gateway 的 **TEMP translate passthrough 仍必需**，留在 `feat/audio-base`（测试），**不进 PR**。等 #42 合入 main 后再撤、改用正式接口。

---

## 6. 测试环境 / 运维要点

- **网关 DB**（os-platform 的 citus）：
  ```
  psql "postgresql://llmgatewayv3_olarestest003_llmgatewayv3:llmgatewayv3@citus-master-svc.user-system-olarestest003:5432/llmgatewayv3_shared_llmgatewayv3?sslmode=disable"
  # 用 kubectl -n os-platform exec citus-0 -- psql ... 执行
  ```
  迁移验证套路：老网关在跑时先 `UPDATE schema_migrations SET version=20, dirty=false;`，再滚新镜像，后端启动自动 `migrate up` 跑 21/22/23。
- **后端启动即自动 migrate**（`cmd/gateway/main.go` `db.Run(...)`，失败即 Fatal）。
- **部署 / 滚镜像 / 连库执行 永远由用户手动**；AI 只负责本地代码、打包、本地起 demo。
- 音频基座实例重建走 RULE-1（见台账）；**公网 URL 问用户**。

---

## 7. 机器 / 工具红线（血泪，别重蹈覆辙）

- **严禁本地全量跑 `golangci-lint`**：会吃满内存把这台 Mac 拖死（今天翻过车）。**lint 交给 CI**。`gofmt`、单包 `go vet` 是轻的，可用。
- **Docker 镜像构建很重**：只在要打镜像时开 Docker Desktop，用完退掉（今晚已退，释放内存）。别留 buildx buildkit 容器。
- **Shell 工具有 pipefail/errexit**：`grep` 无匹配返回 1 会**中止整条命令**（表现为莫名其妙的空输出/exit 1）→ grep/管道后加 `|| true`，或用 `if` 块。杀端口/进程同理。
- **llm-init、llm-gateway 在工作区外**：git 写操作要 `required_permissions:["all"]`；联网操作加 `full_network`。
- **本地 main 落后是常态**：rebase / 建分支前先 `git fetch` + 把本地 main 更新到 origin/main（`git branch -f main origin/main`）。
- CI lint 版本：llm-init 用 golangci-lint **v2.12.2**；llm-gateway 用 **v1.64.8**（源码用项目 go 版本现编，见 backend-ci.yml 注释）。gateway lint 配置在 `backend/.golangci.yml`。

---

## 8. 当前 git 快照（llm-gateway，供核对）

- `feat/audio-base`：`… TEMP translate` / `… chore(migrate) 回退脚本` / `39a2bd4 feat(audio)` / `fbeefac chore(lint)`（注：#12 分支是 `39a2bd4`+`fbeefac`；feat/audio-base 在其上再叠回退脚本 + translate）。
- `stt-audio-base`：`fbeefac` + `39a2bd4`（= PR #12）。
- llm-init `stt-audio-base`：`a075678`（= PR #46）。

# Per-workload replica counts (keys match workloadReplicas in OlaresManifest):
#   audiofasterwhisperbasex2v3 = engine Deployment (== Release.Name), llminit = llm-init proxy Deployment.
workloads:
  audiofasterwhisperbasex2v3:
    replicaCount: 1
  llminit:
    replicaCount: 1

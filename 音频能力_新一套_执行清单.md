# 音频能力 · 新一套 执行清单

> 目标：新开一套"mode=audio + supports 多选 + 一镜像一 base"的音频体系。**现有 12 个 audiolabx2v3 实例完全不动**，本清单只涉及代码/chart 层。
> 部署/上架仍是人工，AI 只负责本地代码、打包、本地自测。

---

## 一、已锁定的决策

1. **现有实例不动**，另起新一套。
2. **粗 mode 收敛为 `audio`**；能力粒度下沉为 **`MODEL_SUPPORTS` 多选**。
3. **support 词表 = 音频 mode 全表，`embed`→`speaker_embed`**（见下表，共 15 项）。
4. **`MODEL_SUPPORTS` 用 `editable` 多选下拉**（照 `llamacppllmbasev3` / `vllmllmbasev3`），选项按各 base 引擎能力收窄。
5. **clone 为默认**（填 `MODEL_NAME`）；"专用非 clone"仅在确认官方 1:1（无其它版本/量化）时，用 `editable:false` 固定 supports（参照 `ollamaornith35bv3`）。
6. **一镜像一 base**：每个实际模型只挑对它最好的 base；不跨引擎 auto-select；接受多引擎复杂度。
7. **命名**：`audio<引擎>basexv3`（`x`=开发期）。
8. **llm-init**：新分支 off `main`，镜像 `docker.io/lovehunter9/llm-init:v1.3.2-audiobase1`（`lovehunter9`=推送位置；版本号 `-` 后用 `audiobase<N>` 新计数序列，好区分）。
9. **llm-gateway**：新分支 off `main`，版本 `2.0.7-audiobase1`（同上，`-` 后新序列）。
13. **应用层新写 x2 副本**：audiominutes / audiostudio 都**另写一份 `x2`，不动原版**，以防方案回溯。
10. **校验时机**：下载大权重**之前**用 HF 元数据预检（见线 A）。
11. **未知 support token**：拒绝报错（fail-fast）。
12. **chart 版本锁定**：1.0.0 不擅动。

---

## 二、support 词表（→ 端点）

| support | 端点（llm-init 放行） | 是否已有引擎 |
|---|---|---|
| stt | `POST /v1/audio/transcriptions` `/translations` | ✅ vLLM / faster-whisper |
| stt_stream | `WS /v1/audio/stream` | ✅ vLLM(qwen) |
| vad | `POST /v1/audio/vad` | ✅ pyannote 镜像 |
| diar | `POST /v1/audio/diarization` | ✅ pyannote |
| diar_stream | `WS /v1/audio/diarize/stream` | ✅ NeMo |
| align | `POST /v1/audio/align`(+`/v1/align`) | ✅ vLLM(aligner) |
| enhance | `POST /v1/audio/enhance` | ✅ pyannote 镜像(speechbrain) |
| speaker_embed | `POST /v1/audio/embeddings` | ✅ pyannote |
| tts | `POST /v1/audio/speech` | ⬜ 暂无 |
| tts_clone | （克隆 TTS，端点待定） | ⬜ 暂无 |
| tts_dialogue | `POST /v1/audio/dialogue` | ⬜ 暂无 |
| audio_llm | `POST /v1/audio/qa` `…/understand` | ⬜ 暂无 |
| audio_s2s | `WS /v1/audio/conversations` | ⬜ 暂无 |
| sound_fx | `POST /v1/audio/soundfx` | ⬜ 暂无 |

> 未建引擎的能力：词表/映射先登记（gateway AllSupports 收全），但不做 base、不放行。

---

## 三、Base 清单（4 个）

| base（=官方引擎镜像） | 可选 supports | 模型举例 | 形态 |
|---|---|---|---|
| `audiovllmbasexv3` | stt / stt_stream / align | whisper、qwen3-asr、qwen-aligner | clone |
| `audiopyannotebasexv3` | vad / diar / speaker_embed / enhance | silero、pyannote-* / speechbrain | clone |
| `audionemobasexv3` | diar_stream | sortformer | clone（除非确认 1:1） |
| `audiofasterwhisperbasexv3` | stt | CT2 whisper | clone |

---

## 四、执行清单

### 线 A · llm-init（分支 off main，镜像 `v1.3.2-audiobase1`）
- [ ] 端点门控由 `MODEL_MODE` 改为读 `MODEL_SUPPORTS`；`MODEL_MODE` 固定 `audio`
- [ ] 内置 support→端点映射表（15 项；未建引擎登记但不放行）
- [ ] `/v1/models` 合成：自报 `id=MODEL_NAME, mode=audio, supports=[...]`
- [ ] **下载前 HF 元数据预检**：
  - [ ] 拉 `config.json` / 文件清单 / model card（不下大权重）
  - [ ] 维护 `arch/model_type → 可支持能力集合 + 适配引擎` 映射表
  - [ ] `MODEL_SUPPORTS` 与推断不符、或模型格式与本 base 引擎不符 → **readyz 立即 fail + 明确报错**
  - [ ] 推断不了的回落到"声明 + load 时兜底"
- [ ] supports 校验对照 gateway `AllSupports`；**未知词拒绝报错**
- [ ] dashboard/端点目录：从"按 mode"改为"遍历 supports"展示
- [ ] 回归：多 WS + 多 POST 并存透传、health、3600s 超时、NotReadyGuard

### 线 B · audiolab chart（4 个新 base；现有 audiolabx2v3 不动）
每个 base 通用：
- [ ] 写死 `ENGINE_IMAGE`，删掉跨引擎 auto-select（`$engines/$engineAlts/按名字判别`）
- [ ] **多能力 dispatcher wrapper**：按 `MODEL_SUPPORTS` 在同一引擎端口挂对应 router
  - [ ] vLLM base：Qwen **单份权重同时服务 `stt`+`stt_stream`**（不双加载显存）
- [ ] 克隆表单：title + `MODEL_NAME` + `MODEL_SOURCE` + `MODEL_SUPPORTS`(multiSelect) + GPU（`MODEL_MODE` 去掉/固定 audio）
- [ ] OlaresManifest：`MODEL_SUPPORTS` = `editable` 多选，**options 只列该 base 能干的**
- [ ] 引擎容器引用 llm-init `v1.3.2-audiobase1`
- [ ] 保留：`$pydepsPrelude` / 下载 sentinel 等待进主容器 / GPU-HAMi 显存计算
- [ ] chart 版本 1.0.0 锁定

各 base supports 选项：
- [ ] `audiovllmbasexv3`：stt / stt_stream / align
- [ ] `audiopyannotebasexv3`：vad / diar / speaker_embed / enhance
- [ ] `audionemobasexv3`：diar_stream
- [ ] `audiofasterwhisperbasexv3`：stt

### 线 C · llm-gateway（分支 off main，版本 `2.0.7-audiobase1`）
- [ ] `AllSupports` 镜像加入音频 support key（含 `speaker_embed`；不含 translate——它是文字模型侧）
- [ ] 接受 `mode=audio`；端点调用按模型 `supports` 校验
- [ ] provider auto-seed 从 Market metadata 读 `supports`
- [ ] 保留旧 per-mode 兼容（现有实例仍在跑）

### 线 D · 应用层（新写 x2 副本，不动原版）
> audiominutes / audiostudio 各**新写一份 `x2`**（如 audiominutesx2 / audiostudiox2），原版保持不动以便回溯。
- [ ] 模型选择/过滤：从"按 mode"改为"按 supports 包含"（转写 stt、对齐 align、问答 speaker_embed/embedding 等处；翻译 translate 走文字模型侧，不在音频）
- [ ] provider 注册脚本：改用 `mode=audio` + `supports`
- [ ] `embed → speaker_embed` 命名跟随

---

## 五、顺序 / 依赖

1. 先 **线 C 的 AllSupports** + **线 A 的 llm-init**（否则 supports 校验通不过；开发期可让 llm-init 校验先宽松）。
2. 再 **线 B 的 chart**（引用 `v1.3.2-audiobase1`，含 dispatcher + HF 预检依赖）。
3. 最后 **线 D 的 audiominutes** 适配。
4. 全程不碰现有 12 实例。

---

## 六、验证口径
- 新 chart 独立上线，完全不碰现有 12 实例。
- 重点验证**多能力单实例**：Qwen-ASR `supports=[stt,stt_stream]` → 一份权重、两端点、显存不翻倍。
- 验证 **HF 预检**：给非流式模型误勾 `stt_stream` → 部署后**秒级** readyz 报错（不等下载）。

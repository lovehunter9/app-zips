{{- $opencodeDomainENV := split  ","  .Values.domain.opencode -}}
{{- $opencodeDomain := index $opencodeDomainENV "_0" -}}
{{- $env := .Values.olaresEnv | default dict -}}
{{- $enableOMO := ne (index $env "OPENCODE_OMO" | default "false") "false" }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opencode
  namespace: "{{ .Release.Namespace }}"
  labels:
    io.kompose.service: opencode
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: opencode
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.network/chrome-default: "true"
        io.kompose.service: opencode
        bytetrade.io/terminal: opencode
    spec:
      initContainers:
        - name: init-setup
          image: "docker.io/beclab/lovehunter9-anomalyco-opencode:1.16.0-test1"
          command:
            - sh
            - "-c"
            - |
              set -e
              # python3 is baked into the forked image; no apk / network needed here.
              OC_DOMAIN="{{ $opencodeDomain }}"
              export OLARES_OC_DOMAIN="$OC_DOMAIN"
              export OLARES_ENABLE_OMO="{{ if $enableOMO }}1{{ else }}0{{ end }}"
              export OLARES_OMO_VERSION="{{ index $env "OPENCODE_OMO_VERSION" | default "4.7.5" }}"
              mkdir -p /home/opencode/workspace \
                       /home/opencode/.opencode \
                       /home/opencode/.config/opencode \
                       /home/opencode/.config/opencode/skills/web-preview \
                       /home/opencode/.config/opencode/skills/system-admin \
                       /home/opencode/.pkg-queue
              SKILL="/home/opencode/.config/opencode/skills/web-preview/SKILL.md"
              printf '%s\n' \
                '---' \
                'name: web-preview' \
                'description: "Framework-specific dev-server configs (Vite / Next.js / FastAPI / Flask / static / Go / Express), HMR setup, and a troubleshooting table for previewing web pages in Olares. The baseline 5-step launch flow is authoritative; this skill only fills in per-framework details."' \
                '---' \
                '' \
                '## Scope' \
                '' \
                'The mandatory 5-step launch flow is in the system baseline (always in your context).' \
                'This skill provides framework config snippets, optional HMR setup, and a troubleshooting table.' \
                'Do NOT re-derive the launch flow here; follow the baseline and use this document for details only.' \
                '' \
                '## Framework configs' \
                '' \
                '### Vite (vite.config.js / vite.config.ts)' \
                '' \
                '```js' \
                'export default defineConfig({' \
                '  base: "/__preview/<PORT>/",' \
                '  server: {' \
                '    host: "0.0.0.0",' \
                '    port: <PORT>,' \
                '    // Optional: HMR over the TLS reverse proxy' \
                '    hmr: { protocol: "wss", clientPort: 443 },' \
                '  },' \
                '})' \
                '```' \
                '' \
                '### Next.js (next.config.js / next.config.mjs)' \
                '' \
                '```js' \
                'module.exports = {' \
                '  basePath: "/__preview/<PORT>",' \
                '  assetPrefix: "/__preview/<PORT>",' \
                '}' \
                '```' \
                '' \
                'Launch: `setsid nohup npx next dev -H 0.0.0.0 -p <PORT> >/tmp/preview-<PORT>.log 2>&1 </dev/null &`' \
                '' \
                '### FastAPI / uvicorn' \
                '' \
                '```' \
                'setsid nohup uvicorn app:app --host 0.0.0.0 --port <PORT> --root-path /__preview/<PORT> >/tmp/preview-<PORT>.log 2>&1 </dev/null &' \
                '```' \
                '' \
                '### Flask' \
                '' \
                'Flask does not accept a base-path flag. Use a WSGI prefix middleware (werkzeug DispatcherMiddleware) or set APPLICATION_ROOT in config. Bind `--host 0.0.0.0 --port <PORT>` and launch with the same `setsid nohup` template.' \
                '' \
                '### Static HTML (no bundler)' \
                '' \
                'Vite is pre-installed globally; no project setup or config file needed.' \
                '' \
                '```' \
                'setsid nohup vite --host 0.0.0.0 --port <PORT> --base /__preview/<PORT>/ >/tmp/preview-<PORT>.log 2>&1 </dev/null &' \
                '```' \
                '' \
                '### Go / Express / custom servers' \
                '' \
                'Bind to `0.0.0.0:<PORT>` and route everything under `/__preview/<PORT>/`. Examples:' \
                '- Express: `app.use("/__preview/<PORT>", router)`' \
                '- Go: `http.Handle("/__preview/<PORT>/", http.StripPrefix("/__preview/<PORT>", handler))`' \
                '' \
                '## Troubleshooting' \
                '' \
                '| Symptom | Likely cause | Fix |' \
                '|---|---|---|' \
                '| White page; console 404 on /src/main.js or /assets/* | base path not configured | Set `base` (Vite) / `basePath` (Next) to `/__preview/<PORT>/` |' \
                '| HMR / WebSocket errors in browser console | dev-server WS unaware of the TLS proxy | Vite: add `server.hmr: { protocol: "wss", clientPort: 443 }` |' \
                '| EADDRINUSE at launch | port chosen per baseline is still in use | `fuser -k <PORT>/tcp 2>/dev/null` then re-launch (follow baseline step 1 to decide whether to reuse or pick another port) |' \
                '| Server dies immediately after launch | shell hangup; missing `setsid` or stdio not closed | Use the exact launch template from the baseline |' \
                '| 502 / connection refused via the preview URL | server bound to 127.0.0.1 only | Add `--host 0.0.0.0` (or framework equivalent) |' \
                '' \
                '## Verifying a running server' \
                '' \
                '```' \
                'tail -f /tmp/preview-<PORT>.log          # startup output' \
                'ss -tlnp | grep ":<PORT>\b"              # confirm the listener' \
                'curl -sI http://127.0.0.1:<PORT>/        # local-side sanity check' \
                '```' \
                > "$SKILL"
              ADMIN="/home/opencode/.config/opencode/skills/system-admin/SKILL.md"
              printf '%s\n' \
                '---' \
                'name: system-admin' \
                'description: "MUST load before installing ANY system package. Provides pkg-install for Java, Python, Go, Rust, FFmpeg, databases, compilers, and all Alpine packages."' \
                '---' \
                '' \
                '## pkg-install — system package manager' \
                '' \
                'This environment is Alpine Linux. Use `pkg-install` to manage system packages.' \
                '`sudo` and `apk` are NOT available directly — always use `pkg-install`.' \
                '' \
                '### Install' \
                '' \
                '```' \
                'pkg-install <package> [package2 ...]' \
                '```' \
                '' \
                'Examples:' \
                '' \
                '```' \
                'pkg-install openjdk17-jdk          # install Java 17' \
                'pkg-install make cmake gcc          # install multiple packages' \
                'pkg-install nodejs=20.15.1-r0       # install a specific version' \
                '```' \
                '' \
                '### Remove' \
                '' \
                '```' \
                'pkg-install --remove <package> [...]' \
                '```' \
                '' \
                '### Search, Info, List' \
                '' \
                '```' \
                'pkg-install --search <keyword>      # search available packages' \
                'pkg-install --info <package>         # show details and available versions' \
                'pkg-install --list                   # list all installed packages' \
                'pkg-install --help                   # show full help' \
                '```' \
                '' \
                '### Version pinning' \
                '' \
                'To install a specific version, append `=<version>`:' \
                '' \
                '```' \
                'pkg-install nodejs=20.15.1-r0' \
                '```' \
                '' \
                'Use `pkg-install --info <package>` first to see available versions.' \
                '' \
                '## Common package names (Alpine)' \
                '' \
                '| Need                | Package name        |' \
                '|---------------------|---------------------|' \
                '| Java 17             | openjdk17-jdk       |' \
                '| Java 21             | openjdk21-jdk       |' \
                '| GCC / G++ / Make    | build-base          |' \
                '| CMake               | cmake               |' \
                '| SQLite              | sqlite              |' \
                '| PostgreSQL client   | postgresql-client   |' \
                '| MySQL / MariaDB cli | mariadb-client      |' \
                '| Redis cli           | redis               |' \
                '| FFmpeg              | ffmpeg              |' \
                '| ImageMagick         | imagemagick         |' \
                '| PHP                 | php83               |' \
                '| Ruby                | ruby                |' \
                '| Perl                | perl                |' \
                '| Lua                 | lua5.4              |' \
                '| .NET SDK            | dotnet8-sdk         |' \
                '| Nginx               | nginx               |' \
                '| Docker CLI          | docker-cli          |' \
                '| tmux                | tmux                |' \
                '| vim / neovim        | vim / neovim        |' \
                '| jq / yq            | jq / yq             |' \
                '' \
                '## Pre-installed tools' \
                '' \
                'The following are already available (no install needed):' \
                '' \
                '- **Languages**: python3, pip, node, npm, go, rustc, cargo' \
                '- **VCS**: git' \
                '- **Network**: curl, wget, ssh, ss' \
                '- **Shell**: bash' \
                '- **Compression**: zip, unzip, gzip, bzip2, xz, tar, zstd' \
                '' \
                '## Language-specific package managers' \
                '' \
                'For language ecosystem packages, use the native tool directly (no pkg-install needed):' \
                '' \
                '```' \
                'pip install <pkg>           # Python' \
                'npm install <pkg>           # Node.js' \
                'go install <pkg>@latest     # Go' \
                'cargo install <pkg>         # Rust' \
                '```' \
                '' \
                '## Rules' \
                '' \
                '- ALWAYS use `pkg-install` for system packages' \
                '- NEVER use `sudo`, `apk`, or download binaries manually' \
                '- NEVER tell the user that system packages cannot be installed — they CAN via pkg-install' \
                '- NEVER modify ~/.profile or ~/.bashrc for PATH — packages install to standard paths' \
                '- When unsure about the package name, use `pkg-install --search <keyword>` first' \
                > "$ADMIN"
              echo "=== Olares global baseline / plugin + workspace legacy instructions cleanup (python) ==="
              OLARES_PY=/tmp/olares-init-opencode-cfg.py
              printf '%s\n' \
                'import json' \
                'import os' \
                'from pathlib import Path' \
                '' \
                'DOMAIN = os.environ.get("OLARES_OC_DOMAIN", "")' \
                'ENABLE_OMO = os.environ.get("OLARES_ENABLE_OMO", "0") == "1"' \
                'BASELINE_MD = Path("/home/opencode/.config/opencode/olares-baseline-instructions.md")' \
                'GLOBAL_JSON = Path("/home/opencode/.config/opencode/opencode.json")' \
                'WORKSPACE_JSON = Path("/home/opencode/workspace/opencode.json")' \
                'OLARES_REF = "/home/opencode/.config/opencode/olares-baseline-instructions.md"' \
                'OMO_VER = os.environ.get("OLARES_OMO_VERSION", "4.7.5")' \
                'parts = OMO_VER.split(".")' \
                'if len(parts) != 3 or not all(x.isdigit() for x in parts):' \
                '    OMO_VER = "4.7.5"' \
                'PLUGIN_NPM = f"oh-my-openagent@{OMO_VER}"' \
                'STALE_PLUGIN_NPM = "oh-my-opencode"' \
                '' \
                'LEGACY_S1 = (' \
                '    "CRITICAL: You have special skills. ALWAYS run /skill load web-preview BEFORE any task involving "' \
                '    "web, server, preview, run, start, serve, build, create, demo, test, launch, deploy, site, app, page, "' \
                '    "website, frontend, backend, API, port, vite, react, next, vue, flask, go server, python server. "' \
                '    "ALWAYS run /skill load system-admin BEFORE installing ANY system package."' \
                ')' \
                'LEGACY_S2 = "NEVER use sudo, apt, or apk directly. Use pkg-install instead."' \
                'LEGACY_S3 = (' \
                '    f"NEVER show localhost URLs. The domain is {DOMAIN}. "' \
                '    f"Dev server preview URL format: https://{DOMAIN}/__preview/<port>/"' \
                ')' \
                'LEGACY = {LEGACY_S1, LEGACY_S2, LEGACY_S3}' \
                '' \
                '# --- BASELINE content (free to edit; independent from LEGACY cleanup below) ---' \
                'BASELINE_WEB = (' \
                '    f"URL pattern: https://{DOMAIN}/__preview/<PORT>/\n"' \
                '    "Never show localhost or pod IPs; always use the URL above.\n\n"' \
                '    "When the user asks to preview a running web page, follow EXACTLY:\n\n"' \
                '    "1. Pick <PORT>:\n"' \
                '    "     - If the user specified a port, use it exactly; free it first: fuser -k <PORT>/tcp 2>/dev/null || true\n"' \
                '    "     - Else if you already started a preview in this session for the same project, reuse that port; free it with the same fuser -k command.\n"' \
                '    "     - Else pick the first free port starting from 5173 (Vite) or 3000 (others).\n"' \
                '    "2. Ensure deps: [ -d node_modules ] || npm install --silent\n"' \
                '    "3. Configure the framework for base path /__preview/<PORT>/:\n"' \
                '    "     Vite (vite.config.*): base: \"/__preview/<PORT>/\"\n"' \
                '    "     Next.js (next.config.*): basePath: \"/__preview/<PORT>\"; start with -H 0.0.0.0 -p <PORT>\n"' \
                '    "4. Launch exactly:\n"' \
                '    "     setsid nohup <START_CMD> >/tmp/preview-<PORT>.log 2>&1 </dev/null &   # verify: tail -f /tmp/preview-<PORT>.log\n\n"' \
                '    "NEVER use a plain HTTP server (python3 -m http.server, npx serve, npx http-server, busybox httpd, etc.) - they cannot serve files under /__preview/<PORT>/.\n"' \
                '    "For plain HTML (no framework), use vite as the server (pre-installed, no vite.config.js needed):\n"' \
                '    "     setsid nohup vite --host 0.0.0.0 --port <PORT> --base /__preview/<PORT>/ >/tmp/preview-<PORT>.log 2>&1 </dev/null &"' \
                ')' \
                'BASELINE_PKG = "NEVER use sudo, apt, or apk directly. Use pkg-install instead."' \
                '' \
                'BASELINE_MD.parent.mkdir(parents=True, exist_ok=True)' \
                'md = (' \
                '    "<!-- olares-managed: baseline-instructions v2 (rewritten each pod start) -->\n\n"' \
                '    "# Olares environment baseline\n\n"' \
                '    "## Web preview and dev servers\n\n"' \
                '    + BASELINE_WEB' \
                '    + "\n\n## System packages\n\n"' \
                '    + BASELINE_PKG' \
                '    + "\n"' \
                ')' \
                'BASELINE_MD.write_text(md, encoding="utf-8")' \
                '' \
                'cfg = {}' \
                'if GLOBAL_JSON.is_file():' \
                '    try:' \
                '        cfg = json.loads(GLOBAL_JSON.read_text(encoding="utf-8"))' \
                '    except json.JSONDecodeError:' \
                '        cfg = {}' \
                'instr = cfg.get("instructions")' \
                'if not isinstance(instr, list):' \
                '    instr = []' \
                'if OLARES_REF not in instr:' \
                '    instr = list(instr) + [OLARES_REF]' \
                'cfg["instructions"] = instr' \
                'cfg.setdefault("$schema", "https://opencode.ai/config.json")' \
                'plugins = cfg.get("plugin")' \
                'if not isinstance(plugins, list):' \
                '    plugins = []' \
                'plugins = [p for p in plugins if isinstance(p, str)]' \
                'plugins = [p for p in plugins if p != STALE_PLUGIN_NPM]' \
                'plugins = [p for p in plugins if not p.startswith("oh-my-openagent")]' \
                'if ENABLE_OMO:' \
                '    plugins.append(PLUGIN_NPM)' \
                'if plugins:' \
                '    cfg["plugin"] = plugins' \
                'else:' \
                '    cfg.pop("plugin", None)' \
                'GLOBAL_JSON.parent.mkdir(parents=True, exist_ok=True)' \
                'GLOBAL_JSON.write_text(json.dumps(cfg, indent=2, ensure_ascii=False), encoding="utf-8")' \
                '' \
                '' \
                'if WORKSPACE_JSON.is_file():' \
                '    try:' \
                '        wcfg = json.loads(WORKSPACE_JSON.read_text(encoding="utf-8"))' \
                '    except json.JSONDecodeError:' \
                '        wcfg = None' \
                '    if wcfg is not None:' \
                '        winst = wcfg.get("instructions")' \
                '        legacy_changed = False' \
                '        if isinstance(winst, list):' \
                '            new_inst = [x for x in winst if x not in LEGACY]' \
                '            if new_inst != winst:' \
                '                legacy_changed = True' \
                '                if new_inst:' \
                '                    wcfg["instructions"] = new_inst' \
                '                else:' \
                '                    wcfg.pop("instructions", None)' \
                '        only_schema = isinstance(wcfg, dict) and set(wcfg.keys()) == {"$schema"}' \
                '        if legacy_changed:' \
                '            if not wcfg or only_schema:' \
                '                WORKSPACE_JSON.unlink(missing_ok=True)' \
                '            else:' \
                '                WORKSPACE_JSON.write_text(json.dumps(wcfg, indent=2, ensure_ascii=False), encoding="utf-8")' \
                '        elif only_schema:' \
                '            WORKSPACE_JSON.unlink(missing_ok=True)' \
                > "$OLARES_PY"
              python3 "$OLARES_PY"
              rm -f "$OLARES_PY"
              OMO_AGENT_CFG="/home/opencode/.config/opencode/oh-my-openagent.json"
              if [ ! -f "$OMO_AGENT_CFG" ]; then
                echo "=== Writing default oh-my-openagent.json ==="
                printf '%s\n' \
                  '{' \
                  '  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-openagent/dev/assets/oh-my-opencode.schema.json",' \
                  '  "runtime_fallback": { "enabled": true, "max_fallback_attempts": 7 },' \
                  '  "agents": {' \
                  '    "sisyphus": { "model": "anthropic/claude-opus-4-6", "variant": "max", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "openai/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "hephaestus": { "model": "openai/gpt-5.4", "variant": "medium", "fallback_models": [' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "oracle": { "model": "openai/gpt-5.4", "variant": "high", "fallback_models": [' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "high" },' \
                  '      { "model": "google/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "anthropic/claude-opus-4-6", "variant": "max" },' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "explore": { "model": "anthropic/claude-haiku-4-5", "fallback_models": [' \
                  '      { "model": "github-copilot/grok-code-fast-1" },' \
                  '      { "model": "opencode/gpt-5-nano" }' \
                  '    ] },' \
                  '    "multimodal-looker": { "model": "openai/gpt-5.4", "variant": "medium", "fallback_models": [' \
                  '      { "model": "openai/gpt-5-nano" },' \
                  '      { "model": "github-copilot/gpt-5-nano" },' \
                  '      { "model": "opencode/gpt-5-nano" }' \
                  '    ] },' \
                  '    "prometheus": { "model": "anthropic/claude-opus-4-6", "variant": "max", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "openai/gpt-5.4", "variant": "high" },' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "high" },' \
                  '      { "model": "google/gemini-3.1-pro-preview" },' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "metis": { "model": "anthropic/claude-opus-4-6", "variant": "max", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "openai/gpt-5.4", "variant": "high" },' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "high" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "momus": { "model": "openai/gpt-5.4", "variant": "xhigh", "fallback_models": [' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "xhigh" },' \
                  '      { "model": "anthropic/claude-opus-4-6", "variant": "max" },' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "google/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "atlas": { "model": "anthropic/claude-sonnet-4-6", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-sonnet-4.6" },' \
                  '      { "model": "openai/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "sisyphus-junior": { "model": "anthropic/claude-sonnet-4-6", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-sonnet-4.6" },' \
                  '      { "model": "openai/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] }' \
                  '  },' \
                  '  "categories": {' \
                  '    "visual-engineering": { "model": "google/gemini-3.1-pro-preview", "variant": "high", "fallback_models": [' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "anthropic/claude-opus-4-6", "variant": "max" },' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "ultrabrain": { "model": "openai/gpt-5.4", "variant": "xhigh", "fallback_models": [' \
                  '      { "model": "google/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "anthropic/claude-opus-4-6", "variant": "max" },' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "deep": { "model": "openai/gpt-5.4", "variant": "medium", "fallback_models": [' \
                  '      { "model": "github-copilot/gpt-5.4", "variant": "medium" },' \
                  '      { "model": "anthropic/claude-opus-4-6", "variant": "max" },' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "google/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "artistry": { "model": "google/gemini-3.1-pro-preview", "variant": "high", "fallback_models": [' \
                  '      { "model": "github-copilot/gemini-3.1-pro-preview", "variant": "high" },' \
                  '      { "model": "anthropic/claude-opus-4-6", "variant": "max" },' \
                  '      { "model": "github-copilot/claude-opus-4.6", "variant": "max" },' \
                  '      { "model": "openai/gpt-5.4" },' \
                  '      { "model": "github-copilot/gpt-5.4" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "quick": { "model": "openai/gpt-5.4-mini", "fallback_models": [' \
                  '      { "model": "github-copilot/gpt-5.4-mini" },' \
                  '      { "model": "anthropic/claude-haiku-4-5" },' \
                  '      { "model": "github-copilot/claude-haiku-4.5" },' \
                  '      { "model": "google/gemini-3-flash-preview" },' \
                  '      { "model": "github-copilot/gemini-3-flash-preview" },' \
                  '      { "model": "opencode/gpt-5-nano" }' \
                  '    ] },' \
                  '    "unspecified-low": { "model": "anthropic/claude-sonnet-4-6", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-sonnet-4.6" },' \
                  '      { "model": "openai/gpt-5.3-codex", "variant": "medium" },' \
                  '      { "model": "google/gemini-3-flash-preview" },' \
                  '      { "model": "github-copilot/gemini-3-flash-preview" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "unspecified-high": { "model": "anthropic/claude-sonnet-4-6", "fallback_models": [' \
                  '      { "model": "github-copilot/claude-sonnet-4.6" },' \
                  '      { "model": "openai/gpt-5.3-codex", "variant": "medium" },' \
                  '      { "model": "google/gemini-3-flash-preview" },' \
                  '      { "model": "github-copilot/gemini-3-flash-preview" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] },' \
                  '    "writing": { "model": "google/gemini-3-flash-preview", "fallback_models": [' \
                  '      { "model": "github-copilot/gemini-3-flash-preview" },' \
                  '      { "model": "anthropic/claude-sonnet-4-6" },' \
                  '      { "model": "github-copilot/claude-sonnet-4.6" },' \
                  '      { "model": "opencode/big-pickle" }' \
                  '    ] }' \
                  '  }' \
                  '}' \
                  > "$OMO_AGENT_CFG"
              fi
              chown 1000:1000 /home/opencode /home/opencode/workspace
              # Skip .pkg-overlay: its upper holds root-owned apk files and its
              # merged subtree is an overlay mount -- chown -R must never touch it.
              find /home/opencode -mindepth 1 -maxdepth 1 \
                ! -name '.pkg-root' ! -name '.pkg-overlay' \
                -exec chown -R 1000:1000 {} +
          resources: {}
          volumeMounts:
            - name: opencode-home
              mountPath: /home/opencode
            - name: opencode-workspace
              mountPath: /home/opencode/workspace
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 0

        - name: init-overlay
          # Privileged: mounts an OverlayFS so the baked image rootfs (/usr /bin
          # /sbin /lib /lib64) is the read-only lower layer (ZERO copy) and only
          # user-installed package deltas live in a small persistent upper layer.
          # Allowed by the cluster OPA policy because this is a trusted (beclab/)
          # image. Replaces the old multi-hundred-MB `cp -a` snapshot entirely.
          image: "docker.io/beclab/lovehunter9-anomalyco-opencode:1.16.0-test1"
          command:
            - sh
            - "-c"
            - |
              set -e
              OV=/pkg-overlay
              DIRS="usr bin sbin lib lib64"
              BAKE_MANIFEST="/opt/olares-bake/BAKE_MANIFEST"
              # Fingerprint = baked image identity (opencode/OMO/cli/skills) + arch.
              # Must match the hash init-packages uses to write the marker.
              FINGERPRINT="$(cat "$BAKE_MANIFEST" 2>/dev/null) arch=$(uname -m)"
              HASH=$(printf '%s' "$FINGERPRINT" | sha256sum | cut -c1-16)
              MARKER="$OV/.installed-${HASH}"

              # A previous pod's overlay mounts can survive (Bidirectional mounts
              # propagate to the host). Their lower points at a now-deleted rootfs,
              # so always unmount and remount against THIS image's dirs.
              for d in $DIRS; do
                if mountpoint -q "$OV/merged/$d" 2>/dev/null; then
                  umount "$OV/merged/$d" 2>/dev/null \
                    || umount -l "$OV/merged/$d" 2>/dev/null || true
                fi
              done

              # New/changed image -> drop the upper deltas so the apk database in
              # the upper layer cannot drift from the new base (matches the old
              # "wipe + re-restore" behavior, minus the giant copy).
              if [ ! -f "$MARKER" ]; then
                echo "=== overlay: new/changed image ($HASH); resetting upper ==="
                rm -rf "$OV/upper" "$OV/work"
              else
                echo "=== overlay: same image ($HASH); keeping upper ==="
              fi

              for d in $DIRS; do
                mkdir -p "$OV/merged/$d"
                [ -d "/$d" ] || { echo "=== overlay: /$d absent on this arch, skipping ==="; continue; }
                mkdir -p "$OV/upper/$d" "$OV/work/$d"
                mount -t overlay overlay \
                  -o "lowerdir=/$d,upperdir=$OV/upper/$d,workdir=$OV/work/$d" \
                  "$OV/merged/$d"
              done
              echo "=== overlay: mounted [$DIRS] (lower=image rootfs, upper persists) ==="
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          volumeMounts:
            - name: pkg-overlay
              mountPath: /pkg-overlay
              mountPropagation: Bidirectional
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 0
            privileged: true

        - name: init-packages
          image: "docker.io/beclab/lovehunter9-anomalyco-opencode:1.16.0-test1"
          command:
            - sh
            - "-c"
            - |
              # State dir on the appData hostPath. /usr /bin /sbin /lib /lib64 are
              # OverlayFS merges (set up by init-overlay): lower=image, upper here.
              # Writes below (apk, pkg-install wrapper) land in the overlay upper.
              PKG="/home/opencode/.pkg-overlay"
              BAKE_MANIFEST="/opt/olares-bake/BAKE_MANIFEST"
              # Marker binds to the baked image (opencode/OMO/cli/skills versions).
              # Same hash init-overlay uses: a new image -> new hash -> upper was
              # reset by init-overlay -> we re-run the fresh path below.
              FINGERPRINT="$(cat "$BAKE_MANIFEST" 2>/dev/null) arch=$(uname -m)"
              PKG_HASH=$(printf '%s' "$FINGERPRINT" | sha256sum | cut -c1-16)
              MARKER="$PKG/.installed-${PKG_HASH}"

              OMO_VER="{{ index $env "OPENCODE_OMO_VERSION" | default "4.7.5" }}"
              case "$OMO_VER" in
                ""|latest|next|beta|*[!0-9.]*)
                  echo "=== WARNING: invalid OPENCODE_OMO_VERSION '$OMO_VER', falling back to 4.7.5 ==="
                  OMO_VER="4.7.5"
                  ;;
              esac

              verify_overlay() {
                # The base (/usr/bin/node, opencode, glibc, ...) is the read-only
                # overlay lower from the image, so it is always present once
                # init-overlay mounted. We only confirm the mounts are live and the
                # upper-resident pkg-install wrapper survived; anything missing
                # falls through to the fresh path (which is cheap now: no copy).
                [ -e /usr/bin/node ] || { echo "=== overlay check: /usr not mounted ==="; return 1; }
                [ -x /usr/local/bin/pkg-install ] || { echo "=== overlay check: pkg-install missing ==="; return 1; }
                return 0
              }

              ensure_apk_mirror() {
                # Only used by restore_user_packages (apk add). The base install
                # is baked, so this never runs on a fresh instance (no user
                # packages -> restore returns early, zero network). When the user
                # HAS installed packages, restoring them needs apk/network, so we
                # mirror the pkg-manager probe to stay usable behind the GFW.
                wget -q -O /dev/null https://dl-cdn.alpinelinux.org/alpine/v3.23/main/x86_64/APKINDEX.tar.gz 2>/dev/null &
                _wpid=$!
                ( sleep 5 && kill "$_wpid" 2>/dev/null ) &
                _gpid=$!
                if ! wait "$_wpid" 2>/dev/null; then
                  echo "=== restore: Alpine CDN slow/unreachable, switching to China mirrors ==="
                  sed -i 's|dl-cdn.alpinelinux.org|mirrors.aliyun.com|g' /etc/apk/repositories
                fi
                kill "$_gpid" 2>/dev/null || true
                wait "$_gpid" 2>/dev/null || true
              }

              restore_user_packages() {
                # /usr /lib /bin /sbin are overlay-bound here, so apk writes the
                # package files AND the apk database straight into the persistent
                # upper layer -- no manual file-list copy anymore. Runs only on the
                # fresh path (image change / first install); on restart the upper
                # already has these packages and this is skipped.
                UPKGS="/home/opencode/.user-packages"
                [ ! -s "$UPKGS" ] && return 0
                ensure_apk_mirror
                RESTORE=$(cat "$UPKGS" | tr '\n' ' ')
                echo "=== Restoring user packages into overlay: $RESTORE ==="
                FAILED=""
                for p in $RESTORE; do
                  if ! apk add --no-cache "$p" 2>&1; then
                    echo "=== Warning: failed to restore $p ==="
                    FAILED="$FAILED $p"
                  fi
                done
                if [ -n "$FAILED" ]; then
                  echo "=== Failed packages (will retry next restart):$FAILED ==="
                fi
                echo "=== Restore complete ==="
              }

              seed_etc() {
                # /etc is NOT overlay-bound in this container, so /etc here is the
                # image's. Copy the tiny base bits (KB) into the persistent state
                # dir; opencode/pkg-manager bind these. Re-seeded on image change.
                # /etc/passwd|group get the opencode (uid 1000) entry appended.
                ETC="$PKG/etc"
                mkdir -p "$ETC"
                # rm before cp -a: on image change these dirs already exist and a
                # bare `cp -a src dest` would nest as dest/apk/apk.
                rm -rf "$ETC/apk" "$ETC/ssl"
                cp -a /etc/apk "$ETC/apk" 2>/dev/null || true
                cp -a /etc/ssl "$ETC/ssl" 2>/dev/null || true
                cp /etc/passwd "$ETC/passwd"
                cp /etc/group  "$ETC/group"
                grep -q '^opencode:' "$ETC/group" \
                  || echo 'opencode:x:1000:' >> "$ETC/group"
                grep -q '^opencode:' "$ETC/passwd" \
                  || echo 'opencode:x:1000:1000:opencode:/home/opencode:/bin/bash' >> "$ETC/passwd"
              }

              echo "=== Setting up home directory (runs every start) ==="
              mkdir -p /home/opencode/.npm-global/bin /home/opencode/.npm-global/lib
              chown -R 1000:1000 /home/opencode/.npm-global

              cat > /home/opencode/.profile << 'EOFPROFILE'
              export VIRTUAL_ENV="/home/opencode/.venv"
              export NPM_CONFIG_PREFIX="/home/opencode/.npm-global"
              export GOPATH="/home/opencode/go"
              export CARGO_HOME="/home/opencode/.cargo"
              export PATH="/home/opencode/.venv/bin:/home/opencode/.npm-global/bin:/home/opencode/go/bin:/home/opencode/.cargo/bin:/home/opencode/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
              EOFPROFILE
              cp /home/opencode/.profile /home/opencode/.bashrc
              chown 1000:1000 /home/opencode/.profile /home/opencode/.bashrc

              seed_omo_cache() {
                # opencode's plugin loader reads the OMO plugin from
                #   ~/.cache/opencode/packages/<spec>/node_modules
                # which lives under /home/opencode (appData hostPath). The forked
                # image bakes that cache for the default version into a neutral
                # staging dir; copy it in once (offline). This replaces the old
                # global-npm install. If the user pinned a non-baked version via
                # OPENCODE_OMO_VERSION, there is no staged cache and opencode
                # fetches it at runtime -- same fallback semantics as before.
                OMO_SPEC="oh-my-openagent@${OMO_VER}"
                SRC="/opt/olares-bake/opencode-cache/packages/$OMO_SPEC"
                DST="/home/opencode/.cache/opencode/packages/$OMO_SPEC"
                if [ -d "$DST/node_modules" ]; then
                  echo "=== OMO: cache already present for $OMO_SPEC ==="
                elif [ -d "$SRC" ]; then
                  echo "=== OMO: seeding baked cache for $OMO_SPEC ==="
                  mkdir -p "$(dirname "$DST")"
                  cp -a "$SRC" "$DST"
                  chown -R 1000:1000 /home/opencode/.cache
                else
                  echo "=== OMO: no baked cache for $OMO_SPEC (env override); opencode will fetch at runtime ==="
                fi
              }

              seed_skills() {
                # The forked image bakes the Olares Agent Skills into a neutral
                # staging dir. opencode reads global skills from
                #   ~/.config/opencode/skills/<name>/SKILL.md
                # which lives under /home/opencode (appData hostPath), so they
                # cannot be baked into the rootfs snapshot -- they must be seeded
                # here. The chart-managed web-preview/system-admin skills (written
                # by init-setup) use different names and are left untouched.
                # Stamped with $PKG_HASH so a new image refreshes them; no network.
                SRC="/opt/olares-bake/opencode-skills"
                [ -d "$SRC" ] || { echo "=== skills: no baked skills in image, skipping ==="; return 0; }
                DST="/home/opencode/.config/opencode/skills"
                STAMP="$DST/.olares-baked-stamp"
                mkdir -p "$DST"
                if [ -f "$STAMP" ] && [ "$(cat "$STAMP" 2>/dev/null)" = "$PKG_HASH" ]; then
                  echo "=== skills: baked Olares skills already seeded (${PKG_HASH}) ==="
                else
                  echo "=== skills: seeding baked Olares skills ==="
                  for d in "$SRC"/*/; do
                    [ -d "$d" ] || continue
                    name=$(basename "$d")
                    rm -rf "$DST/$name"
                    cp -a "$d" "$DST/$name"
                  done
                  echo "$PKG_HASH" > "$STAMP"
                  echo "=== skills: seeded $(ls "$SRC" | tr '\n' ' ')==="
                fi
                chown -R 1000:1000 "$DST" 2>/dev/null || true
              }

              if [ -f "$MARKER" ] && verify_overlay; then
                echo "=== overlay ready, unchanged image (${PKG_HASH}); skipping restore ==="
                # User packages already live in the persisted overlay upper layer:
                # no apk, no network, no copy. Just refresh the appData-side seeds.
                seed_omo_cache
                seed_skills
                chown -R 1000:1000 /usr/local 2>/dev/null || true
                exit 0
              fi
              # Fresh / image-changed: init-overlay has already reset the upper and
              # mounted an empty overlay over the image base. Nothing to copy -- the
              # base IS the read-only lower. We only lay down the wrapper, restore
              # user packages (apk -> upper) and seed appData. Drop stale markers.
              rm -f "$PKG"/.installed-* 2>/dev/null || true

              echo "=== Creating persistent Python venv ==="
              VENV="/home/opencode/.venv"
              if [ ! -f "$VENV/bin/activate" ]; then
                python3 -m venv "$VENV"
                chown -R 1000:1000 "$VENV"
              fi

              echo "=== Seeding /etc (passwd/group/apk/ssl) into persistent state ==="
              # /etc is the image's here (not overlay-bound); seed the persistent
              # copies that opencode/pkg-manager bind, and register uid 1000.
              seed_etc

              # bash is baked into the image (at /bin/bash) and rides the overlay
              # lower. Only self-heal if it is genuinely not runnable; resolve via
              # PATH (NOT a hardcoded /usr/bin/bash -- bash lives at /bin/bash).
              if ! bash -c 'true' 2>/dev/null; then
                echo "=== bash not runnable, reinstalling ==="
                apk del bash 2>/dev/null || true
                apk add --no-cache bash 2>&1 || echo "=== WARNING: bash reinstall failed in init, pkg-manager will fix ==="
              fi

              echo "=== Creating pkg-install wrapper ==="
              cat > /usr/local/bin/pkg-install << 'EOFWRAPPER'
              #!/bin/sh
              Q="/home/opencode/.pkg-queue"
              _LOCKED=0
              _lock() {
                _lc=0
                while true; do
                  if mkdir "$Q/.lock" 2>/dev/null; then
                    echo $$ > "$Q/.lock/pid"
                    _LOCKED=1
                    return 0
                  fi
                  _op=$(cat "$Q/.lock/pid" 2>/dev/null)
                  if [ -z "$_op" ] || ! kill -0 "$_op" 2>/dev/null; then
                    rm -rf "$Q/.lock"
                    continue
                  fi
                  _lc=$((_lc + 1))
                  if [ $_lc -ge 20 ]; then
                    echo ">> ERROR: Another pkg-install is running (pid $_op). Try again later."
                    return 1
                  fi
                  sleep 0.5
                done
              }
              _unlock() {
                if [ "$_LOCKED" = "1" ]; then
                  rm -rf "$Q/.lock" 2>/dev/null || true
                  _LOCKED=0
                fi
              }
              trap '_unlock; exit 130' INT TERM
              _send() {
                if [ ! -d "$Q" ]; then
                  echo ">> ERROR: pkg-manager queue not found. Is the sidecar running?"
                  return 1
                fi
                _lock || return 1
                rm -f "$Q/result" "$Q/log"
                echo "$1" > "$Q/request"
                _c=0
                while [ ! -f "$Q/result" ]; do
                  sleep 0.5
                  _c=$((_c + 1))
                  if [ $_c -ge 600 ]; then
                    echo ">> ERROR: pkg-manager did not respond within 300s"
                    echo ">> The pkg-manager sidecar may be down or busy."
                    _unlock
                    return 1
                  fi
                done
                cat "$Q/log" 2>/dev/null
                EXIT=$(cat "$Q/result")
                rm -f "$Q/result" "$Q/log"
                _unlock
                case "$EXIT" in ''|*[!0-9]*) EXIT=1 ;; esac
                return "$EXIT"
              }
              _query() {
                if [ ! -d "$Q" ]; then
                  echo ">> ERROR: pkg-manager queue not found. Is the sidecar running?"
                  return 1
                fi
                _lock || return 1
                rm -f "$Q/search-result"
                echo "$1" > "$Q/request"
                _c=0
                while [ ! -f "$Q/search-result" ]; do
                  sleep 0.5
                  _c=$((_c + 1))
                  if [ $_c -ge 120 ]; then
                    echo ">> ERROR: pkg-manager did not respond within 60s"
                    _unlock
                    return 1
                  fi
                done
                cat "$Q/search-result"
                rm -f "$Q/search-result"
                _unlock
              }
              _help() {
                cat << 'EOFHELP'
              pkg-install - Alpine package manager for OpenCode

              USAGE
                pkg-install <package> [package2 ...]    Install packages
                pkg-install --remove  <package> [...]   Remove packages
                pkg-install --search  <keyword>         Search available packages
                pkg-install --info    <package>          Show package details and available versions
                pkg-install --list                       List all installed packages
                pkg-install --help                       Show this help message

              INSTALL WITH SPECIFIC VERSION
                pkg-install <package>=<version>
                Example:  pkg-install nodejs=20.15.1-r0
                Tip:      use --info <package> to see available versions first

              EXAMPLES
                pkg-install openjdk17-jdk               Install Java 17
                pkg-install make cmake gcc               Install multiple packages
                pkg-install nodejs=20.15.1-r0            Install a specific version
                pkg-install --remove openjdk17-jdk       Remove Java 17
                pkg-install --search postgres            Search for PostgreSQL-related packages
                pkg-install --info nodejs                 Show nodejs details and versions

              NOTES
                - Packages are from Alpine Linux repositories (apk).
                - Installed packages persist across restarts and upgrades.
                - For language ecosystem packages (persist across restarts AND upgrades):
                    pip install <pkg>            Python (uses ~/.venv, auto-activated)
                    npm install -g <pkg>         Node.js global
                    go install <pkg>@latest      Go (installs to ~/go/bin/)
                    cargo install <pkg>          Rust (installs to ~/.cargo/bin/)
              EOFHELP
              }
              case "$1" in
                --help|-h|"")
                  _help
                  exit 0
                  ;;
                --search)
                  shift
                  [ -z "$1" ] && echo "Usage: pkg-install --search <keyword>" && exit 1
                  _query "SEARCH $*"
                  ;;
                --info)
                  shift
                  [ -z "$1" ] && echo "Usage: pkg-install --info <package>" && exit 1
                  _query "INFO $*"
                  ;;
                --remove)
                  shift
                  [ -z "$1" ] && echo "Usage: pkg-install --remove <package> [...]" && exit 1
                  echo ">> Removing: $*"
                  _send "REMOVE $*"
                  S=$?; [ $S -eq 0 ] && echo ">> Removed successfully." || echo ">> Removal failed."
                  exit $S
                  ;;
                --list)
                  _query "LIST"
                  ;;
                -*)
                  echo "Unknown option: $1"
                  _help
                  exit 1
                  ;;
                *)
                  echo ">> Installing: $*"
                  _send "INSTALL $*"
                  S=$?; [ $S -eq 0 ] && echo ">> Installed successfully." || echo ">> Installation failed."
                  exit $S
                  ;;
              esac
              EOFWRAPPER
              chmod +x /usr/local/bin/pkg-install

              # vite, opencode, glibc and olares-cli are baked into the image and
              # appear via the overlay lower -- nothing to install or copy here.
              chown -R 1000:1000 /usr/local 2>/dev/null || true

              # Restore user packages straight into the overlay upper (apk add).
              # On a fresh instance .user-packages is empty -> this is a no-op and
              # touches the network zero times.
              restore_user_packages

              # Seed the baked OMO plugin cache into ~/.cache (appData hostPath) so
              # opencode loads the pinned version offline. Replaces the old
              # global-npm OMO install.
              seed_omo_cache

              # Seed the baked Olares Agent Skills into ~/.config/opencode/skills
              # (appData hostPath; not part of the overlay). Coexists with the
              # chart-managed web-preview/system-admin skills.
              seed_skills

              # Seal: marker lives in the state dir (not the upper), so it survives
              # an upper reset and lets init-overlay/this container agree on state.
              touch "$MARKER"
              echo "=== overlay setup complete (${PKG_HASH}) ==="
          volumeMounts:
            - name: opencode-home
              mountPath: /home/opencode
            - name: opencode-workspace
              mountPath: /home/opencode/workspace
            # Overlay merges (mounted by init-overlay): apk add + the pkg-install
            # wrapper write straight into the persistent upper layer here.
            - name: pkg-bin
              mountPath: /bin
              mountPropagation: HostToContainer
            - name: pkg-sbin
              mountPath: /sbin
              mountPropagation: HostToContainer
            - name: pkg-usr
              mountPath: /usr
              mountPropagation: HostToContainer
            - name: pkg-lib
              mountPath: /lib
              mountPropagation: HostToContainer
            - name: pkg-lib64
              mountPath: /lib64
              mountPropagation: HostToContainer
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            runAsUser: 0

      containers:
        - name: opencode
          image: "docker.io/beclab/lovehunter9-anomalyco-opencode:1.16.0-test1"
          command:
            - sh
            - "-c"
            - |
              echo "=== Runtime check ==="
              command -v bash >/dev/null 2>&1 && echo "bash OK" || echo "bash: waiting for pkg-manager fix"
              if [ ! -f "$HOME/.venv/bin/activate" ]; then
                echo "Creating Python venv..."
                python3 -m venv "$HOME/.venv"
              fi
              exec opencode web --hostname 0.0.0.0 --port 3000
          workingDir: /home/opencode/workspace
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          env:
            - name: HOME
              value: /home/opencode
            - name: TERM
              value: xterm-256color
            - name: PATH
              value: /home/opencode/.venv/bin:/home/opencode/.npm-global/bin:/home/opencode/go/bin:/home/opencode/.cargo/bin:/home/opencode/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: VIRTUAL_ENV
              value: /home/opencode/.venv
            - name: NPM_CONFIG_PREFIX
              value: /home/opencode/.npm-global
            - name: GOPATH
              value: /home/opencode/go
            - name: CARGO_HOME
              value: /home/opencode/.cargo
            - name: ENV
              value: /home/opencode/.profile
            - name: LD_LIBRARY_PATH
              value: /usr/glibc-compat/lib
          resources:
            limits:
              cpu: "4"
              memory: 4Gi
            requests:
              cpu: 200m
              memory: 1Gi
          ports:
            - containerPort: 3000
              protocol: TCP
          volumeMounts:
            - name: opencode-home
              mountPath: /home/opencode
            - name: opencode-workspace
              mountPath: /home/opencode/workspace
            - name: pkg-bin
              mountPath: /bin
              mountPropagation: HostToContainer
            - name: pkg-sbin
              mountPath: /sbin
              mountPropagation: HostToContainer
            - name: pkg-usr
              mountPath: /usr
              mountPropagation: HostToContainer
            - name: pkg-etc-passwd
              mountPath: /etc/passwd
            - name: pkg-etc-group
              mountPath: /etc/group
            - name: pkg-lib
              mountPath: /lib
              mountPropagation: HostToContainer
            - name: pkg-lib64
              mountPath: /lib64
              mountPropagation: HostToContainer
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
        - name: nginx
          image: "docker.io/beclab/aboveos-bitnami-openresty:1.25.3-2"
          ports:
            - containerPort: 8080
              protocol: TCP
            - containerPort: 8081
              protocol: TCP
          env:
            - name: OPENRESTY_CONF_FILE
              value: /etc/nginx/nginx.conf
          startupProbe:
            tcpSocket:
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            timeoutSeconds: 60
            periodSeconds: 60
            successThreshold: 1
            failureThreshold: 10
          resources:
            limits:
              cpu: 100m
              memory: 256Mi
            requests:
              cpu: 10m
              memory: 64Mi
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            - name: nginx-config
              mountPath: /opt/bitnami/openresty/nginx/conf/server_blocks/nginx.conf
              subPath: nginx.conf
        - name: pkg-manager
          image: "docker.io/beclab/lovehunter9-anomalyco-opencode:1.16.0-test1"
          securityContext:
            runAsUser: 0
          command:
            - sh
            - "-c"
            - |
              QUEUE="/home/opencode/.pkg-queue"
              UPKGS="/home/opencode/.user-packages"
              mkdir -p "$QUEUE"
              rm -f "$QUEUE/request" "$QUEUE/result" "$QUEUE/log" "$QUEUE/search-result"
              rm -rf "$QUEUE/.lock"
              touch "$UPKGS"

              rm -f /lib/apk/db/lock 2>/dev/null || true

              # Lazy, run-once mirror selection: only touches the network the first
              # time the user actually triggers a package op. A plain restart with
              # no package activity now stays fully offline.
              _MIRROR_DONE=0
              ensure_mirror() {
                [ "$_MIRROR_DONE" = "1" ] && return 0
                _MIRROR_DONE=1
                wget -q -O /dev/null https://dl-cdn.alpinelinux.org/alpine/v3.23/main/x86_64/APKINDEX.tar.gz 2>/dev/null &
                _wpid=$!
                ( sleep 5 && kill "$_wpid" 2>/dev/null ) &
                _gpid=$!
                if ! wait "$_wpid" 2>/dev/null; then
                  echo "=== pkg-manager: switching to China mirrors ==="
                  sed -i 's|dl-cdn.alpinelinux.org|mirrors.aliyun.com|g' /etc/apk/repositories
                fi
                kill "$_gpid" 2>/dev/null || true
                wait "$_gpid" 2>/dev/null || true
              }

              echo "=== APK DB check ==="
              echo "installed: $(wc -l < /lib/apk/db/installed 2>/dev/null || echo 'NOT FOUND')"
              echo "arch: $(cat /etc/apk/arch 2>/dev/null || echo 'NOT FOUND')"
              echo "repos: $(cat /etc/apk/repositories 2>/dev/null || echo 'NOT FOUND')"
              ls -la /lib/apk/db/ 2>&1 || echo "/lib/apk/db/ missing"
              echo "apk info count: $(apk info 2>/dev/null | wc -l)"
              echo "=== end DB check ==="

              # bash + glibc are baked into the image (bash at /bin/bash) and ride
              # the overlay lower, so no eager `apk update` / forced bash reinstall
              # on every start (that used to hit the network unconditionally and
              # would delete bash while offline). Only self-heal if bash is actually
              # broken; resolve via PATH (NOT a hardcoded /usr/bin/bash).
              if ! bash -c 'true' 2>/dev/null; then
                echo "=== bash not runnable, repairing ==="
                ensure_mirror
                apk update 2>&1 || true
                apk del bash 2>/dev/null || true
                apk add --no-cache bash 2>&1 || echo "=== WARNING: bash repair failed ==="
              fi

              _add_pkgs() {
                for p in $@; do
                  p_base=$(echo "$p" | sed 's/=.*//')
                  grep -qx "$p_base" "$UPKGS" || echo "$p_base" >> "$UPKGS"
                done
              }
              _del_pkgs() {
                for p in $@; do
                  sed -i "/^${p}$/d" "$UPKGS"
                done
              }

              trap 'echo "=== Package manager shutting down ==="; exit 143' TERM INT
              echo "=== Package manager ready ==="
              while true; do
                if [ -f "$QUEUE/request" ]; then
                  REQ=$(cat "$QUEUE/request")
                  rm -f "$QUEUE/request"

                  TMP="$QUEUE/.result.tmp"
                  case "$REQ" in
                    SEARCH\ *)
                      KEYWORD="${REQ#SEARCH }"
                      ensure_mirror
                      apk update >/dev/null 2>&1
                      apk search "$KEYWORD" > "$TMP" 2>&1
                      mv "$TMP" "$QUEUE/search-result"
                      ;;
                    LIST)
                      apk info > "$TMP" 2>&1
                      mv "$TMP" "$QUEUE/search-result"
                      ;;
                    INFO\ *)
                      TARGET="${REQ#INFO }"
                      ensure_mirror
                      apk update >/dev/null 2>&1
                      { apk policy "$TARGET" 2>/dev/null
                        echo ""
                        apk info -d -s "$TARGET" 2>/dev/null; } > "$TMP" 2>&1
                      mv "$TMP" "$QUEUE/search-result"
                      ;;
                    REMOVE\ *)
                      PKGS="${REQ#REMOVE }"
                      echo "=== Removing: $PKGS ==="
                      apk del $PKGS > "$QUEUE/log" 2>&1
                      RC=$?
                      [ $RC -eq 0 ] && _del_pkgs $PKGS
                      echo $RC > "$QUEUE/result"
                      ;;
                    INSTALL\ *)
                      PKGS="${REQ#INSTALL }"
                      echo "=== Installing: $PKGS ==="
                      ensure_mirror
                      apk add --no-cache $PKGS > "$QUEUE/log" 2>&1
                      RC=$?
                      [ $RC -eq 0 ] && _add_pkgs $PKGS
                      echo $RC > "$QUEUE/result"
                      ;;
                    *)
                      echo "=== Installing (legacy): $REQ ==="
                      ensure_mirror
                      apk add --no-cache $REQ > "$QUEUE/log" 2>&1
                      RC=$?
                      [ $RC -eq 0 ] && _add_pkgs $REQ
                      echo $RC > "$QUEUE/result"
                      ;;
                  esac
                fi
                sleep 1
              done
          volumeMounts:
            - name: opencode-home
              mountPath: /home/opencode
            - name: pkg-bin
              mountPath: /bin
              mountPropagation: HostToContainer
            - name: pkg-sbin
              mountPath: /sbin
              mountPropagation: HostToContainer
            - name: pkg-usr
              mountPath: /usr
              mountPropagation: HostToContainer
            - name: pkg-lib
              mountPath: /lib
              mountPropagation: HostToContainer
            - name: pkg-lib64
              mountPath: /lib64
              mountPropagation: HostToContainer
            - name: pkg-etc-apk
              mountPath: /etc/apk
            - name: pkg-etc-ssl
              mountPath: /etc/ssl
            - name: pkg-etc-passwd
              mountPath: /etc/passwd
            - name: pkg-etc-group
              mountPath: /etc/group
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command: ["test", "-d", "/home/opencode/.pkg-queue"]
            initialDelaySeconds: 10
            periodSeconds: 30
      terminationGracePeriodSeconds: 10
      restartPolicy: Always
      volumes:
        - name: opencode-home
          hostPath:
            path: "{{ .Values.userspace.appData }}"
            type: DirectoryOrCreate
        - name: opencode-workspace
          hostPath:
            path: "{{ .Values.userspace.userData }}/Code"
            type: DirectoryOrCreate
        - name: nginx-config
          configMap:
            name: nginx-config
            defaultMode: 438
            items:
              - key: nginx.conf
                path: nginx.conf
        # Parent of the overlay tree (upper/work/merged + marker). init-overlay
        # mounts this Bidirectional so the overlay mounts it creates propagate to
        # the node and are visible to the other containers' merged binds below.
        - name: pkg-overlay
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay"
            type: DirectoryOrCreate
        # Merged overlay views (image lower + user upper), populated by init-overlay.
        - name: pkg-bin
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/merged/bin"
            type: DirectoryOrCreate
        - name: pkg-sbin
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/merged/sbin"
            type: DirectoryOrCreate
        - name: pkg-usr
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/merged/usr"
            type: DirectoryOrCreate
        - name: pkg-lib
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/merged/lib"
            type: DirectoryOrCreate
        - name: pkg-lib64
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/merged/lib64"
            type: DirectoryOrCreate
        # Tiny /etc bits kept as plain persistent files/dirs (seeded by init-packages
        # from the image); not part of the overlay.
        - name: pkg-etc-passwd
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/etc/passwd"
            type: FileOrCreate
        - name: pkg-etc-group
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/etc/group"
            type: FileOrCreate
        - name: pkg-etc-apk
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/etc/apk"
            type: DirectoryOrCreate
        - name: pkg-etc-ssl
          hostPath:
            path: "{{ .Values.userspace.appData }}/.pkg-overlay/etc/ssl"
            type: DirectoryOrCreate

---
apiVersion: v1
kind: Service
metadata:
  name: opencode
  namespace: "{{ .Release.Namespace }}"
  labels:
    io.kompose.service: opencode
spec:
  ports:
    - name: web
      port: 3000
      targetPort: 3000
  selector:
    io.kompose.service: opencode
status:
  loadBalancer: {}

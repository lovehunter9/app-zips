# 音频能力契约对齐方案（模型自报 / 能力 / 端点）

> 目标：把音频这条线的"模型自报、能力声明、端点目录"三件事,全部搬回上游已有的约定上。
> 三条硬约束（老板定的）：
> 1. **不留任何过渡字段、不写任何兼容分支**——一次切干净;
> 2. **不给 llm-gateway 的同事增加任何学习或兼容负担**——他们读的东西一个字都不变;
> 3. **`/v1/models` 必须真正尊重标准**,像 llama-server 那样返回完整响应,不是只给两个字段。

---

## 一、要修的是什么（带证据）

### 1. 引擎的 `/v1/models` 既不标准也不完整

现在只回 `{object, data:[{id, object, mode, supports, endpoints}]}`：

```16:28:/Users/wangrongxiang/beclab/audio-engines/wrapper/contract.py
def models_payload(model_name, mode, supports, endpoints):
    return {
        "object": "list",
        "data": [
            {
                "id": model_name,
                "object": "model",
                "mode": mode,
                "supports": supports,
                "endpoints": endpoints,
            }
        ],
    }
```

两个问题叠在一起：**标准字段缺**（`created` / `owned_by` 都没有,llm-init 自己的实现就明确要求这四个字段齐全）,**自造字段多**（`mode` / `supports` / `endpoints` 三个非标）。同一条链上的翻译模型（llama-server）回的是 Ollama `models[]` + OpenAI `data[]` 的完整并集,带 `capabilities`、`details`、`meta`。

```146:151:/Users/wangrongxiang/beclab/llm-init/internal/adapter/ollama/openai_types.go
type OpenAIModelDatum struct {
	ID      string `json:"id"`
	Object  string `json:"object"`
	Created int64  `json:"created"`
	OwnedBy string `json:"owned_by"`
}
```

### 2. "能干什么"这件事,标准里本来就有字段名,我另造了一个

Ollama 的能力词表是 `completion / tools / insert / vision / embedding / thinking / image / **audio**`（`ollama/types/model/capability.go`）,以裸键数组出现在模型列表的 `capabilities` 字段里。也就是说粗粒度的 `audio` 早就是标准取值,我却在 OpenAI 行上另开了一个 `supports` 数组。

### 3. 能力键名在 spec 那一层也不合规

llm-init 的 spec 里能力标志是**带前缀**的（`supports_translate` 是它自己的本地扩展,LiteLLM 那批是 `supports_vision` / `supports_function_calling`）,而且未知键**直接 fail-fast**：

```133:137:/Users/wangrongxiang/beclab/llm-init/internal/config/config_load_model.go
// parseSupportsCSV decodes the MODEL_SUPPORTS comma-separated enabled-key
// list into the supports map. Unlisted keys are false (absent). Unknown
// keys fail-fast: a typo in an operator env is a config error, unlike the
// file path which soft-tags unknown keys into extensions._unknown_flag.
```

llamacpp 的 chart 是在**模板里**把用户友好的粗粒度组名展开成 `supports_*` 再交给 llm-init（`$supportsGroups` 那段）。我当初给音频加的是裸键 `stt`,并且是把裸键直接塞进了 llm-init 的 `knownSupports`,等于把不合规变成了"合规"。

### 4. 端点目录有两个家,而官方那个对音频返回的是错信息

llm-init 有官方端点目录 `GET /api/endpoints`,行结构里已经带 `available` / `reasons`（"编译进来了"与"这个进程里可达"分开）。但对 `ENGINE_KIND=audio`,它返回的是通用 OpenAI 那几行（chat/embeddings）——**不是缺音频端点,是给了错的端点**,而我在 PR #48 里写了条测试把这个行为固定成了"预期"。前端只好绕道去抓引擎的 `/v1/models` 拼真实端点（`dashboard.js` 的 `fetchEngineEndpoints`）。

### 5. 结果是：一个没有主人的能力契约

同一件事有三份拷贝、两种形状、零对账：引擎 `/v1/models.supports[]`、llm-init spec `supports{}`、gateway `model_spec.supports{}`。其中后两份**都能被独立编辑**（llm-init 有 `PUT /api/model-spec`,gateway 控制台有能力勾选器）,而改哪一份都不会改引擎真正挂了什么路由。gateway 用来放行/拒绝的那一份,引擎永远看不到。

### 6. 而且"引擎自报"这个前提本身是假的

引擎的 supports 是把 `MODEL_SUPPORTS` 原样回显（`parse_supports()` 读 env）,endpoints 也是按这个 env 拼出来的（`build_app(supports)` 里 `if has_stt: ...`）。所以真正缺的分层是：**镜像实现了什么**（代码事实）与**实例声明开放什么**（env 子集）从来没有被区分,env 写了镜像没实现的能力也没人拦——而 gateway 那个 `capability_not_supported` 门存在的意义正是拦这个。

---

## 二、目标分层

| 层 | 谁是权威 | 载体 |
|---|---|---|
| 镜像实现了什么（能力上限） | 引擎代码 | 引擎内省端点的 `implements` |
| 实例声明开放什么 | chart env `MODEL_SUPPORTS` | 必须 ⊆ `implements`,越界**启动硬失败** |
| 对外的能力契约 | llm-init | `GET /api/model-spec` 的 `supports{}`（上游形状,gateway 早就在读） |
| 对外的端点目录 | llm-init | `GET /api/endpoints`（含引擎中继来的行） |
| 对外的模型列表 | 引擎（llm-init 原样反代） | `GET /v1/models`,**纯标准**（Ollama `models[]` + OpenAI `data[]`） |

一句话：**能力是声明,端点是推导**。声明只有一个入口（chart env）、一个权威校验点（引擎启动）、一个对外出口（llm-init）。

---

## 三、四个 wire 面的确定形状

### 3.1 引擎 `GET /v1/models`（对外,纯标准）

对齐 llama-server 的并集,任何能吃 llamacpp 的客户端都能吃我们：

```json
{
  "models": [
    {
      "name": "Qwen/Qwen3-ASR-1.7B",
      "model": "Qwen/Qwen3-ASR-1.7B",
      "modified_at": "2026-07-29T06:12:03Z",
      "size": 3554414592,
      "digest": "",
      "type": "model",
      "description": "",
      "tags": [""],
      "capabilities": ["audio", "stt", "stt_stream"],
      "parameters": "",
      "details": {
        "parent_model": "",
        "format": "safetensors",
        "family": "qwen3-asr",
        "families": ["qwen3-asr"],
        "parameter_size": "1.7B",
        "quantization_level": ""
      }
    }
  ],
  "object": "list",
  "data": [
    {
      "id": "Qwen/Qwen3-ASR-1.7B",
      "object": "model",
      "created": 1785306380,
      "owned_by": "audio-qwen",
      "meta": { "sample_rate": 16000, "languages": ["zh", "en", "..."] }
    }
  ]
}
```

- `capabilities`：Ollama 标准字段,裸键数组。首元素是标准词表里的 **`audio`**,后面跟本实例开放的细粒度能力键。
- `data[]` 补齐 `created` + `owned_by`（`owned_by` 用引擎名,与 llama-server 用 `llamacpp` 同理）。
- `size` / `modified_at` / `details` 从磁盘上的模型文件与已知信息填;拿不到的**留空**,和 llama-server 一样,不编造。
- **删掉** `mode` / `supports` / `endpoints` 三个自造字段,不留过渡。`mode` 的信息由 `capabilities` 里的 `audio` 承担。

### 3.2 引擎内省端点（引擎 ↔ llm-init 内部,不对应用暴露）

`GET /api/engine-spec`,直接用 llm-init `EndpointInfo` 的行结构,让 llm-init 只做拼接：

```json
{
  "schema_version": 1,
  "implements": ["stt", "stt_stream", "align"],
  "declares": ["stt", "stt_stream"],
  "endpoints": [
    {"method": "POST", "path": "/v1/audio/transcriptions", "category": "data-plane-engine",
     "description": "Offline transcription (single / batch segments; async=1 supported)",
     "available": true},
    {"method": "WS", "path": "/v1/audio/stream", "category": "data-plane-engine",
     "description": "Streaming ASR (WebSocket)", "available": true},
    {"method": "POST", "path": "/v1/audio/align", "category": "data-plane-engine",
     "description": "Forced alignment", "available": false,
     "reasons": ["capability not declared in MODEL_SUPPORTS"]}
  ]
}
```

用现成的 `available` / `reasons` 表达"镜像会但这个实例没开",不发明新语义。

### 3.3 llm-init `GET /api/model-spec`（能力权威,**形状不变**）

`supports` 仍是 `map[string]bool`,键仍是 `supports_*`——**gateway 与市场那边一个字都不用改**。音频键改成合规写法：

`supports_stt` / `supports_stt_stream` / `supports_vad` / `supports_diar` / `supports_diar_stream` /
`supports_align` / `supports_enhance` / `supports_speaker_embed` / `supports_tts` / `supports_tts_clone` /
`supports_tts_dialogue` / `supports_audio_llm` / `supports_audio_s2s` / `supports_sound_fx`

### 3.4 llm-init `GET /api/endpoints`（端点唯一出口）

引擎变 alive 后拉一次 `/api/engine-spec`,把 `endpoints` 合并进目录,行上加 `source: "engine"`（`llm-init` 自己的行是 `source: "llm-init"`）。**llm-init 里不出现任何音频词汇**——它只是"把引擎自报的端点转成目录行",llamacpp / vLLM 谁自报都能用。

---

## 四、键名约定（三个面三种写法,规则固定）

| 出现的地方 | 写法 | 例子 | 依据 |
|---|---|---|---|
| chart env `MODEL_SUPPORTS` | 裸键（对人友好） | `stt,stt_stream` | llamacpp 的 chart 也是裸组名 |
| 引擎 `/v1/models.capabilities` | 裸键数组 | `["audio","stt","stt_stream"]` | Ollama 标准 |
| model spec / 市场 / gateway 的 `supports{}` | `supports_*` 布尔映射 | `{"supports_stt": true}` | llm-init + LiteLLM 上游约定 |
| 能力的"名字"（路由段、默认模型槽位） | 裸键 | `stt` | 它是一个东西的名字,不是布尔标志 |

裸键 → `supports_*` 的展开**放在 chart 模板里**（跟 llamacpp 的 `$supportsGroups` 同一做法）：llm-init 拿到展开后的 CSV,引擎拿到裸键 CSV。这样 llm-init 不需要认识音频词表,去特殊化的原则不被破坏。

---

## 五、校验与降级（这是"不写兼容分支"还能不出事的关键）

- **引擎启动**：`declares ⊄ implements` → **硬失败并打印差集**。静默照挂会让 gateway 的能力门形同虚设。
- **llm-init**：拉不到 `/api/engine-spec` → 目录里就没有 `source=engine` 的行,记一条 warn,**不阻塞启动**。
- **gateway**：完全不变。它读市场 `model_metadata` / 自己的 `model_spec.supports`,和别的模型一模一样。
- 原则：**降级,不双解析**。新旧任一侧单独存在时都只是"少一部分信息",不需要任何一处写 if-old-else-new。

---

## 六、切换

没有过渡期,所以按实例原子切换：新引擎镜像 + 新 llm-init 镜像一起,走 RULE-1 重建（title/env 不变 → 公网 URL 不变）。

顺序（每步都可独立发布,单独存在时按上面的降级语义）：

1. **audio-engines**：`/v1/models` 换成标准并集；新增 `/api/engine-spec`；`implements` 落成代码事实；启动做子集硬校验。→ 新 tag
2. **llm-init**：`/api/endpoints` 中继引擎行（`source` 字段）；删 `dashboard.js` 的 `fetchEngineEndpoints`；改掉 PR #48 那条把错行为固定住的测试；`knownSupports` 里的裸音频键换成 `supports_*`。→ 新镜像
3. **四个基座 chart + 九个直装 chart**：`MODEL_SUPPORTS` 分两路（llm-init 拿 `supports_*`,引擎拿裸键）。
4. **我们自己的 WIP gateway 分支**：删掉 `provider_models_sync.go` 里解析引擎 `/v1/models` 扩展的那段（不能把一个我们正要删掉的形状交给同事去维护）；`AllAudioSupports` 改 `supports_*`。
5. 九个实例走一遍 RULE-1；台账 + skill 里的自检口令更新。

验收（每个实例一条命令就能看）：

```bash
# 1) /v1/models 标准且完整：models[] 有 capabilities，data[] 有 created/owned_by，且没有 supports/endpoints
curl -s "$PUB/v1/models" | jq '{caps: .models[0].capabilities, created: .data[0].created,
  owned_by: .data[0].owned_by, leaked: [.data[0] | has("supports"), has("endpoints"), has("mode")]}'
# 2) 端点只有一个家，且音频端点确实在里面
curl -s "$PUB/api/endpoints" | jq '[.endpoints[] | select(.source=="engine") | "\(.method) \(.path)"]'
# 3) 能力契约是 supports_* 映射
curl -s "$PUB/api/model-spec" | jq '.supports'
```

---

## 七、影响面

| 仓库 / 位置 | 动什么 |
|---|---|
| `audio-engines` | `wrapper/contract.py`（响应重写 + 新内省端点）、`wrapper/caps/*.py`（endpoints 构造 + `implements` 声明）、启动校验、README |
| `llm-init` | `internal/controlplane/endpoints.go`（`source` + 引擎中继）、`endpoints_test.go`（那条测试）、`internal/config/modelspec.go`（音频键改 `supports_*`）、`static/dashboard.js`（删绕道）、docs |
| `app-zips` charts | 四个基座 + 九个直装：`MODEL_SUPPORTS` 分两路 |
| `llm-gateway`（我们的 WIP） | 删 `provider_models_sync.go` 的扩展解析；`AllAudioSupports` 改前缀写法 |
| 运行环境 | 九个实例 RULE-1 重建（URL 不变） |
| 文档 | 本文件、台账、`audiolabxv3-instances` skill、`音频能力_llm-gateway接入讲解要点` |

---

## 八、明确不做

- 不引入 mode-per-capability（`mode=audio` 一个粗粒度 mode 不变）。
- 不在 gateway 侧白名单化能力词表。
- 不把音频词汇写回 llm-init——中继必须是通用的。
- 不改 `supports` 这个 wire 名（上游的,沿用）；只把音频键改成合规前缀、把形状统一成 map。
- 不保留任何"过渡字段"或"双形状容错"。

---

## 九、待拍的决定

1. **gateway 侧的发现面缺口要不要一并提给同事**（`/v1/models` 带 `mode` + `capabilities`,值就是 `model_spec.supports` 里为 true 的键去掉 `supports_` 前缀 → 正好是 Ollama 那个裸键数组,零新词表）。属于他们的活,本方案不含,但建议写进讲解要点。
2. **运维编辑能力键的口子**（llm-init `PUT /api/model-spec` / gateway 控制台）：关掉,还是保留但以引擎 `implements` 为准并告警？
3. **`data[0].meta` 填多少**：llama-server 填的是 GGUF 头里的真参数。我们能拿到的（采样率、支持语种、模型参数量）填到什么程度算够。

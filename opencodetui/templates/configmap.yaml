apiVersion: v1
kind: ConfigMap
metadata:
  name: bash-wrapper
  namespace: {{ .Release.Namespace }}
data:
  bash: |
    #!/bin/sh
    if [ -t 0 ] && [ $# -eq 0 ]; then
      cd /home/opencode/workspace 2>/dev/null
      exec opencode attach http://localhost:3000
    fi
    exec /bin/sh "$@"

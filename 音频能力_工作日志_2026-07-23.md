# 音频能力 工作日志 · 2026-07-23

> 明天准备提 PR。本文=今日成果 + 当前状态 + git/镜像落点 + 明日 PR 待办 + 关键决策留痕。

---

## 一、今日主线成果

### 1. 分段批量(batch STT / batch align)—— 全链路落地
- **契约(方案见 `音频能力_批量STT与对齐_方案_2026-07-23.md`)**:同端点可选 `segments`(JSON 数组)触发批量;无则老单条行为逐字节不变。
  - `POST /v1/audio/transcriptions` + `segments=[{start,end}]` → `{results:[{text}|{error}]}`
  - `POST /v1/audio/align` + `segments=[{start,end,text,[language]}]` → `{results:[{units(片内相对时间),language}|{error}]}`
  - 逐项容错;llm-init/gateway **零改动**(透传)。
- **基座 wrapper 实现**(改动点只在此层):
  - `audiovllmbasexv3`:`stream.py`(Qwen 离线 STT)+ `align.py`(ForcedAligner)各加 segments 分支。
  - `audiofasterwhisperbasexv3`:`stt_fw.py` 加 segments 分支(无 soundfile → ffmpeg 切片 + 复用 `_run`)。
  - 三脚本 `py_compile` 通过。
- **四层解耦原则**:魔改沉基座;共享层(init/gateway)保持通用透明;**不做能力广播**(应用自知/嗅探,带 segments 支持的回 `{results}`、不支持的老 wrapper 回 `{text}`)。

### 2. 两个 demo 加「分段批量」模式(三选一互斥,默认 batch;整段/分段旧逻辑不动)
- **minutesx2**(server 端):config/options/PUT/runJob 全接 `sttMode ∈ {integral,segmented,batch}`(默认 batch);`runBatch` = diar 窗 → FLAC 子片按累计字节 ≤30MiB 贪心分批 → 每批 1 次 batch STT + 1 次 batch align → 拼装;设置页 3 单选 + 每文件弹窗 3 选。
- **studiox2**(浏览器端):`sttMode` state 默认 batch;`api.ts audioBatch()`;`runSttBatch`(WAV 子片,`sliceWav`)+ align stage 批量分支复用 `finishAlign`;UI 复选框→三单选。
- **载荷/分批**:唯一阈值=**累计上传字节 ≤ 31.8 MiB**(网关 nginx `client_max_body_size 32M`);minutes 用 FLAC(无损)、studio 用 WAV;均按 **32KB/s 保守估算**打包(保证任何内容不 413)。**不设墙钟**(RTF 不准、Olares 更快、集群内可绕)。

### 3. 上传体积守卫(压缩)—— 修长音频 413
- 网关前端 nginx 上限 **32M**;阈值 **31.8 MiB**,超阈值才转「按时长动态码率的 16k 单声道 mp3」(能不压就不压)。
- minutes `gwAudioOp/gwAudioEnhance`(diar 整段等)、studio `edgeSafe`(8M→31.8M + 已压缩超阈值也重压)、`/api/transcode` 动态码率。

### 4. `/v1/align` 别名彻底移除
- 6 个 wrapper 去 `@app.post("/v1/align")`,llm-init `adapter.go` 删别名映射;gateway 本就没有。确认它非官方契约/非模型原生(强制对齐业界无 HTTP 标准;模型原生只有 `.align()`)。

### 5. 翻译"临时"来源查清(明日 PR 关键)
- `/translate*` 端点=**llm-init 对开源 MTranServer(`xxnuo/MTranServer`)官方 API 的 Go 复刻**(不是生造),在 llm-init **另一分支 `feat/supports-translate-api`**(`internal/translate/handler.go`,`maxBatchTexts=64`)。
- gateway 的 `f1bcbec TEMP translate passthrough` **只在 gateway**、只 2 文件(router.go +11 行 + translate_temp.go)。**最新 main 无任何语言翻译路由 → rebase 后 TEMP 仍必须保留**;要去掉须在 gateway 补正式翻译路由(未做)。

### 6. rebase 到最新 main + 全量重打镜像 + 重 clone
- **llm-init** rebase→`ffaecc0`,0 冲突,`go build` 干净。
- **llm-gateway** rebase→`e6ba965`,仅 `go.mod` 冲突(gofuzz+gorilla/websocket 都留,`go mod tidy`),`go build` 干净。
- **migration**:我们 `000021_audio_mode` 接 main 的 `000020` 之后无撞号;线上 DB 早已是 audiobase 态(`stt_relative` 一次性手动脚本此前已执行),**本次 roll 无需动 DB**。
- 两分支 rebase 前有 `backup/prerebase-*` 备份 ref。

---

## 二、当前落点(镜像已推 / git 未提交)

**已推镜像(docker.io/lovehunter9):**
- `llm-init:v1.3.2-audiobase6`
- `llm-gateway-backend:v2.0.7-audiobase3`、`llm-gateway-frontend:v2.0.7-audiobase3`
- `audiostudiox2demo:1.0.0`、`audiominutesx2demo:1.0.0`

**已上传 chart(market,source=upload):** `audiostudiox2demo-1.0.0`、`audiominutesx2demo-1.0.0`(新建)。

**已重 clone(RULE-1,9 个 audiobase 实例,hash 全部原样复现→URL 不变→provider 无需重指):**
基座 chart llm-init 镜像已 bump 到 audiobase6;translate 基座 `llamacpphymt218bggufv3` 未动(另一分支)。
台账见 `音频能力_基座实例台账.md`(9 个 clone 的 title+5 输入 + RULE-1 周期)。

**git 状态(均未 commit / 未 push):**
- `llm-init` `feat/audio-base`:已 rebase(4 提交在 ffaecc0 上)。
- `llm-gateway` `feat/audio-base`:已 rebase(`6a95d61` 音频数据面 + `f1bcbec` TEMP 翻译,在 e6ba965 上)。
- `app-zips`:x2 charts(studio 1.0.0 / minutes 新建 1.0.0)、两 demo 批量代码、压缩守卫、wrapper segments 分支、/v1/align 移除、台账、方案文档、本日志——全部**未提交**。

---

## 三、待用户/明日验证
- 网关 **roll 到 audiobase3**(前+后端);9 基座实例就绪(下载模型较久)。
- 测试机安装 `audiostudiox2demo` / `audiominutesx2demo`(1.0.0),app 内配网关,验分段批量(默认)/分段/整段三模式 + 长音频(2h/3h22m)413 消失。

---

## 四、明日 PR 待办
1. **llm-init PR**(`feat/audio-base`→main):audio surface + MODEL_SUPPORTS 门控 + 各引擎 GPU diag + /v1/align 移除 + `segments` 批量分支。rebase 已干净,直接推分支开 PR。
2. **llm-gateway PR**(`feat/audio-base`→main):音频数据面 `6a95d61`。**`f1bcbec` TEMP 翻译要取舍**——① 从 PR 里摘出(只 PR 音频面,TEMP 留本地) 或 ② 先补一版正式翻译路由再一起 PR。**需先拍板**。
3. **app-zips 提交**:x2 charts/demos/批量/守卫/台账/方案/日志 落库(app-zips 是独立仓,按其流程)。
4. 提 PR 前再 `git fetch` 一次,若 main 又前进则重跑 rebase(llm-init 预计仍干净;gateway 关注 go.mod / providers)。

---

## 五、关键决策留痕(供 PR 描述引用)
- 批量=同端点 `segments` 加法式扩展,不破坏 OpenAI 兼容;逻辑只在基座 wrapper;init/gateway 透传。
- 批量按**上传字节**分批(非分钟/非墙钟);载荷无损(FLAC/WAV),质量不妥协。
- 一切音频切点应按**能量/静音**(VAD),严禁掐秒平均(增强除外)——**待办**:`buildWindows` 的 SPLIT_MAX 均匀切仍是掐秒,后续改能量切点(本轮未做,列入 batch 后续)。
- diar 超长(3h+)仍有自身墙钟/需 chunk+stitch —— **后续**,本轮先靠压缩过 413。

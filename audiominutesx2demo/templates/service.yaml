apiVersion: v1
kind: Service
metadata:
  name: audiominutesx2demo
  namespace: "{{ .Release.Namespace }}"
  labels:
    io.kompose.service: audiominutesx2demo
spec:
  type: ClusterIP
  selector:
    io.kompose.service: audiominutesx2demo
  ports:
    - name: web
      protocol: TCP
      port: 8080
      targetPort: 8080

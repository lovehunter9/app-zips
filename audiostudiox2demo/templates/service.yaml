apiVersion: v1
kind: Service
metadata:
  name: audiostudiox2demo
  namespace: "{{ .Release.Namespace }}"
  labels:
    io.kompose.service: audiostudiox2demo
spec:
  type: ClusterIP
  selector:
    io.kompose.service: audiostudiox2demo
  ports:
    - name: web
      protocol: TCP
      port: 8080
      targetPort: 8080

# 工作日志 2026-07-10 — audiominutesxdemo 长音频对齐塌陷根治，发布 1.0.7 + 1.0.8

飞书妙记式音频笔记 App（`audiominutesxdemo`）本日迭代。镜像 `docker.io/lovehunter9/audiominutesxdemo`，
chart 目录 `audiominutesxdemo/`，代码 `audiominutesxdemo-app/`（前端 `web/` + Node 服务 `server.js`）。
本地自测口径：服务起在 **:8090**（严格照 skill `local-dev-server`：先放端口，再 `DATA_DIR=./data PORT=8090 node server.js`
作为工具托管的前台后台任务 `block_until_ms:0`、`required_permissions:["all"]`；`&`/`nohup` 一律会被回收，禁用）。

本日主线：**根治「整段转写 + 词级对齐」长音频时间戳塌陷**（同一秒挤几十段），先补提交昨天遗留的 1.0.7
异步提取，再实现并修复 `alignLong` 饱和切窗，发布 1.0.8（仅 amd64）。另有 llm-gateway 一条支线（见文末）。

## 一、补提交昨天遗留的 1.0.7（异步音频提取）
- 发现昨天「上传即回包 + 后台提取音频」这套 1.0.7 改动一直没 commit，混在今天的实验性对齐改动里。
- 用 `git apply --cached` 把 `server.js` 按 hunk 拆分：**昨天**（`prepQueue`/`runPrepJob`/`extractWav16kMono`/
  非阻塞 `/api/upload`/multer 错误处理/MIME 兜底/`pendingTranscribe`）+ `web/src/*` + chart 单独提交为 1.0.7；
  **今天**（对齐）留在工作区继续改。

## 二、定位长音频对齐塌陷的真正病根（不是玄学）
- 现象：整段转写 >~5 分钟音频，文字记录从某一时刻起「几十段全挤在同一秒」，词时间退化成 0.2s 均匀兜底值。
- 用户关键澄清（务必记牢）：**STT（Qwen3-ASR）不返回任何时间戳**，只给文本；**ALIGN 是唯一时间来源**；
  DIAR 只给说话人。所以严禁用「按字数平均切」去分窗——那是一切错误的根源。
- 查 Qwen 官方口径：`Qwen3-ForcedAligner-0.6B` 只在 ~270s 内可靠，超过后索引饱和、LIS 单调化把后面所有词
  压到一个时间戳（「同一秒」签名）。官方做法＝迭代重对齐，检测饱和点并在此切窗。
- **实现 `alignLong`（饱和切窗）**：每窗喂 >一窗的文本（over-provision），`reliableAlignEnd` 检测塌陷平台、
  只保留可靠前缀，从最后一个可靠词的 END 时间 + 剩余文本续对下一窗。移除旧的 `splitTextN`（按字数比例切）。

## 三、抓出 alignLong 的致命字符偏移 bug（本日核心修复）
- 盘上 23:24 用新代码重转的记录仍从 `645.86` 起整条尾巴塌平 → 说明后半段 unit 没能映射回 `fullText`。
- 用**该记录真实文本**逐趟复现（探针打印 `a/cOff/part/units/k/nextA/nextC`），抓到元凶：
  ```
  pass4 a=634.9 cOff=11746 partLen=6609 units=1160 k=340 keptEnd=746.98  nextC=18355 ← 字符偏移跳到文本末尾!
  → 循环因 cOff 到头而终止，音频 747→1012s(4分钟)从未对齐 → 尾部被 buildCharToTime 全钉在 645.86
  ```
- 根因：饱和窗口的 `part` 是重复短词极多的尾巴（So/It/the…），我却对**全部 unit（含塌陷的垃圾尾）**跑
  `mapUnitsToRef`，`indexOf` 在重复词上一路越级匹配 race 到文本末尾，`nextC` 跳飞。初版探针「没事」只因当时
  用旧文本（本身就是对齐器产出的），匹配太顺；真实新 STT 文本 tokenization 一变就露馅。
- **修复两条**（`server.js`）：
  1. 只映射**可靠前缀** `u.slice(0, k+1)`，绝不碰塌陷垃圾尾（先算 `k` 再映射）。
  2. 新增 `mapUnitsBounded`：token 只允许在游标前方 `slack=48` 字符内匹配，短词不得跨越式跳；无近匹配则游标最小前进。
- 同音频复现对比：pass4 `nextC` 18355→13712；覆盖时长 746s→1005.9s（≈全长1012）；非单调 0；最大塌陷 run 巨大→9。

## 四、句/标点分段（沿用，本日无回退）
- 中文在句/叹/问号 + 右半引号连用时于引号处断句，英文句点 + 引号也在引号处断；`isSentEnd`/`swallowClosers`/
  `sentenceCuts` 统一处理 CJK 与 ASCII 闭合引号/括号。整段路径直接用 STT 全文的句界切，时间取自 ALIGN，不再夹 DIAR 窗边界。

## 五、发布 1.0.7 + 1.0.8
- 1.0.7：`docker.io/lovehunter9/audiominutesxdemo:1.0.7`（已于昨天流程打，本日仅补 commit）。
- 1.0.8：`docker.io/lovehunter9/audiominutesxdemo:1.0.8`（**仅 amd64**，buildx `--platform linux/amd64 --push`，
  builder `multi-platform`）；manifest list `sha256:b6691b415823347c8d6a5ac39801df2432a3e68ff18cbdcb3f1b9f8ad1e5f419`。
- chart 同步 1.0.8：`values.yaml`(image tag)、`Chart.yaml`(version/appVersion)、`OlaresManifest.yaml`(version/versionName + 1.0.8 changelog)。

## 六、本日提交
- `8c1ce59` audiominutesxdemo 1.0.8: long-audio alignment via saturation chunking + tail-collapse fix（4 文件 +251/−135）。
- 另有 1.0.7 补提交（异步音频提取，server.js 昨天 hunk + web/src/* + chart）。

## 七、待办 / 交接（下周一继续）
1. 实机部署 1.0.8 验证：把 Vault（16:52）整段重转，确认 10:45 之后不再塌、时间戳一路单调递增到 16:52；
   再抽查中英文标点断句是否符合（句号/问号/引号处断，逗号不断）。
2. **逐词高亮「突进→卡住」强化修复**（用户 1.0.8 实测残留，今日仅定位未修）：详见「八」——去 burst + 区分真停顿 + 窗内塌陷检测 + 接缝重叠。
3. `alignLong` 边界稳健性可再压测：>1 小时音频、空窗（对齐失败返回空）时的 no-progress guard；`slack=48` 是否需按语言调。
4. `audiostudioxdemo`（8080 DEMO）同源问题仍在：用户已确认 DEMO 的融合也是错的（有说话人时更明显），
   本日**未改 DEMO**，待 audiominutes 稳定后把 `alignLong` + 前缀映射同步过去。
5. i18n changelog（`i18n/en-US|zh-CN`）本次未同步 1.0.7/1.0.8，如上架公开市场需补齐。

## 八、残留问题（下周一强化）：逐词高亮「突进→卡住」(burst-then-gap)
用户实测（1.0.8，8090 重转 Vault，记录 `60921671`，203 段、时间已单调）反馈：某些句子字幕高亮会
**飞快向下滑一串词、然后停在某个词上等好久**，直到音频真到那个词才继续。示例（截图取证）：

- 13:44 「So maybe we're doing dynamic credentialing for our message queues, but this kind **of** goes on.」
  逐词：`So@824.58 … but@827.22 this@827.7 kind@827.78 of@827.94` → **`goes@831.38`**（`of`→`goes` 凭空空 3.44s）。
  高亮 824.58→827.94 一口气冲到 `of`，随后卡住到 831.4。
- 14:35 「**And this way**, you're constantly moving…」逐词：`And@875.517 this@875.745 way@875.86` →
  **`you're@878.9`**（`way`→`you're` 空 3.04s）。三词挤在 0.34s，随即停住。

### 数据扫描（全片 3261 词）
写脚本统计「相邻词间隔 ≥2s」的位置，共 **16 处**，每处都伴随「前 5 词被压在很短时间内」的 burst 特征：
```
42.08(2.24s, 前5词1.44s) 45.92 49.60 56.72(前5词0.59s!) 262.96 412.46 419.14
508.74(2.88s) 518.82 634.90 722.68 757.86(前5词0.88s) 765.70 780.82
827.94(3.44s, 前5词1.32s ←用户案例) 875.86(3.04s, 前5词0.60s ←用户案例)
```

### 成因判断（两类，需分别处理）
1. **对齐器自身的局部微塌陷**（主因，用户两例都属此类，且都在 pass5 窗口**内部**、非接缝）：
   Qwen ForcedAligner 在停顿/换气处会把前面一串词的时间**前挤成 burst**、再留一个大空档，本质是
   全局塌陷的「轻症局部版」。我们目前**原样保留对齐器逐词时间**（`finalizeWords` 只做钳位+严格单调，
   不识别 burst、不平滑大间隔），前端卡拉OK高亮如实渲染，就表现为「突进→卡住」。
2. **窗口接缝处的边界词**（次因）：`alignLong` 每窗接续时，下一窗首词起始＝上一窗最后可靠词的 END(`a`)，
   若真话在此处有停顿，边界词会拿到 `a` 而与后词间形成间隔（如 `634.90s` 恰为 pass3→pass4 接缝）。
   `reliableAlignEnd` 只裁**窗尾**塌陷平台，不管**窗内**微塌陷。

### 下周一强化方向（择一/组合，先不动）
- **去 burst（de-burst）**：检测「N 个词被压在异常小的 dt 内（如 >4~5 词/秒）且紧跟 ≥2s 间隔」的段，
  把这串词**在 [burstStart, 下一词起始] 区间内重新均摊**（用那段空档时间把突进的词铺开），使高亮匀速推进。
- **区分真停顿**：大间隔可能是真静默（此时高亮该停）。用 VAD/能量或已提取的 16k WAV 在该区间做一次
  静默检测——**仅当该处并非真静默**时才判定为对齐 burst 并平滑，避免把合理停顿也抹平。
- **窗内塌陷检测**：把 `reliableAlignEnd` 的平台检测从「只裁窗尾」扩展为「窗内多平台」识别，对内部平台做局部重摊。
- **接缝重叠**：`alignLong` 相邻窗加少量 overlap（如 5~10s），用重叠区校正边界词时间，消除接缝间隔。
- 复现脚本已就绪（`/tmp` 探针思路：flatten 逐词 start → 找 ≥2s 间隔 + 前窗 burst 密度），可固化到 docs 便于回归。

## 九、测试机复现「算法不稳定」（下周一细查，今日仅记录，未查未修）
1.0.8 部署到测试机后，用户重转同一条 Vault，逐词高亮仍出问题，且**比本地更严重**（用户原话「图2情况更严重」）。截图取证：
- 图1：视频在 **16:10**（字幕「maybe network connectivity is an issue / 也许网络连接是一个问题」，已开补翻译显示译文），
  但文字面板里 **16:03 一带堆了多段**（"So you don't have a single PEM…" / "We can audit against…" /
  "Either statically or dynamically…" / "And then, in a broader deployment…" / "For example, this might be console…" 全标 16:03）。
- 图2（更严重）：视频同在 **16:10**，但文字面板/自动滚动却停在 **6–12 分钟**区（6:02、6:14、11:39、12:07…），
  即当前词定位/自动滚动**跳飞到十分钟以前**。

### 关键对照：同代码不同结果 ⇒ 不稳定（这是本条核心）
- **本地 1.0.8 跑同一条音频**（记录 `60921671`，23:34 重转）扫描结论：**203 段、段起始非单调 0 处、无任何「同一秒 ≥3 段」堆叠、
  尾部一路单调到 1005s** —— 完全干净。
- **测试机 1.0.8 同镜像同音频**：却出现 16:03 堆叠 + 高亮跳到 6–12 分钟。
- 结论：**同一份代码/镜像，换一次运行就从「干净」退化到「严重塌陷+定位跳飞」**，符合用户判断「算法不稳定」。

### 初步怀疑（待周一证实，勿当结论）
1. **STT 转写文本 run-to-run 不一致** 是最大嫌疑：`alignLong` 用「字符偏移 `cOff` + 按 `cps` 估算每窗 `want`」来切窗，
   对文本长度/内容高度敏感。文本稍有出入 → 窗口边界、over-provision 量、饱和点全变 → 可能重新触发 `cOff` 漂移
   / 窗内塌陷 / 边界词错位。今晚想用「STT×2、ALIGN×2 比对」证实，但探针请求字段没对齐真实服务（STT/ALIGN 都返回 0，
   是探针 bug 不是服务问题），**未完成**，周一重做（真实参数见 `server.js` `gwAudioOp`/`sttParams`：
   STT `response_format=json`、align 传 `text`+`model`+`file`，auth 用 Bearer key + cookie + x-bfl-user；
   注意 `config.json` 里 `language:"zh"` 而音频是英文——是否语言参数影响 STT/align 也要一并查）。
2. 也不能排除 **ALIGN 自身非确定性**（同音同文两次输出微差）叠加放大。
3. 「16:03 堆叠 + 跳 6–12 分钟」的组合，形态上像 **cOff 漂移的老病复发**（后半段某窗喂空/错位 → 一批段拿不到正确时间
   → buildCharToTime 把它们钉到邻近某时刻，同时另一批早段被后段时间污染），只是本地这次没触发、测试机触发了。

### 周一必做（按顺序）
1. **取证**：把测试机那条记录的 JSON 拉下来（`data/library/<id>.json`），本地扫描：段起始单调性、`floor(start)` 同秒堆叠、
   尾部覆盖、逐词 ≥2s 间隔 + burst——和本地干净版逐项对比，锁定退化发生的时间段与对应 `alignLong` 窗口。
2. **复现不稳定**：修好 determinism 探针，STT×3 比对文本差异；对差异文本各跑 alignLong，看窗口边界/塌陷是否随文本漂移。
3. 若证实是文本敏感：让 alignLong **少依赖 `cOff` 字符记账**——考虑用「本窗实际消费的 unit 数」推进、或窗间加 overlap 用时间校正边界，
   降低对 STT 文本逐字一致的依赖（与「八」的强化方向合并处理）。

## 十、2026-07-13(周一)细查测试机复现：决定性结论——非「空窗跳文本」，是数据/负载敏感的窗账错位

### 取证与复现（关键）
- 拉到测试机那条问题记录 JSON(`ca88100d…`，即 Vault 视频；用户导出到 Mac `~/Downloads/Introduction to HashiCorp Vault…json`，
  格式为前端导出 `{language,speakers,segments[]}`，203 段、英文、含逐词 `words`)。
- **跳变藏在「一段之内」，段间 gap 检测漏它**：段起始全单调(nonMono=0)、段间 gap≥30s 有 0 处；但有**一段**
  `377.68→674.14`(「Versus in a dynamic secret world, each of those 50 web servers had a unique credential, so we know very specifically.」)——
  **一句话的 END 吞了 ~296s(≈一个窗宽 290s)**，把后面所有段顶到 11:xx。逐词证据：
  ```
  had@381.86 a@382.1  →  unique@526.82  →  credential,@671.54  so@672.34 …
  ```
  `unique@526.82` 是 `buildCharToTime` 在锚点 `a@382` 与 `credential@671.54` 之间的**线性插值**((382+671)/2≈526)；
  真病灶＝**「credential」这个词被钉到了 671s 的对齐单元**(应在 ~383s)。
- **用测试机 fullText + 本地音频重跑 alignLong(当前 1.0.8 代码)：结果完全干净**
  (`unique→382.1s`、`credential→382.3s`、`Webmachine 42→383.4s`)，pass 边界正常
  (`pass3 a=381.5 cOff=7064 nextC=11749`，非单调 0)。
- 文本差异：测试机 fullText 18362 字符 vs 本地 18355，首差在 char2341(「Right?」vs「Right.」)——**仅 7 字符**，
  且用测试机文本重跑仍干净 ⇒ **文本差异不是触发器**。

### 决定性结论
- **同文本、同代码(镜像 digest 已验证含 `mapUnitsBounded`)，换测试机那次运行就从「干净」退化到「一句吞 290s」。**
  差异只剩：**测试机那份 WAV** 和/或 **网关负载下对齐器的非确定输出**。
- **推翻了 1.0.8 之后我提交的「空窗跳文本(cOff)」修复的适用性**：测试机日志此前显示 unit 数满(3258/3259)、
  **无 alignSlice 空窗**，所以那条修复(空窗重试 + cOff 按 cps 前进)**根本不会触发**，自然治不了本 case。
- 真实机理(证据链)：某窗对齐**成功但产出不可靠(把已在 ~382s 说过的 "unique credential…" 后挤/在后一窗被重对齐)**，
  `reliableAlignEnd` 只裁**窗尾**塌陷、`mapUnitsBounded` 的 `cOff` 记账在此处欠进/错配，导致这句尾巴的字符被映射到
  **一个窗之后**的单元上(+~290s)，`buildCharToTime` 再把中间字符插值铺开。**这与「八」的窗内微塌陷(burst-then-gap)同源**，
  都是「对齐器局部不可靠 + 我们对 cOff 字符记账过度信任」。

### 下一版真正要做的修复(未改，待用户指示是否打 1.0.9)
1. **每个 kept unit 硬钳进本窗 [a,b]**：任何单元时间不得超出其来源窗口，从根上禁止「+一个窗」的凭空前跳。
2. **窗账自检(cps 合理性)**：一窗消费的字符数/覆盖音频时长若严重偏离全局 cps，判为错配 → 触发窗内重摊或缩窗重试，
   而非放任 cOff 欠进把老文本喂给后一窗。
3. **少依赖 cOff 逐字记账**：窗间加 overlap，用重叠区的时间锚点校正边界词，降低对 STT 文本逐字一致 & 对齐器稳定性的依赖。
4. 与「八」de-burst / VAD 真停顿区分 / 窗内多平台检测合并处理。
5. 复现口径固化：探针 `/tmp/probe_repro.mjs`(载入导出 JSON 的 fullText → alignLong 带 pass trace → 打印指定字符位的时间)可回归。

### 定音之锤：同源文件 + 同网关 + 对齐器确定 ⇒ 病因＝并发负载下对齐器质量降级
- 用户澄清：**本地和测试机跑的是同一个源文件**(`~/Downloads/…HashiCorp Vault….mp4`)。ffmpeg `-ac 1 -ar 16000`
  确定性提取 ⇒ WAV 实质相同 ⇒ **音频不是变量**。
- **本地连跑 alignLong ×3：逐字节相同**(unique=382.1 / credential=382.3 / Webmachine=383.4，pass 边界一致) ⇒
  **对齐器在本环境是确定的、且干净**。
- **本地探针的 `base` = `https://cdc01162.olarestest003.olares.com`** ⇒ 打的就是**测试机同一集群/同一网关/同一 GPU/同一对齐模型**。
- 变量全部对齐后，本地(干净) vs 测试机(跑偏) **唯一差异 = 是否有并发负载**：
  本地探针只串行发 align；测试机真实转写整条流水线并发(DIAR+STT+ALIGN+翻译)+ 其它应用抢 GPU。
- **最终根因：Qwen3-ForcedAligner 在共享 GPU/网关被并发压满时，对某一窗吐出降级/塌陷的 unit(把该说的词后挤/时间前跳一个窗)；
  空载时同窗同文完全干净。** 不是文件差异、不是 7 字符文本差异、不是代码差异、更不是上次修的「空窗」(测试机 unit 数满、无空窗)。
- 推论：**修复必须假设「对齐器输出不可信」**，在 `alignLong` 层加防线(见下「真正要做的修复」)，而非指望对齐器稳定。

### 交接口径(给用户)
- **本地无法复现**：不是环境/文件问题，是**负载**——空载探测永远干净，要复现须在测试机制造同等并发 GPU 负载。
- 我**只按用户上次指示提交了 5 分钟跳的(空窗)修复、未打新镜像**；本条已证其对本现象无效(无空窗)，故**未再改代码**，等指示。

## 十一、2026-07-13 防御性修复：alignLong 抗「负载降级窗」(未打镜像，先 8090 本地验证)

### 病因回顾(已坐实)
对齐器在共享 GPU 被并发压满时，对某一窗吐出降级输出；`alignLong` 过度信任对齐器、把该窗的坏账传播到下游，
整条尾巴被顶后 ~一个窗(~5 min)。空载复跑同音同文完全干净 ⇒ 修复不能指望对齐器稳定，必须在 `alignLong` 层设防。

### 两类降级 + 对应防线(均 rate-independent，不误伤慢速/静音)
1. **早塌陷(earlyPlateau)**：窗内单元多、内容跨整窗，但可靠前缀 `covAudio < 30s`(健康窗实测最小 ~110s)。
   → 不信任该前缀，音频+文本一起按 cps 估算前进(该段退化为插值)。
2. **字符欠映射(underMap，真凶)**：音频游标正常前进，但对齐器单元**文本对不上转写稿**(负载下产垃圾)，
   `mapUnitsBounded` 命中率 `matchRate < 0.6` ⇒ 文本游标 `cOff` 落后音频 ⇒ 陈旧文本被喂给后面的窗、对到更晚的音频 = +290s。
   → 强制 `nextC` 按 `cps*(nextA-a)` 追上音频，杜绝陈旧文本外溢下游。
- `mapUnitsBounded` 增加每单元 `hit` 标记以算 `matchRate`(其它调用方只用 ci/cj，不受影响)。
- **每个 kept unit 硬钳进本窗 `[a,b]`**：任何单元都不能注入越窗的巨大前跳。
- 空窗/降级均**重试至多 3 次**(负载瞬时波动可自愈)，取 matchRate 最优的一次；仍降级才走上面的强制前进。

### 离线仿真验证(mock 对齐器，无需真复现负载，old vs new)
构造与测试机同位置(覆盖 382s 的窗)的降级窗(垃圾文本+丢半数单元)，对比修复前后：
- **健康路径 old==new 逐字节相同**(unique=398.0/credential=398.4/Webmachine=400.2) ⇒ 未改动正常行为。
- **下游精度**(降级窗之后，真值≈frac×total)：
  ```
  OLD  60%:err +187s  75%:err +184s  90%:err +101s   ← 整条尾巴后移~3分钟(即测试机症状)
  NEW  60%:err  +19s  75%:err  +15s  90%:err  +14s   ← 残差仅来自那一坏窗自身插值，局部有界、不外溢
  健康 60%:err   +5s  75%:err   +2s  90%:err   +0s
  ```
- 结论：修复把下游漂移从 ~187s 压到 ~15s，且健康路径零回归。真值也从真网关空载 3×连跑逐字节一致侧证。

### 十一.b 追加：修「卡顿快跳」burst-then-gap(本地确定性问题，与上面负载 bug 无关)
- 用户尖锐指出：卡顿快跳**本地也有**，别把它和负载 +290s 混为一谈——上面的窗粒度防线**管不到**词粒度的窗内微塌陷。承认：确实两回事，之前日志说「同源」是过度宣称。
- 数据坐实(本地干净记录 `60921671`)：无漂移(maxGap 3.44s)但有 16 处 gap≥2s，**884 个词的相邻间隔 <0.1s**(对齐器在停顿/句尾把一串词挤成物理不可能的密度，再留个空档)。
- **修复 `deburstUnits`(min-slot 松弛 + 有界追账)**：保证每个 unit 至少 `MINSLOT=0.14s` 显示位，从邻近 slack 借时间、在宽松处还账(`MAXCARRY=1.5s`，不永久漂移)；只在物理不可能处生效，**真停顿保留**。严格单调。
- 接入点：`runJob` 里 `alignLong` 返回后、`buildCharToTime` 之前，对全局 `units` 调 `deburstUnits`(units≈words 近 1:1，跨段 burst 也能处理)。
- **端到端验证(真网关 alignLong→deburst→buildCharToTime→逐词时间)**：
  ```
  不去burst: 词间<0.1s = 884   minSlot=0.000  maxGap=3.44  nonMono=0
  去burst  : 词间<0.1s =   0   minSlot=0.134  maxGap=3.40(真停顿保留) nonMono=0
  ```
  「快冲一串词」彻底消除，停顿不被抹平，全程单调。

### 状态
- 仅改 `audiominutesxdemo-app/server.js`：(1) `alignLong` 抗负载降级(matchRate/covAudio 守卫 + 钳窗 + 重试)；
  (2) `mapUnitsBounded` 加 `hit` 标记；(3) 新增 `deburstUnits` 并接入 `runJob`。无前端改动；`node -c` 通过、无 lint。
- **已在 8090 起本地服务待用户验证**；**未** bump 版本、**未**打镜像(按用户「验证 OK 再考虑 1.0.9」)。
- 诚实边界：负载 +290s 修复本地不可复现(仅仿真+逻辑证明)；burst-then-gap 修复本地端到端已证。

## 附：llm-gateway 支线（不同仓库 /Users/wangrongxiang/beclab/llm-gateway，feat/stt_relative）
- 修复 CI 两个失败（main 也在跳）：lint（errcheck/paramTypeCombine/stringXbytes/misspell）+ NATS 集成测试 flaky
  （加 flush + timeout 加固）；新 commit 走同一 PR，commit 信息已简化。
- 实机 test 环境：修 `auth.source` 配置（k8s_informer → local_header，直接 kubectl 改 ConfigMap 未重传 chart）、
  镜像 tag 升 test9/test2、迁移脏库（version 20 dirty）手动在 citus 复位后让 19/20/21 顺序跑通。

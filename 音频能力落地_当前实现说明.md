# 音频能力落地 · 当前实现说明

> 本文只用于**说明目前这套体系"做了什么、怎么接的"**，是对现状的记录，**不代表最终架构结论**；其中的分工与取舍都还可讨论、可调整。
>
> 代码 / PR 地址：
> - llm-init PR #44：https://github.com/beclab/llm-init/pull/44
> - llm-gateway PR #5：https://github.com/beclab/llm-gateway/pull/5
> - audiolabx2v3 代码：https://github.com/Above-Os/terminus-apps/tree/audiolabx2v3-1.0.0/audiolabx2v3
>
> 目前的做法大致是：把每个音频模型部署成一个"和 LLM 类似"的独立 provider，再由网关取用。各部分关系：
> - **audiolabx2v3**：音频模型应用（chart），也是 provider 本身——部署后负责**下载、启动模型 + 对外提供 OpenAI 兼容接口**；每个实例 = 一个可被取用的 provider。（同时是验证"免 nginx"路径的 fork。）
> - **llm-init**：跑在 audiolabx2v3 应用**内部**的"下载 + 反代门面"组件（部署里的 `download-svc`）；PR #44 给它加了音频能力。即 `audiolabx2v3 = llm-init(下载+代理) + engine(跑模型)`。
> - **llm-gateway**：把部署好的 audiolabx2v3 实例**当作 provider 取用**（纳管 / 鉴权计费 / 数据面透传）。
> - **audiominutes**：通过 gateway 消费这些 provider 的示例应用。

---

## 〇、目前各部分的分工

| 层 | 组件 | 目前承担 | 目前未涉及 |
|---|---|---|---|
| 模型应用 / provider（1 实例=1 模型） | **audiolabx2v3** = `llm-init` + `engine` | 下载/启动模型、对外提供 OpenAI 兼容接口 | 聚合 / 鉴权 / 编排 |
| └ 下载+代理门面 | **llm-init**（`ENGINE_KIND=audio`，即 `download-svc`） | 下模型 + 反代引擎 `/v1/*` + 合成 `/v1/models` | 业务编排 |
| └ 推理引擎 | **engine**（+wrapper） | 实际跑模型，提供原生 `/v1/*` | —— |
| 聚合网关 | **llm-gateway** | 取用上面的 provider：鉴权/配额/计费/观测 + 纳管 + 数据面透传 | 业务编排 |
| 编排 | **应用**（audiominutes 等） | 把多能力串成业务（转写 pipeline、RAG） | —— |

数据流（单能力）：`Client ─▶ gateway(鉴权/计费, 按 model 透传) ─▶ audiolabx2v3 实例[ llm-init(readyz 门控+反代) ─▶ engine(+wrapper) ]`

---

## 一、llm-init PR #44 — `ENGINE_KIND=audio` 反代适配器

- 反代外部音频引擎的 `/v1/*`（含 WebSocket 流），改由 llm-init 承担（替代此前的 nginx sidecar 方案）。
- `GET /v1/models` 由 llm-init 按 `MODEL_NAME`+`MODEL_MODE` 边缘合成；health 探引擎 `/healthz`|`/health`。
- dashboard/端点目录按 `MODEL_MODE` 展示对应音频端点（不再显示 LLM chat/embedding 行）；WS 升级透传 `Hijack/Flush/Unwrap`；长 STT 用 3600s 超时。

| MODE | 端点 |
|---|---|
| stt | `POST /v1/audio/transcriptions` + `/translations` |
| stt_stream | `WS /v1/audio/stream` |
| vad | `POST /v1/audio/vad` |
| diar | `POST /v1/audio/diarization` |
| diar_stream | `WS /v1/audio/diarize/stream` |
| translate | `POST /v1/translate` |
| embed | `POST /v1/audio/embeddings` |
| enhance | `POST /v1/audio/enhance` |
| align | `POST /v1/audio/align`（+ `/v1/align`） |
| 所有 mode | `GET /v1/models` |

目前的角色：llm-init 作为每个模型 app 的门面，对外给一份标准 OpenAI 兼容契约 + 自述身份（id + mode）+ health 门控，目前只处理它自己这一个模型（类似 ollama 的托管角色）。

---

## 二、llm-gateway PR #5 — 音频/翻译数据面透传

- 复用既有 chat 管线（dispatch→鉴权/配额→转发→计费→观测）来承载音频/翻译透传，PR 自述 *"No new architecture — rides the same skeleton as `/v1/chat`"*。
- 迁移把 `mode` CHECK 从 10 放宽到 23，加入音频 modes；Adapter 新增 `AudioPassthrough`/`TextPassthrough`（`openai_compatible` 实现）。
- 新增 9 端点：`POST /audio/{transcriptions,vad,diarization,embeddings,enhance,align}`、`WS /audio/stream` + `/audio/diarize/stream`、`POST /translate`。
- 目前只读 `model` 解析 provider、body 原样透传；provider 依 Market metadata 自动纳管。

目前的角色：音频对网关而言相当于"多几类 mode 的 provider"；网关目前给用户的是按能力分的独立端点 + 统一鉴权计费 + 类型化目录，暂未在网关侧做编排。

---

## 三、audiolabx2v3 — 音频模型应用（provider）

- 音频模型应用 chart：部署后下载/启动模型并对外提供 OpenAI 兼容接口，每个实例即一个被 gateway 取用的 provider。对外接口由内部 llm-init 直接反代（entrance 指向 `download-svc:8090`）。
- 克隆表单目前只填 title + 4 env：`MODEL_NAME` / `MODEL_MODE` / `MODEL_SOURCE` / `AUDIO_REQUIRED_GPU_MEMORY`。
- 目前以 `mode` 作为分类轴；引擎按 mode（STT 再按模型名判 whisper/qwen/faster-whisper）自动选；目前用 2 个引擎镜像（vLLM cu129 + pyannote/torch）+ 首次加载按需 pip，diar_stream 另用 NeMo 镜像。

目前已上线 12 实例（覆盖 10 类能力）：

| MODE | 模型 | 实例 | GPU |
|---|---|---|---|
| stt | Systran/faster-whisper-large-v3 | 0263ef | 6Gi |
| stt | Qwen/Qwen3-ASR-1.7B | 93e848 | 12Gi |
| stt_stream | Qwen/Qwen3-ASR-1.7B | b0c2ed | 12Gi |
| vad | silero-v5 | dd1ed9 | CPU |
| diar | pyannote-community-1 | 818606 | 4Gi |
| diar_stream | diar-streaming-sortformer | 9c4797 | 6Gi |
| align | Qwen/Qwen3-ForcedAligner-0.6B | c84c8e | 4Gi |
| translate | m2m100-1.2B（商用默认） | 54d617 | CPU |
| translate | nllb-200-distilled-600M | 53f4c2 | CPU |
| translate | nllb-200-distilled-1.3B | 8e1b2f | CPU |
| embed | pyannote-embedding | cdcb44 | CPU |
| enhance | mtl-mimic-voicebank | 0e4d03 | 2Gi |

> `translate.py` 按 `MODEL_NAME` 自适应选 m2m100/nllb，对外统一 FLORES-200，新增翻译模型不需改引擎。公网 URL 属克隆 app 私有信息，需向用户确认。

---

## 四、audiominutes 视角（消费端示例）

audiominutes（妙记复刻）目前是一个客户端 demo，不托管模型，只调 gateway 的独立端点，编排写在自己的 `server.js` 里：

- 转写：`/v1/audio/transcriptions`(STT) + `/v1/audio/align`(对齐) + `/v1/translate`(翻译)（+VAD/diar 视配置）；切片/分段/批量在 `runWindows`。
- 智能问答(RAG)：`/v1/embeddings`(嵌入) + `/v1/chat/completions`(生成)；切块/检索(cosine)/建索引/拼 prompt 都在 `server.js`。

也就是说，目前的形态是：base 提供单能力端点，具体编排写在应用侧（和 LLM 侧的 RAG 类似）。这一层的归属还可再讨论（见下）。

---

## 五、目前的取舍（均可再讨论）

1. 模型按"1 实例 1 模型"的独立 provider 形态提供（标准 OpenAI 契约，同类可换）。
2. 目前以 `mode` 作为分类轴（chart 选引擎 / llm-init 定端点 / gateway 定路由都按它）。
3. 尽量复用既有底座（llm-init 替代 nginx；gateway 复用 chat 管线；2 引擎镜像+按需依赖）。
4. 编排目前写在应用侧，base 未做编排。**这一点尤其待确认**：编排（分段策略、pipeline、RAG）是否/如何沉淀为可复用层（共享库 / olares-cli），而不是留在某个具体应用里。

---

## 六、现状边界

- 目前接线了 9 个 mode；保留未建 6 个（tts / tts_clone / tts_dialogue / audio_llm / audio_s2s / sound_fx）。
- 已做的验证：RULE-1 重建 12 实例哈希可复现、公网 URL 不变；STT/Diar Stream WS 端到端跑通；免 nginx 代理路径已跑通。

apiVersion: v2
appVersion: "1.0.0"
description: Generic audio-model base; llm-init downloads the model, a clone-selected engine serves it. Model + mode supplied at install via env.
name: audiolabxv3
type: application
version: 1.0.0

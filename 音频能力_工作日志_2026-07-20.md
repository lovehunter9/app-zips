# 音频能力（新一套）工作日志 · 2026-07-20

> 明天来了从这里接着干。**触发词：对话开头说「开工」= 明天到了，开始验证 + 后续。**
> 今日成果**均未推送 remote**；且按今日收尾指示：**验证通过后再考虑正式 commit/push**（现有的是本地检查点，可在验证后 amend/squash）。

---

## 0. 今天做了什么（一句话）
把「新一套音频体系」的 **llm-init 干净重写 + audiovllmbasexv3 基座** 做到了「可上传 CHART + CLONE 验证、无错则验收前不再改」的状态。gateway（线 C）和 audiominutes/audiostudio 两个 demo 未动。

---

## 1. 锁定的设计决策（本轮全程遵循）
- **MODEL_MODE 只有一个音频粗 mode = `audio`**；旧的单列能力名（stt/vad/…）**作为 MODEL_MODE 已非法**，只能出现在 `MODEL_SUPPORTS`。
- **`MODEL_SUPPORTS`**：多选、**裸键**（不加 `supports_` 前缀），词表（14 项，**不含 translate**——它是文字→文字翻译，归文字模型同事）：
  `stt / stt_stream / vad / diar / diar_stream / align / enhance / speaker_embed / tts / tts_clone / tts_dialogue / audio_llm / audio_s2s / sound_fx`
  （embed 已改名 `speaker_embed`）
- **一镜像一 base**；每个实际模型只挑对它最好的 base；接受多引擎复杂度。命名 `audio<引擎>basexv3`。
- **clone 为默认**；只有确认官方 1:1（无其它版本/量化）才做专用非-clone。
- **干净重写**：llm-init/gateway 基于 main 新分支，不基于旧 PR#44/stt-relative（仅参考）。
- 版本号 `-` 后用新序列 `audiobase<N>`：llm-init `v1.3.2-audiobase1`、llm-gateway `2.0.7-audiobase1`。
- 应用层将来**新写 x2 副本**（audiominutes/audiostudio），不动原版，便于回溯。

---

## 2. 交付物 + 精确标识（可直接用于验证）

### 镜像（已推 Docker Hub，已核实）
- `docker.io/lovehunter9/llm-init:v1.3.2-audiobase1`
  - amd64，index digest `sha256:5cef038c858d002c64f91dc5ea46bb0eaff8828d24f61947567b4f86a6aa08ca`
  - 含：ENGINE_KIND=audio 反代 + MODEL_SUPPORTS 门控 + /v1/models 合成 + WS 透传 + **HF 预检**。

### chart 包（磁盘就绪；.tgz 被 gitignore，可 `helm package ./audiovllmbasexv3 -d .` 重生）
- `/Users/wangrongxiang/beclab/app-zips/audiovllmbasexv3-1.0.0.tgz`（含 Qwen 双能力 dispatcher）

### 代码提交（本地、未推送）
- **llm-init**（仓库 `~/beclab/llm-init`，分支 `feat/audio-base`，基于 main）：
  - `bfbc749` feat: ENGINE_KIND=audio + MODEL_SUPPORTS 门控 /v1/audio/*（config/engine/proxy/dataplane/controlplane）
  - `84eac93` feat(audio): 下载前按 HF 模型 id 做 MODEL_SUPPORTS fail-fast 预检
  - `go build ./...` + `go test ./...` **全绿**。
- **app-zips**（分支 `audio-base`，基于 `stt-relative`）：
  - `de8e554` / `fcd24d5` 规划文档（一页纸/现状说明/执行清单/llm-init 改动方案）
  - `6ff0c1b` 新 chart audiovllmbasexv3（vLLM + MODEL_SUPPORTS 多选门控）
  - `5ae1ef5` Qwen 单实例 stt+stt_stream dispatcher
  - `helm lint` + 多场景渲染 + 全 wrapper `compile()` **通过**。
- 另：`apps` 分支上 `c62bd0d` 收了批量实验的台账/交接日志（与新一套无关，别混）。

---

## 3. llm-init 改了哪些（干净重写，参考未合并）
- `internal/config/config_enums.go`：加 `ModelAudio`（allowedModelType 放行 audio）；`EngineAudio` 引擎种类（AllEngineKinds）；裸键 `knownAudioSupports`（14 项，无 translate）。
- `internal/config/config_load_model.go`：`parseSupportsCSV` 参数化；audio 模式按 `knownAudioSupports` 校验，未知键 fail-fast。
- `internal/config/config_load_engine.go`：`deriveEngineURL(audio)=http://audio-engine:8000`。
- `internal/config/modelspec.go`：`validateModelSpec` 放行 audio。
- `internal/adapter/factory/factory.go`：EngineAudio → proxy adapter。
- `internal/adapter/proxy/{adapter,messages}.go`：audio 反代；**按 `cfg.Spec.Supports` 门控 `/v1/audio/*`**（未声明能力边缘 404 `capability_not_supported`）；`writeAudioModels` 合成 `/v1/models`（带 supports）；audio health 探活（/healthz|/health）+ 3600s 超时。
- `internal/dataplane/metrics.go`：statusRecorder `Hijack/Flush/Unwrap`（WS 升级）。
- `internal/controlplane/endpoints.go`：`data-plane-audio` 分类，按 supports 出端点、丢 LLM 行。
- `internal/lifecycle/{audio_precheck.go,ensure.go}`：下载前 `precheckAudioSupports`（stt_stream/align 需 qwen；保守；非 audio no-op）+ 单测。

## 4. audiovllmbasexv3 chart 改了哪些
- fork 自 audiolabx2v3。引擎**只留 vLLM**（删跨引擎 auto-select）。
- 运行时按 MODEL_SUPPORTS：`stt`→`vllm serve`（qwen 带 --max-model-len）、`stt_stream`→`stream.py`、`align`→`align.py`。
- `MODEL_MODE` 固定 audio（llm-init 容器）；`MODEL_SUPPORTS` 裸键传两个容器。
- OlaresManifest：`MODEL_SUPPORTS` = editable 多选（stt/stt_stream/align）；去掉 MODEL_MODE/MODEL_ENGINE。
- llm-init 容器镜像 → `v1.3.2-audiobase1`。
- `stream.py`：加离线 `/v1/audio/transcriptions`（复用流式三段式，单载）；engine 补装 `python-multipart`。
- 仍保留 wrappers.yaml 全量脚本（vLLM base 只用 stream.py/align.py；其余未用，**可作清理项**：只留这俩）。

---

## 5. 明天验证步骤（你手动，AI 只管本地代码/打包）
1. `market upload audiovllmbasexv3-1.0.0.tgz`
2. clone 各场景（title + MODEL_SOURCE + MODEL_NAME + **MODEL_SUPPORTS 多选** + AUDIO_REQUIRED_GPU_MEMORY）：
   - STT(Whisper)：`hf://openai/whisper-large-v3` / `openai/whisper-large-v3` / [stt] / 8Gi
   - STT(Qwen)：`hf://Qwen/Qwen3-ASR-1.7B` / `Qwen/Qwen3-ASR-1.7B` / [stt] / 12Gi
   - STT Stream：同上 / [stt_stream] / 12Gi
   - **双能力（重点验证）**：同上 / [stt, stt_stream] / 12Gi → 一实例既离线又流式、显存不翻倍
   - Align：`hf://Qwen/Qwen3-ForcedAligner-0.6B` / `Qwen/Qwen3-ForcedAligner-0.6B` / [align] / 4Gi
3. 预期：app 秒级 running（边下边跑）；dashboard 端点面板按勾选 supports 显示、无 LLM 行；`/v1/audio/*` 生效；调未勾能力→404；误配（whisper 勾 stt_stream）→ readyz 秒级报错、不空等下载。

## 6. 已验证 vs 待验证
- ✅ 本地可验证：go build/test 全绿、helm lint/render、wrapper 语法。
- ⚠ **仅能靠你 GPU clone 坐实**：Qwen 双能力单实例（离线路由复用流式原语，本地无 GPU）；HF 预检的真实 clone 行为；各引擎实际推理。
  - 若这些出错 = 正常的「验证→改到无错」一环，不算翻车。

## 7. 后续步骤（验收/验证后）
- 线 C **llm-gateway `2.0.7-audiobase1`**：AllSupports 加音频裸键；接受 mode=audio；端点按 supports 校验；provider auto-seed 读 supports；保留旧 per-mode 兼容。
- 线 D 应用层：audiominutes / audiostudio **各新写 x2**，模型选择改「按 supports 包含」，provider 注册改 mode=audio+supports。
- 其余 base：`audiopyannotebasexv3`（vad/diar/speaker_embed/enhance）、`audionemobasexv3`（diar_stream）、（按需 `audiofasterwhisperbasexv3` stt）。
- chart 清理：wrappers.yaml 只留 vLLM base 用到的 stream.py/align.py。

## 8. 运维教训（别再踩）
- 打镜像：**必须用受管理后台作业**（Shell 工具直接跑，不加 `&`；`&`/nohup 会被回收）。docker daemon 要先起（`open -a Docker` 等就绪）。
- make 参数：`make docker-multi-push IMAGE=docker.io/lovehunter9/llm-init IMAGE_TAG=v1.3.2-audiobase1 PLATFORMS=linux/amd64`
  —— **IMAGE 不带 tag、IMAGE_TAG 单列**（带 tag 会变双 tag，1 秒失败）。缓存热时 ~95s。
- 工作区外写 `~/beclab/llm-init` 可行；build/test/push 用 `required_permissions:["all","full_network"]`。
- commit 政策（今日指示）：**验证过后再考虑正式 commit/push**。

---

## 9. 2026-07-21 追加：统一 GPU 自省 `/metrics`（形成音频基座范式）

**目标**：所有音频引擎统一用一个 `/metrics` 接口暴露 GPU 占用，llm-init 统一读取 → `/api/diag/gpu`。做成范式，其它基座（pyannote/nemo/…）照抄即可。

**wrapper 侧（chart，`templates/wrappers.yaml`）**
- 新增共享模块 `_gpumetrics.py`（ConfigMap 一个新 key，挂到 `/wrappers/_gpumetrics.py`）：
  用 `torch.cuda.mem_get_info()`（HAMi 下即容器配额切片）手写 Prometheus 文本，**不新增 pip 依赖**。
  暴露裸 gauge：`audio_gpu_present` / `audio_gpu_mem_used_bytes` / `audio_gpu_mem_total_bytes` / `audio_gpu_util_ratio`。
- 每个 FastAPI wrapper `app = FastAPI(...)` 后 `from _gpumetrics import mount_metrics; mount_metrics(app)`
  → `GET /metrics`。已接入全部 9 个 wrapper（vad/diar/translate/embed/enhance/align/stt_fw/stream/diar_stream）。
- 原生 `vllm serve`（whisper/qwen 离线 stt，无 wrapper）自带 vLLM `/metrics`，走回落。

**llm-init 侧（`internal/adapter/proxy/diag.go` + `adapter.go`）**
- 新增 `audioNativeStats`：`EngineAudio` 分支不再返回 unavailable，改为读 `/metrics`：
  - 有 `audio_gpu_*` → `used→VRAMBytes`、`used/total→GPUMemoryUtilization`、mode=full（used>0）/cpu_only（present=0）；Source `audio_metrics`。
  - 无 `audio_gpu_*` 但有 `vllm:cache_config_info` → 复用 vLLM 映射（拆出 `vllmStatsFromFamilies`）；Source `vllm_metrics`。
  - `/metrics` 不可达 → **软降级** unknown+warning（不 503，minimal wrapper 也不炸面板）。
- 新增单测 4 个（diag_test.go）：wrapper gauge / cpu_only / vLLM 回落 / 不可达软降级。**全量 go test 绿**（hfwrap/ollama 两个失败是沙盒 syscall 限制，`["all"]` 外跑即过）。

**产物**
- 镜像 `docker.io/lovehunter9/llm-init:v1.3.2-audiobase4`（amd64，digest `3f421583`）已推。
- chart `llm-init.yaml` 镜像 tag → `audiobase4`；`audiovllmbasexv3-1.0.0.tgz` 已重打包（版本仍锁 1.0.0）。

**验收字段**（perf.js GPU 面板）：VRAM 行显示 used 字节、mode=full 绿、mem-util 显示 used/total%，Source=audio_metrics。

### 9.1 公网 URL 端到端验证（2026-07-21，全绿）

实例 `audiovllmbasexv3fd501a`（title `Audio vLLM Qwen3-ASR`，`MODEL_SUPPORTS=stt,stt_stream`，12Gi）
按 RULE-1 整轮重克隆到 audiobase4，**app 名/URL 未变**。公网入口 `https://d4831e40.olarestest003.olares.com`。

- **GPU 范式** `/api/diag/gpu` → `{"source":"audio_metrics","vram_bytes":8277606788,"gpu_memory_utilization":0.329}`
  （≈7.71 GiB used / 12Gi 配额 ≈ 32.9%；无 model_bytes ⇒ perf.js 折叠为 mode=full 绿）。✅
- **/v1/models 合成** → `{id:Qwen/Qwen3-ASR-1.7B, mode:audio, supports:[stt,stt_stream]}`。✅
- **/api/model-spec** → `mode:audio, supports:{stt:true,stt_stream:true}`。✅
- **门控** → 未声明 `/v1/audio/diarization`、`/v1/audio/vad` = **404** `capability_not_supported`；
  已声明 `/v1/audio/transcriptions`（无文件）= **422**（过门控到 wrapper）。✅
- **端点目录** `/api/endpoints` → stt 转写 / stt_stream(WS) / /v1/models = available:true；
  align/diar/diar_stream/embed/enhance/vad/tts = available:false + 原因。✅
- **单载双能力 dispatcher（重点）**：同一已加载 Qwen 实例
  - 离线 `POST /v1/audio/transcriptions` → 全文正确转写。✅
  - 流式 `WS /v1/audio/stream` → ready→15 partials→final 全文正确。✅
  - GPU 仅 8.28 GiB = **单份模型**，显存未翻倍。✅
- 引擎日志：llm-init（Pod IP 10.233.98.246）周期 `GET /metrics 200`；引擎 READY，restarts=0。

**结论**：新一套（mode=audio + MODEL_SUPPORTS 多选 + 一基座一引擎 + 单载双能力 + 统一 /metrics GPU 范式）
在 audiobase4 + audiovllmbasexv3 上端到端验证通过，验收前无需再改。

---

## 10. 线 C：llm-gateway audio-base（2026-07-21，off main，干净重写）

分支 `feat/audio-base`（== main 起步）。**不兼容旧 12 实例、不留痕迹**（用户指示）。

**设计**
- provider `mode` 只加 **`audio`**（无 legacy per-mode）。能力放 `model_spec.supports`（裸键 stt/stt_stream/vad/diar/diar_stream/align/enhance/speaker_embed/...，**不含 translate**）。
- **audio 的"专门处理"**：每个音频能力键在**默认模型层当作伪-mode**（`defaults.AllModes += AllAudioSupports`；`ResolveOne` arm3 对能力键回落到 `mode=audio AND model_spec.supports @> {cap:true}`）。因为 audio 一个 mode、但各能力端点不同、展成真 mode 太杂。
- `/v1/audio/*` 处理器：`audioCapGate` 校验 `mode=audio` 且 `supports[cap]`；否则 422 `audio_mode_mismatch` / 404 `capability_not_supported`。REST 7 端点 + WS 2 端点（stt_stream/diar_stream，按音频秒计量）。translate 网关侧**不做**（文字模型侧）。
- olares_sync auto-seed：从 Market `model_metadata` 读 `mode`+`supports` 落 `provider_models`（`UpsertOlaresModel` 写 `model_spec.supports`，ON CONFLICT DO NOTHING 不覆盖人工编辑）。
- adapter：`AudioPassthrough`（openai_compatible 真转发；anthropic/gemini/azure 返回 `ErrAudioUnsupported`→422）。
- 迁移 `000021_audio_mode`：provider_models.mode 加 `audio`；default_models.mode 加 `audio`+14 能力键。

**产物**：镜像 `docker.io/lovehunter9/llm-gateway-backend:v2.0.7-audiobase1`（amd64，digest `ccd3f33a`）已推。
前端 nginx WS 转发（`/v1/` 加 Upgrade/Connection）已并入 `deploy/frontend.nginx.conf.template`——但**线上前端 v2.0.6-test1 已含该转发**，可不重建前端。
全量 `go test ./...` 绿；gofmt/vet 干净。新增测试：`audio_test.go`（gate+handler）、`TestSyncer_AudioMetadataAutoSeedsModel`、`defaults.AllModes` 断言更新。

**迁移/DB 处理（用户定：写手动脚本清除旧 000021 影响，回来手动部署时再具体讨论）**：
- 机制核实：golang-migrate、默认 `schema_migrations` 单行表、开机 `m.Up()` 自动跑（`internal/db/migrate.go`）。
- 线上 DB 停在**已废弃 stt_relative 线**的 version 21（13 个 per-mode、无 `audio`）；本分支 off main 的 `000021_audio_mode` 内容不同，因 `m.Up()` 只应用 version>当前 → **不会重跑**。
- **方案（手动前置脚本，不重置 DB、不动 API keys/spend/quota）**：
  `deploy/manual/audiobase_clear_stt_relative_000021.sql` —— 启动 audiobase 后端前执行一次：
  (A) 删除 legacy per-capability 音频行（provider_models + 两张 default 表；去旧 12 实例痕迹）；
  (C) `UPDATE schema_migrations SET version=20, dirty=false` 回退游标 → 开机 `m.Up()` 重新应用 audiobase `000021`。
  脚本带 STEP0 只读预览 + 事务；可选清理孤儿 olares provider（默认注释）。
- 迁移号保持 `000021`（脚本把游标退到 20 后它即生效）。待用户回来一起手动部署。

---

## 11. 其余基座 + audiostudiox2demo 预备（2026-07-21 晚，autonomous）

**线 B 其余 3 个基座**（fork 自 audiovllmbasexv3，同 llm-init `audiobase4`、同 wrappers.yaml 全量 + `_gpumetrics.py` /metrics 范式、同 pydeps/sentinel/HAMi 结构；仅 engine.yaml 引擎镜像 + 按 MODEL_SUPPORTS 选 wrapper 不同）：
- `audiopyannotebasexv3` — 镜像 `beclab/maximsachs-pyannote_fastapi:4.0.4`；caps `vad/diar/speaker_embed/enhance` → wrappers vad/diar/embed/enhance.py（primaryCap 优先级 diar>speaker_embed>vad>enhance）。
- `audiofasterwhisperbasexv3` — 镜像 `beclab/harveyff-whisper-webui:v1.0.7`；cap `stt` → stt_fw.py（CT2 whisper，时间片 vGPU 下快）。
- `audionemobasexv3` — 镜像 `beclab/nvidia-nemo:26.02`；cap `diar_stream` → diar_stream.py（/opt/venv python + /pydeps/diarstream-site 的 NEMO gotcha 已带）。
- 三者 `helm template` 均渲染 engine+llm-init 两个 Deployment；wrapper 全量 `py_compile` 通过；OlaresManifest title/描述/MODEL_SUPPORTS options 已按各 base 收窄。
- 踩坑：Write 长文件时行号栏（`NN|`）误入 3 个 engine.yaml，已用 `^ *\d+\|` 批量剥离并复验（网关侧 Write 文件无此问题）。

**线 D · audiostudiox2demo**（新写 x2，不动原版 audiostudioxdemo）：
- app→`audiostudiox2demo-app`、chart→`audiostudiox2demo`；标识符 `audiostudioxdemo→audiostudiox2demo`、标题 `→X2 Demo`、镜像 `lovehunter9/audiostudiox2demo:1.0.9`。
- **改造**：模型发现从「按 mode」改为「`mode=audio` + `supports` 含能力」——`types.ts` 加 `AUDIO_MODE`/`CAP_SUPPORT`（`embed→speaker_embed`）+ `ProviderModel.supports`；`api.ts` 从 `model_spec.supports` 抽键；`App.tsx` `modelsByCap`/`modelServesCap` + 默认按能力伪-mode + 状态按能力计数；`stream.tsx` 流式按 supports 含 `stt_stream`/`diar_stream`。
- **翻译留位不接**：`translate` 仍在 UI，但无音频 provider 声明 `translate` → 发现不到模型 → 显示「无可用模型」占位。
- provider 注册改由网关 olares_sync 自动 seed（mode=audio+supports），demo 只发现+调用。
- 前端 `npm run build`（tsc -b + vite）**通过**（复用原版 node_modules 绕开 npm 网络卡顿）；chart `helm template` 渲染 1 Deployment+1 Service。
- 待办：demo 镜像 `lovehunter9/audiostudiox2demo:1.0.9` 未构建推送（部署时做）；联调需网关上线 + provider 就绪。

---

## 12. 网关上线 + 端到端验证（2026-07-21 晚，真机手动部署）

**镜像**：backend `lovehunter9/llm-gateway-backend:v2.0.7-audiobase1`（digest ccd3f33a）、frontend `…-frontend:v2.0.7-audiobase1`（df70fa0b）；用户手动 roll 到 `llmgatewayv3-shared`。

**DB 处理（真机 citus，库 llmgatewayv3_shared_llmgatewayv3，pod os-platform/citus-0）**：
- 线上停在废弃 stt_relative 的 `schema_migrations`=21。手动 SQL：删所有旧音频 mode 行（provider_models 11 / system_default 9 / tenant 0）+ 删非 running olares 孤儿外壳 3 + 把三张表 mode CHECK 直接改成 audiobase 目标态（含 `audio`）。
- **应用户要求，真机验证 000021 本身可跑**：`UPDATE schema_migrations SET version=20` → scale 后端 0→1 → 后端开机日志 `"from_version":20,"to_version":21,"migrations applied"`（dirty=false），audiobase 000021 在真机+真实数据上干净跑通。
- 又按名字精准删了 11 个旧音频 provider（`*-audiolabxv3`/`*-audiolabx2v3`，各带 2 个 mode=chat 占位 model）：`DELETE 22`（model）+`DELETE 11`（provider）。剩余 provider 仅 chat-qwen3-4b、embedding-qwen3-0-6b。
- 教训：旧音频 provider 的 model 是注册成 `mode=chat` 的占位行，不是 vad/stt——按 mode 删删不掉，必须按 provider 名删。

**auto-seed 现状**：olares_sync 正常轮询但 market `appstore-svc:81` 返回 404（该环境无 `/app-store/api/v2/llm-providers`），故自动发现拿不到数据、优雅重试不报错。→ **provider 走手动注册**（与 chat/embedding 在此环境一致）。auto-seed 代码保留（前瞻，未来 market 端点补上即可用）。

**手动注册 provider（浏览器控制台脚本，走 SSO cookie；`/console/api/providers` + `/customizable-models`）**：
- provider `audio-qwen3asr-audiovllmbasexv3`，openai_compatible，base_url=**模型 share 入口** `http://7d16905a.shared.olares.com/v1`（新版走 share/内网，绕公网边缘超时）。
- model `Qwen/Qwen3-ASR-1.7B`，`mode=audio`，`supports={stt:true,stt_stream:true}`（新版关键：落 model_spec.supports，端点门控靠它）。
- psql 核对：`Qwen/Qwen3-ASR-1.7B | audio | {"stt":true,"stt_stream":true} | audio-qwen3asr-audiovllmbasexv3 | http://7d16905a.shared.olares.com/v1` ✓。

**端到端验证（经网关公网 `https://cdc01162…` + 数据面 Bearer key，全绿）**：
- `GET /v1/models` → 含 `Qwen/Qwen3-ASR-1.7B` ✓
- 离线 `POST /v1/audio/transcriptions` → 全文正确 ✓
- 流式 `WS /v1/audio/stream` → ready→11 partials→final 正确 ✓
- 门控 `POST /v1/audio/diarization`（带文件）→ **404 capability_not_supported**（"mode=audio 未声明 diar"）✓

**结论**：线 C（llm-gateway audiobase）+ 手动注册在真机端到端跑通；mode=audio + supports 门控生效；000021 真机可跑坐实。下一步：audiostudiox2demo 镜像构建 + 联调；其余基座上传/CLONE。

---

## 13. 明日计划（信号词：用户说「开工」即开始）

**目标：争取全部齐活。** 三件事：

1. **把各基座与模型恢复/建起来**（都走 mode=audio + supports，手动注册 provider，base_url 用各自 share 入口 `http://<hash>.shared.olares.com/v1`；share URL 用户手动找、我问用户要，勿私自签发 KEY）：
   - `audiovllmbasexv3`：stt（whisper/qwen）、stt_stream、align（Qwen3-ForcedAligner）——已跑通 stt+stt_stream 的那个实例；按需再 clone stt-only / align。
   - `audiopyannotebasexv3`：vad / diar / speaker_embed / enhance（chart 已备，需 package→upload→clone→注册）。
   - `audiofasterwhisperbasexv3`：stt（CT2 whisper）。
   - `audionemobasexv3`：diar_stream。
   - **翻译（translate）**：用户去找别的同事要模型/方案；**要不到就先留空**（demo 里位置已留，网关侧本就不接 translate）。
2. **把 `audiostudiox2demo` 架起来测稳**：构建+推 `lovehunter9/audiostudiox2demo:1.0.9` 镜像（app+chart+前端 dist 已就绪）→ 安装 → 填网关 URL/key → 按 supports 发现模型 → 逐能力联调（转写/流式/对齐/vad/diar/embed/enhance；翻译占位）。
3. **新建并搞定 `audiominutesx2demo`**（尚未创建；仿照 audiostudiox2demo 的做法：复制 `audiominutesxdemo`→x2、改成 mode=audio + supports 选模型、翻译留位、前端 build、镜像、安装联调）。

**当前就绪清单（明天可直接用）**：
- 网关 `v2.0.7-audiobase1`（前后端）已上线并验证；provider `audio-qwen3asr-audiovllmbasexv3`（mode=audio, supports stt+stt_stream）已注册、端到端通。
- 4 个 base chart 源码就绪：audiovllmbasexv3（已 clone fd501a 在跑）、audiopyannotebasexv3 / audiofasterwhisperbasexv3 / audionemobasexv3（helm render + wrapper py_compile 均过，未 package/upload）。
- audiostudiox2demo：app+chart+前端 dist 就绪、`npm run build` 过、helm render 过；**镜像未构建**。
- audiominutesx2demo：**未创建**。
- 手动 SQL 脚本 `llm-gateway/deploy/manual/audiobase_clear_stt_relative_000021.sql` 已在真机用过（幂等，可参考）。
- 关键坐标：网关 ns `llmgatewayv3-shared`、DB pod `os-platform/citus-0`、库 `llmgatewayv3_shared_llmgatewayv3`、citus 中间件 `os-platform`；音频实例 ns `audiovllmbasexv3fd501a-shared`、share 入口 `http://7d16905a.shared.olares.com`。
- 规矩：部署/DB 操作在 Olares 机器上用 `k`（olares-cli 不能 exec/连库）；provider 手动注册（此环境 market 404，auto-seed 不生效）；KEY 严禁私自签发，问用户要；公网/ share URL 问用户要。

---

## 14. 2026-07-22 · 离线长音频 500 根治 + 实例瘦回 12Gi

**症状**：双能力实例（Qwen stt+stt_stream）离线转写，>~10min 音频直接 500；旧版（`vllm serve` 那条）不这样。

**真因（引擎侧，非超时/入口）**：qwen-asr in-process vLLM 的**音频 encoder cache 仅 8192 tokens（≈600s @ ~13.65 tok/s）**。我把离线改成"整段原生 `asr.transcribe(whole)`、靠包内 1200s(20min) 才切"，15min(12282 tokens)没到 1200s 不切、整段喂进去 → `ValueError: exceeds pre-allocated encoder cache size 8192` → 500。（encoder cache ≠ decoder 的 max_model_len，我之前混为一谈。）

**修复**：加载 qwen-asr 后 **monkeypatch `qwen_asr.inference.qwen3_asr.MAX_ASR_INPUT_SECONDS`（+utils）= 540s**（env `OFFLINE_MAX_INPUT_SEC`）。这样包**自己**把任意长度静音感知切成 ≤540s(~7.4k tokens<8192) 块、`"".join` 合并（切点在低能量处、合并无分隔符 → 无接缝无多空格）。离线路径仍是**整段一次 `asr.transcribe(whole)` 调用**，零我方切分/拼接代码。3h = 被包切成 ~20 块自动合并。

**顺带瘦身**：既然单次只处理 ≤540s 块，`max_model_len` 不再需要 65536 → 回 **16384**（engine.yaml 按 GPU 档位推导：12Gi→16384）。GPU 从 18Gi **降回 12Gi**（旧 stt_stream 本就 12Gi 跑通）。off_max=4096。

**验证（直连公网入口，全 200 无 500）**：79s→38s；**12min(713s,~9.7k tokens,旧必500)→200/131s**；**26min(1585s,~3块)→200/204s**，首尾完整、多块合并无接缝。

**新坐标（覆盖旧的 fd501a/7d16905a/18Gi）**：
- 实例 `audiovllmbasexv3fd501a`（12Gi 重 clone **复现同 hash**）；ns `audiovllmbasexv3fd501a-shared`。
- **公网入口 `https://d4831e40.olarestest003.olares.com`**（用户给）；provider `audio-qwen3asr-audiovllmbasexv3` 已由用户**重指到该公网 URL**。
- 配置：`STREAM_MAX_LEN=16384` / `VLLM_GPU_UTIL=0.43` / encoder cache 8192 / 窗口 540s / `MODEL_SUPPORTS=stt,stt_stream`。

**超时排查定论（平台侧，非本项目 chart/代码）**：
- 公网入口 `*.olarestest003.olares.com`：`apiTimeout:0` → envoy 上游超时 ~300s，长转写能扛（26min/204s 通过）。
- 共享入口 `*.shared.olares.com`（如 `8fd3f3890…`）：这台机器上超时被设得很短（~25s）→ 长请求 50X。而 **Olares 当前平台源码**（`app-service/pkg/gateway/timeout/timeout.go`：`DefaultSharedBackendResponseTimeout=10min`、`ClampResponseTimeout` 抬到 600s）**本应把共享路由 clamp 到 ≥600s** → 这台是 app-service 版本落后/未 clamp，**平台侧问题**。抬法：给该 shared HTTPRoute 打 `app-service.olares.com/timeout-floor` 注解 / BackendTrafficPolicy `requestTimeout` / 升 app-service（都用 `k`，非 chart）。
- **chart 接线已核对与 llamacpp 一致**（`sharedentrances-api` Service :80→llm-init:8090、`download-svc` :8090），无缺失。
- 结论：**引擎侧 500 已根治，长度上限现在只由平台入口超时决定**（3h 端到端需先解平台超时；demo 走公网、≤~26min 已验证可用）。

**下一步**：clone 出本基座（vLLM）支持的其余模型 → 见 §15。

## 15. 2026-07-22 · 本基座(vLLM)全模型 clone 就绪

本基座 = 纯 vLLM，支持 **stt / stt_stream / align**。旧 12 实例里属于本基座的模型全部 clone 完毕、引擎健康、GPU 塞下（物理 24Gi，与 chat/embedding 共享）：

| 实例 app | 模型 | MODEL_SUPPORTS | GPU | ns | 引擎健康 |
|---|---|---|---|---|---|
| `audiovllmbasexv3fd501a` | `Qwen/Qwen3-ASR-1.7B` | stt,stt_stream | 12Gi | `…fd501a-shared` | ✓（公网 `d4831e40`，provider 已注册/重指） |
| `audiovllmbasexv36ab52b` | `Qwen/Qwen3-ForcedAligner-0.6B` | align | 4Gi | `…6ab52b-shared` | `/healthz` 200 |
| `audiovllmbasexv39ecddb` | `openai/whisper-large-v3` | stt | 8Gi | `…9ecddb-shared` | `vllm serve /health` 200 |

clone 配方（`market clone audiovllmbasexv3 -s upload`，RULE-2 五项）：
- Align：title `Audio vLLM Align` / SOURCE `hf://Qwen/Qwen3-ForcedAligner-0.6B` / NAME 同 / SUPPORTS `align` / 4Gi。
- Whisper：title `Audio vLLM Whisper Large V3` / SOURCE `hf://openai/whisper-large-v3` / NAME 同 / SUPPORTS `stt` / 8Gi。

**不属本基座（另建）**：faster-whisper(stt,CT2)→`audiofasterwhisperbasexv3`；vad/diar/speaker_embed/enhance→`audiopyannotebasexv3`；diar_stream→`audionemobasexv3`；translate→文字模型侧。

**待办**：给 `6ab52b`(align)、`9ecddb`(whisper) 注册网关 provider（mode=audio+supports；需各自公网 URL，问用户；base_url 用公网入口）。然后 audiostudiox2demo 逐能力联调。

## 16. 2026-07-22 · Align 并发 ECONNRESET 根治（align.py 阻塞事件循环）

**症状**：demo 联调，Whisper STT 正常（200/127s/45266字），但 Align 多路请求 `gateway proxy error: socket hang up / ECONNRESET @~47s`。引擎日志却是一堆 `POST /v1/audio/align 200 OK`（引擎没崩）。

**真因**：`align.py` 的处理器把 `_state["model"].align(...)` **同步直调在 async 事件循环里**（不像 stream.py 用 `to_thread`+锁）。对齐一块要 30~110s（vGPU 时分、3 音频实例分卡后更慢），期间**整个 uvicorn 事件循环被冻住** → demo 并发(2)的第 2 路请求 body 都读不到 → 空闲 socket 被入口在 ~47s RST。STT 是"单条连续请求"127s 不空闲所以不被掐；align 是"整段切 14×≤290s、并发 2"，慢块把并发 partner 拖过 47s。**与 demo 并发无关（新旧 demo 都并发 2）、与引擎崩溃无关。**

**修复**（`align.py`）：`import asyncio` + 模块级 `_align_lock = asyncio.Lock()`；把 `.align()` 包进 `async with _align_lock: await asyncio.to_thread(_do_align)`。事件循环不再被卡 → 并发请求都能被读取并像正常长请求一样撑住、按需排队。

**部署（严格 RULE-1 完整循环，吸取教训）**：同版本不 delete 的 re-upload **不刷新**（之前想跳 delete 被用户逮到）。正确做法：`uninstall 全部 3 clone → market delete audiovllmbasexv3 → upload 新 tgz → 重 clone 3 个`。三个 title+env 不变 → **hash 全复现**（fd501a/6ab52b/9ecddb）→ 公网 URL 不变（d4831e40/a2995f93/2f63127e）→ **provider 不用重指**。

**验证**：直连 `a2995f93` 打 **2 路并发** `/v1/audio/align`（带 `language=en`）→ 两路都 **200**、逐词时间戳正确、**无 ECONNRESET**（旧版必掐一路）。三实例 `/healthz` 200、model-spec 正确。

**注**：本次只改 `align.py`；fd501a 的整段 500 修复（stream.py）未受影响，重 clone 内容一致、行为不变。

## 17. 2026-07-22 · pyannote 基座（支持最多能力）建起 + 4 模型 clone

**自检（第一基座改动是否要跟随）**：pyannote 的 vad/diar/embed/enhance handler 也是"async 里同步调模型"，但 **demo 对它们都是单次/串行调用**（embed 明确 `POST ×N(串行)`）→ 不像 align 并发 → **不会触发 ECONNRESET → 本版无需跟修**。真正会踩的是将来 faster-whisper 基座（STT 扇出并发 → `stt_fw.py` 要改 to_thread）。STREAM_MAX_LEN/离线切窗/12Gi 等是 vLLM/qwen 专属，不适用 pyannote。

**文案修正**：pyannote 的 OlaresManifest `description`/`fullDescription`/`upgradeDescription` 原是从 vLLM 基座拷来的错文案（写着"vLLM engine / stt / stt_stream / align"），已改写为 pyannote 正确内容（vad/diar/speaker_embed/enhance、pyannote+speechbrain 引擎、4 组 clone 示例、gated 说明）。

**GPU 教训**：卡是**时分 vGPU，gpumem 配额可超售**（老早 12 实例一卡同跑）；我一度误判"卡满要腾显存"，被用户纠正。**不停任何东西，4 个直接 clone**。

**部署（package→upload→clone ×4，全 running + 引擎 loaded）**：

| 实例 app | 模型 | MODEL_SUPPORTS | GPU | ns | 引擎日志 |
|---|---|---|---|---|---|
| `audiopyannotebasexv3029777` | silero-v5 (`onnx-community/silero-vad`) | vad | 0 | `…029777-shared` | silero-vad loaded; ready |
| `audiopyannotebasexv3f98a8d` | pyannote-community-1 (`pyannote/speaker-diarization-community-1`, gated) | diar | 4Gi | `…f98a8d-shared` | pipeline loaded on cuda |
| `audiopyannotebasexv38b9c8a` | pyannote-embedding (`pyannote/embedding`, gated) | speaker_embed | 0 | `…8b9c8a-shared` | embedding loaded on cuda |
| `audiopyannotebasexv3544ba0` | mtl-mimic-voicebank (`speechbrain/mtl-mimic-voicebank`) | enhance | 2Gi | `…544ba0-shared` | speechbrain loaded 'waveform' on cuda:0 |

gated 的 diar/embed 下载成功 → 用户 Olares HuggingFace 集成 token 生效。

**provider 注册（脚本 `audiopyannotebasexv3-register-providers.js`，用户跑）+ 网关端到端验证（全 200）**：
| provider | 公网 base_url | model | supports | 网关抽验 |
|---|---|---|---|---|
| audio-vad-audiopyannotebasexv3 | `https://7c7654b4…/v1` | silero-v5 | vad | 200 · 12 段/75.4s |
| audio-diar-audiopyannotebasexv3 | `https://9b4d7dd3…/v1` | pyannote-community-1 | diar | 200 · 1 spk/20 段 |
| audio-embed-audiopyannotebasexv3 | `https://48109140…/v1` | pyannote-embedding | speaker_embed | 200 · dim 512 |
| audio-enhance-audiopyannotebasexv3 | `https://fa58c7b8…/v1` | mtl-mimic-voicebank | enhance | 200 · audio/wav 2.5MB |

**里程碑**：网关端到端已提供 **7 个 audio 能力**：`stt`(Qwen双+Whisper) / `stt_stream`(Qwen双) / `align` / `vad` / `diar` / `speaker_embed` / `enhance`。剩 `diar_stream`（NeMo 基座 `audionemobasexv3` 未 package/clone）+ `translate`（文字侧，暂缓）。7 个 provider 全用公网入口 base_url。

**下一步**：audiostudiox2demo 逐能力联调（转写/流式/对齐/vad/diar/embed/enhance，翻译占位）；按需再建 NeMo(diar_stream)、faster-whisper(stt CT2；注意 stt_fw.py 需 to_thread 跟修) 基座。

## 18. 2026-07-22 · faster-whisper + NeMo 基座建起（stt_fw.py to_thread 跟修）

**自检跟修**：faster-whisper 的 `stt_fw.py` 原是 handler 直接同步 `_run()`（`transcribe()`+`list()` 阻塞事件循环），而 demo STT 扇出是**并发**的 → 会踩 align 同款 ECONNRESET。已改：`import asyncio`；`_run(task,data,filename,…)` 收 bytes；handler `data=await file.read()` 后 `await asyncio.to_thread(_run,…)`。**不加锁**——CTranslate2 线程安全、并行解码正是本基座的意义，加锁会掐死扇出。NeMo `diar_stream.py` 本就全程 `_infer_lock`+`to_thread`，无需改。

**部署（package→upload→clone，全 running + /healthz 200）**：
| 实例 app | 模型 | MODEL_SUPPORTS | GPU | ns |
|---|---|---|---|---|
| `audiofasterwhisperbasexv311be67` | Systran/faster-whisper-large-v3 | stt | 6Gi | `…11be67-shared` |
| `audionemobasexv3b7ca1e` | diar-streaming-sortformer (`nvidia/diar_streaming_sortformer_4spk-v2.1`, 公开) | diar_stream | 6Gi | `…b7ca1e-shared` |

**里程碑**：除 translate（同事模型未给）外，**8 个 audio 能力全部有实例**：stt / stt_stream / align / vad / diar / speaker_embed / enhance / diar_stream。

**待办**：给 fw(stt)、nemo(diar_stream) 注册网关 provider（需公网 URL，问用户）。translate 等同事模型。

## 19. 2026-07-22 EOD · 翻译打通（临时走网关）+ demo 翻译改造 + 网关两次 commit

**翻译模型（同事给的，非 clone，直接上传安装）**：`tencent/Hy-MT2-1.8B-GGUF:Q4_K_M`（llama.cpp，API 格式 = **Translate**）。app `llamacpphymt218bggufv3`（1.0.11，同事之前上传过旧版、用户点了更新）。公网入口 `https://aea0358b.olarestest003.olares.com`。端点在 **host root**（非 /v1）：`POST /translate {from,to,text}→{result}`、`POST /translate/batch {from,to,texts[]}→{results[]}`、`GET /languages`、`POST /detect`；语言码 MTran（zh-Hans/en/…）；`MODEL_SUPPORTS=supports_translate`。文档 `translate-api.md`。

**网关为何要临时改**：网关**故意不接 translate**（`router.go`:“translate is intentionally NOT here”），且 supports 白名单无 `translate` 键，且这模型是 Translate 格式非 OpenAI → 现成数据面到不了它。用户决定：**临时在网关加 translate 透传，单独 commit，后续废弃**。

**llm-gateway 两次 commit（分支 feat/audio-base）**：
- `6a95d61` `feat(backend): audio data plane …` —— 把本轮 audiobase 全量工作提交干净（/v1/audio/* + ModeAudio + supports + AudioPassthrough + olares_sync + 000021 + 计量/nginx/Dockerfile）。
- `f1bcbec` `TEMP(backend): translate passthrough … (REVERT LATER)` —— 自包含临时透传：`translate_temp.go`（4 个 handler：`?model=` 解析→转发 method+body 到 base_url+path）+ router 注册 `/v1/translate`、`/v1/translate/batch`、`/v1/languages`、`/v1/detect`。**这条 commit 明天准备 PR 时删掉。**
- 后端镜像已 build+push：**`lovehunter9/llm-gateway-backend:v2.0.7-audiobase2`**（linux/amd64，VERSION 注入 audiobase2）。**用户尚未 roll**。

**translate provider 注册（脚本 `translate-hymt2-register-provider.js`，用户 roll 后跑）**：openai_compatible、**mode=chat**、**base_url=host root（不带 /v1）**、`model=tencent/Hy-MT2-1.8B-GGUF:Q4_K_M`、`supports={translate:true}`（未知键被保留为 `_unknown_flag`，demo 靠它发现）。

**demo 翻译改造（audiostudiox2demo，已 build 过、`index-C3nVwfyU.js`）**：
- `api.ts`：`translate(s,model,from,to,text)`→`/v1/translate?model=`→读 `{result}`；新增 `translateBatch(s,model,from,to,texts[])`→`/v1/translate/batch?model=`→读 `{results[]}`。
- `App.tsx`：加 `FLORES_TO_MTRAN`/`floresToMtran`（zho_Hans→zh-Hans、eng_Latn→en、jpn_Jpan→ja、kor_Hang→ko、fra_Latn→fr、spa_Latn→es）；**整段→`translate` 一次**、**分段→`translateBatch` 一次**（取消旧的 per-line 并发）；`modelServesCap` 放宽为“supports 含该键即可（任意 mode）”，让 mode=chat 的翻译模型能被发现；纯文本翻译路径同步改；`summarize` 读 `result`。out.translate 兼容旧读法（同时写 `text`+`translation`）。
- 遗留：翻译并发滑块现为空操作（批量一次调用），可后续删。

**明天开工先做的 3 步（验翻译完全体）**：① roll 网关后端到 `v2.0.7-audiobase2`（无新 migration，直接换镜像）；② 跑 `translate-hymt2-register-provider.js`；③ 硬刷新 demo(8080)。然后验整段/分段翻译。

---

## 20. 明日计划（信号词「开工」）

1. **audiominutesx2demo**：新建（仿 audiostudiox2demo：复制 audiominutesxdemo→x2、mode=audio+supports 选模型、翻译走 `/translate`+`/translate/batch`、前端 build、本地起服务联调）。
2. **两个新 demo 打镜像并部署**：`audiostudiox2demo` + `audiominutesx2demo` 各 build+push 镜像 → 用户手动安装/部署到测试机。
3. **llm-init + llm-gateway 基于各自最新 main 做必要调整与测试**，然后**把最终保留的部分整理成 PR**（注意：llm-gateway 的 `f1bcbec` TEMP translate commit 是临时的，PR 前按方案取舍——要么剔除、要么换成正式 translate 设计）。

**当前完整台账（明天直接用）**：
- 网关 ns `llmgatewayv3-shared`（pod `llmgatewayv3-7bf54d479b-*`，前端+后端两容器）；DB pod `os-platform/citus-0`，库 `llmgatewayv3_shared_llmgatewayv3`。网关公网 `https://cdc01162.olarestest003.olares.com`。当前后端 `v2.0.7-audiobase1`（待 roll 到 audiobase2）。
- **音频实例（9 个，均 running/healthy，公网入口 base_url 走 provider）**：
  - Qwen3-ASR 双能力 `audiovllmbasexv3fd501a`（stt+stt_stream，12Gi，公网 `d4831e40`）— 离线整段 500 已根治（窗口 540s/`STREAM_MAX_LEN=16384`）。
  - Align `audiovllmbasexv36ab52b`（align，4Gi，`a2995f93`）— align.py 已 to_thread+锁。
  - Whisper `audiovllmbasexv39ecddb`（stt，8Gi，`2f63127e`）。
  - VAD `audiopyannotebasexv3029777`（`7c7654b4`）/ Diar `…f98a8d`（`9b4d7dd3`）/ Embed `…8b9c8a`（`48109140`）/ Enhance `…544ba0`（`fa58c7b8`）。
  - faster-whisper `audiofasterwhisperbasexv311be67`（stt，6Gi，`7793a7fe`）— stt_fw.py 已 to_thread。
  - NeMo `audionemobasexv3b7ca1e`（diar_stream，6Gi，`e9f5a47f`）。
  - 翻译 `llamacpphymt218bggufv3`（chat+translate，`aea0358b`）。
- **provider（网关，全公网 base_url，除翻译 host-root）**：audio-qwen3asr / audio-align / audio-whisper / audio-vad / audio-diar / audio-embed / audio-enhance（7 个已注册验证过 200）+ 待注册：audio-fasterwhisper、audio-diarstream、translate-hymt2。
- **能力覆盖**：stt / stt_stream / align / vad / diar / speaker_embed / enhance / diar_stream（引擎+实例全就绪）+ translate（模型就绪，待 roll 网关+注册）。
- **超时/GPU 教训**：公网入口 `apiTimeout:0→~300s`（envoy stream_idle 300s 硬顶，chart 改不动）；share 入口这台机短超时（平台版本旧，`k` 抬 timeout-floor 可修，非本项目）；卡是时分 vGPU、gpumem 可超售但**同时多路推理会 thrashing**→ demo `api.ts` 全局并发信号量 `MAX_DATA_INFLIGHT=2`、align 并发=1/重试=4。
- **脚本**：`audiovllmbasexv3-register-providers.js`、`audiopyannotebasexv3-register-providers.js`、`audio-fw-nemo-register-providers.js`、`translate-hymt2-register-provider.js`。
- **规矩**：部署/roll/DB 用户手动（AI 只本地代码+打包+本地起服务+olares-cli upload/clone）；provider 用浏览器控制台脚本（此环境 market auto-seed 404）；公网/share URL 问用户；KEY 严禁私自签发。

# Per-workload replica counts (keys match workloadReplicas in OlaresManifest):
#   audiopyannotebasex2v3 = engine Deployment (== Release.Name), llminit = llm-init proxy Deployment.
workloads:
  audiopyannotebasex2v3:
    replicaCount: 1
  llminit:
    replicaCount: 1

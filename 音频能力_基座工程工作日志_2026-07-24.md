# 音频能力 · 基座工程工作日志 — 2026-07-24

## 口令

**「预备，跑」= 打镜像。** 现走 **个人仓库 CI**（本地打会被代理卡死，见第二节）：

```
gh workflow run qwen-ci.yml --repo lovehunter9/audio-engines --ref feat/audio-stt \
  -f namespace=lovehunter9 -f image_tag=audio-stt-testN
```

- 产物：`docker.io/lovehunter9/audio-qwen:audio-stt-testN`（linux/amd64）。
- `gh` 走代理，`olares-cli` 不走代理，两者别混在一条命令里。
- 底座 `beclab/vllm-vllm-openai:v0.23.0-cu129` 10GB+，CI 一轮约 30 分钟。

---

## 一、今日已完成

### 1. 新引擎工程 `~/beclab/audio-engines`（GitHub `beclab/audio-engines`，**Private**）
- 分支策略：`main` **空**（占位 commit）；全部代码在 **`feat/audio-stt`**（当前分支，已 push）。
- 文件：
  - `wrapper/gpu.py` — 通用 `/metrics`，输出 llm-init 逐字要的 `gpu_present / gpu_mem_used_bytes / gpu_mem_total_bytes / gpu_util_ratio`。
  - `wrapper/contract.py` — `/v1/models`(id+mode+supports+endpoints) + `/health|/healthz|/readyz`；**ready 前 503、ready 后才 200**（对齐 llama-server 语义，修正探活）。
  - `wrapper/app.py` — 读 `MODEL_SUPPORTS` 派发 cap（stt 家族 / align 互斥）。
  - `wrapper/caps/stt_stream.py` — 忠实 port `stream.py`：一次加载同供离线 `asr.transcribe()` + 流式 `streaming_transcribe`（保留 batch/roll/`_infer_lock`）。
  - `wrapper/caps/align.py` — 忠实 port `align.py`（保留 batch/single/`_align_lock`，守护线程加载）。
  - `bases/qwen/Dockerfile` — FROM vllm cu129，**构建期**装 qwen-asr/soundfile/librosa/av/websockets/python-multipart/fastapi/uvicorn（含 blinker 修复），COPY wrapper，`CMD python3 -m wrapper.app`。
  - `.github/workflows/qwen-ci.yml` — 照 llm-gateway：`workflow_dispatch + push[main] + tags v*`，`docker-build` 推 `beclab/audio-qwen`（PR 只 build）。
  - `Makefile` — 本地「偷偷打」，同一 Dockerfile 推 `lovehunter9/audio-qwen:<tag>`，逻辑零重复。
  - `README.md` — 英文；含 Engine contract 表、Bases 表（qwen=active，fasterwhisper/pyannote/nemo=planned）、Build（CI + local）、Adding-a-new-base 五步、Branching。**新增引擎要更新 Bases 表。**

### 2. 极简 chart `~/beclab/app-zips/audioqwenbasex2v3`
- 文件集**严格 = `llamacppllmbasev3`**（已 diff）：`Chart.yaml / OlaresManifest.yaml / owners / values.yaml / i18n/{en-US,zh-CN} / templates/{_helpers.tpl, engine.yaml, llm-init.yaml, wrappers.yaml}`。**无 `docker-compose.yml`，补 `i18n/`**。
- `engine.yaml`：瘦身（删 primaryCap 三选一 / pip 巨串 / pydeps），留 GPU sizing + sentinel 壳；默认镜像 `docker.io/lovehunter9/audio-qwen:audio-stt-test2`。
- `wrappers.yaml`：薄 shell，等 `model_download_finish` → `exec python3 -m wrapper.app`。
- `llm-init.yaml`：镜像 `lovehunter9/llm-init:v1.3.2-audiobase7`，探针只在 llm-init 打 `/livez`，引擎零探针。
- helm 渲染验证过：12Gi→`gpumem 12288`、`VLLM_GPU_UTIL 0.43`、`STREAM_MAX_LEN 16384`、`MODEL_SUPPORTS=stt,stt_stream` 全对。

---

## 二、2026-07-25 进展：qwen 基座**已跑通并回归通过**

### 镜像
- 本地打镜像因代理下 BuildKit 拉不到 Docker Hub（`EOF`）而放弃，改走 **个人镜像仓库 CI**：
  `lovehunter9/audio-engines`（Private，副 remote 名 `mine`），`workflow_dispatch` 传 `namespace` + `image_tag`。
- CI 需在个人仓库配 `DOCKERHUB_USERNAME` / `DOCKERHUB_PASS` 两个 secret（已配）。
- runner 磁盘不够 → workflow 加了「清预装 toolchain + 把 docker data-root 挪到 `/mnt`」。
- 产物：`docker.io/lovehunter9/audio-qwen:**audio-stt-test2**`（test1 是首版，端点自报有瑕疵）。
- 本地 buildx builder / 镜像 / 卷已清干净。

### 端点自报的两处修正（test1 → test2）
- `wrapper/contract.py`：`/v1/models` 自报里**自动带上 `GET /v1/models`** 自身。
- `wrapper/caps/stt_stream.py`：`/v1/audio/stream` 的 method 从 `GET` 改为 **`WS`**。
- `/v1/audio/translations`：**按用户决定不提供**（Qwen3-ASR 只转写，不做翻译；翻译交 faster-whisper 基座）。旧 vLLM 那版只是个「翻译名义、实际转写」的兼容壳，不再沿用。

### 部署与回归（RULE 1 全流程走了一遍）
- 卸旧 clone → 删 chart → `tar -czf` 打包（`olares-cli market upload` 只吃 `.tgz`，**不吃 zip、无 `--path` 参数**）→ 重新 clone。
- 标题与 4 个 env 完全复用，实例名复现为 **`audioqwenbasex2v39319c8`**，公网 URL 不变。
- 回归结果（全绿）：
  - `GET /v1/models` → 200，自报 `Qwen/Qwen3-ASR-1.7B` / `mode=audio` / `supports=[stt,stt_stream]` / 三个端点（含 WS）。
  - 离线转写中英各一条，文本完全正确。
  - 流式 WS 中英各一条，9 个 partial + FINAL 正确。
  - `GET /api/diag/gpu`（llm-init 中继）→ `source=engine_metrics`，vram 8.28GB / util 0.33。

### align 实例（qwen 基座第二个能力，同一镜像换 `MODEL_SUPPORTS`）
- 实例 `audioqwenbasex2v3e71e2d`，标题 `Audio Qwen X2 Forced Aligner`，
  `hf://Qwen/Qwen3-ForcedAligner-0.6B` / `MODEL_SUPPORTS=align` / `4Gi`；模型走共享 hf-cache，起得很快。
- 自报正确：`supports=[align]`，端点 `GET /v1/models` + `POST /v1/audio/align`。
- 回归：英文单条（词级）、中文单条（字级）、英文 batch segments（切片相对时间）全部正确。
- **发现并修复一个老 bug（xv3 逐字同款，非本次回归）**：`Qwen3ForcedAligner.align()` 的 `language`
  是**必填**参数，不传时旧代码走 `align(path, text)` 兜底同样 TypeError → 500
  `missing 1 required positional argument: 'language'`。实测该参数对取值宽容
  （`auto`/`xx` 与显式 `en`/`zh` 结果逐字相同），故 `align.py` 改为**缺省 `language="auto"` 恒传**，
  兜底也改成位置传参。**需下一版镜像（test3）生效**；当前 demo 恒传 `language=auto`，线上不受影响。

### 已知环境坑
- `olares-cli` 走国内，**命令前必须 `unset *_proxy`**，否则 `EOF`。
- 对该实例的 HTTPS/WSS 偶发 `ConnectionResetError`，重试即可，非服务端问题。

---

## 二之三、2026-07-25：另外三个基座**代码已复制完成**（未打镜像、未验证）

### 通用改造（影响所有基座，qwen 也要跟着出新镜像）
- `wrapper/app.py` 从 qwen 专用改为**通用派发**：`ROUTES[AUDIO_BASE] = [(能力集, 模块)]`。
  同一个 `stt` 在 qwen 上是 qwen-asr、在 fasterwhisper 上是 CT2，所以**必须按基座路由**，
  `AUDIO_BASE` 由各自 Dockerfile 烤进镜像。11 组派发用例已本地干跑通过（含互斥告警、未知基座、空 supports）。
- 每个镜像新增 `/usr/local/bin/audio-python` 软链，指向**真正装了依赖的解释器**
  （NeMo 的依赖在 `/opt/venv`，不是 `python3`）。四个 chart 的 sentinel 脚本因此可以
  完全一致地 `exec audio-python -m wrapper.app`，不把镜像内情泄进 chart。
- CI 抽成**可复用 workflow** `.github/workflows/build-image.yml`（167 行只此一份），
  `<base>-ci.yml` 只留触发器/路径过滤 + `base`/`repo` 两个入参，约 45 行。
- 新增 `wrapper/audioio.py`：vad/diar/embed/enhance 三份重复的解码逻辑收敛成一份
  （soundfile → torchaudio → stdlib wave 三级兜底，绕开镜像里坏掉的 torchcodec）。

### fasterwhisper 基座
- `wrapper/caps/whisper.py`（port 自 `stt_fw.py`）：批量推理管线、`segments` 批量、
  `response_format`（json/text/verbose_json）、`/v1/audio/translations` 全部保留。
- `bases/fasterwhisper/Dockerfile`：`FROM beclab/harveyff-whisper-webui:v1.0.7`，
  **只加 web 层**（绝不重装 ctranslate2，那是 Blackwell 专门构建的），构建期断言
  `faster_whisper / huggingface_hub / torch / ffmpeg` 都在。
- chart `audiofasterwhisperbasex2v3`：`FW_BATCH_SIZE` 按显存档位推导（12Gi→16、6Gi→8、
  4Gi→4、CPU→4 且 `FW_DEVICE=cpu`），已逐档渲染验证。

### pyannote 基座
- `wrapper/caps/{vad,diar,embed,enhance}.py`：silero VAD 参数全量、pyannote 4/3 双写法
  兼容、embedding `window="whole"`、enhance 的三类推理类自动识别 + 长音频滑窗交叉淡化。
- `bases/pyannote/Dockerfile`：`FROM beclab/maximsachs-pyannote_fastapi:4.0.4`，把原先
  运行期 pip 的 `silero-vad / omegaconf / speechbrain` 全部烤进构建期。
- chart `audiopyannotebasex2v3`：`ENHANCE_CHUNK_S` 按显存推导（≥4Gi→120s、≥2Gi→60s、否则 30s）。

### nemo 基座
- `wrapper/caps/diar_stream.py`：真流式增量路径 + 有界窗口兜底路径、连接前静音自检、
  AOSC 标签稳定性、`_relabel` 重映射，逐字保留。
- `bases/nemo/Dockerfile`：`FROM beclab/nvidia-nemo:26.02`，依赖装进 `/opt/venv`（有 nemo 的那个解释器）。
- chart `audionemobasex2v3`：新增 `DIAR_LATENCY_PRESET`（low/high/veryhigh），默认 `high`
  （约 10s 延迟，会议场景准确率更好且便宜约 18 倍），一个开关映射 5 个 `DIAR_*` 参数。

### 验证到什么程度
- 四个 chart `helm lint` + `helm template` 全绿，关键字段（镜像、command、env 推导、gpumem）逐个核对。
- wrapper 全量语法检查通过；`ruff --select F,E9,B006,B023` 干净（顺手删了 `stt_stream.py` 里一个未用 import）。
- 当时**尚未做**：三个新镜像都没打过，也没 clone 过实例——已在「二之五」补齐。

---

## 二之五、镜像出齐 + 九个实例全量重建（07-25 深夜）

### 最终镜像（全部 CI 出，个人镜像仓库 `lovehunter9`）

| 基座 | tag | commit | 说明 |
|---|---|---|---|
| qwen | `audio-stt-test4` | `260fc24` | align 的 language 修复、通用派发、audio-python |
| fasterwhisper | `audio-stt-test4` | `342acc8` | audio-python 改脚本 + transformers>=4.56 |
| pyannote | `audio-stt-test2` | `260fc24` | 四能力 |
| nemo | `audio-stt-test2` | `260fc24` | crane append 出（见下） |

- **nemo 爆盘的解决办法**：`beclab/nvidia-nemo:26.02` 压缩态 25.7 GB、解压 ~55 GB，
  而 GitHub runner 只有一块 ~72 GB 盘（`/mnt` 不是第二块盘，是同一块上的目录），
  `docker build` 光解包 base 就撑爆。改成 `scripts/append-image.sh`：用 `crane append`
  只上传几 KB 的 wrapper 层，其余 blob 在 registry 内部跨仓库挂载，`crane mutate` 改配置。
  磁盘开销=那个 tar，耗时 **39 秒**。`bases/nemo/append.env` 存在即自动切到这条路径（CI 与 Makefile 都认）。
  代价：构建期什么都跑不了（没有 RUN），所以上游镜像必须自带全部依赖，且**没有构建期自检**。

### 九个实例（RULE-1 全流程重建，台账见 `音频能力_基座实例台账.md`）
- 旧的 8 个 clone 连 chart 副本 + 4 个基座 chart 全部 uninstall/delete，重新 package/upload 后按台账 clone。
- 补上了第 9 个：`openai/whisper-large-v3` 从 vLLM 挪到 fasterwhisper 基座
  （wrapper 首次加载时把 transformers 权重转成 CT2 落到共享缓存，实测 **33 秒**，重启/兄弟实例复用）。
- fw 两个实例的 title 从 `... X2 LV3` 改成 `Audio FW X2 Systran/OpenAI Large V3`
  （原来是为压 30 字符上限硬缩的，含义不明）。**改名=换 hash=换公网 URL**，
  其余七个 title 未动、URL 照旧。
- 九个引擎的 `/v1/models` 全部稳定 200，模型均已加载。

### 这一轮踩的两个坑（都出在 fasterwhisper，记下来别再犯）
1. **`audio-python` 不能做软链，必须是 shell 包装脚本**。这个基座的引擎装在
   `/Whisper-WebUI/venv` 里，而 CPython 判断自己在不在 venv 里，看的是**可执行文件同目录**
   有没有 `pyvenv.cfg`。`/usr/local/bin/audio-python` 这个软链旁边没有，于是按系统 prefix 启动，
   venv 的 site-packages 根本不进 `sys.path` → `ModuleNotFoundError: fastapi`。
   qwen/pyannote 的解释器不在 venv 里，所以同样的软链对它们无害——**只有 fw 会炸**。
   更要命的是构建期自检当时用的是 `$PY`（真实路径），**检查路径 ≠ 运行路径**，所以全绿放行。
   现在自检一律 `audio-python -c ...`，检查的就是 chart 真正执行的那条命令。
2. **transformers 必须 >= 4.56**。镜像里的 ctranslate2 调
   `from_pretrained(dtype=...)`，旧 transformers 不认这个 kwarg，会把它塞给模型构造函数，
   转 `openai/whisper-large-v3` 时报 `unexpected keyword argument 'dtype'`。
   4.56+ 同时接受旧的 `torch_dtype`，所以这个下限对任何 ct2 都安全。构建期已断言版本。

---

### 功能验证（九个实例全过）
明细见 `音频能力_基座实例台账.md` 的「功能验证结论」。要点：九个 `/v1/models` 自报全对；
qwen 的离线+流式+align、fw 两个实例的单段/批量/`translations`、pyannote 四能力、
nemo 流式分离都跑通；nemo 流式的四段结果与 pyannote 离线分离几乎重合，互为印证。
公网边缘偶有 SSL 断连，重试即过，与基座无关。

---

## 二之六、收尾状态（07-26 凌晨截止）

**已完成**
- 四个基座镜像全部 CI 出齐（qwen/fw `audio-stt-test4`，pyannote/nemo `audio-stt-test2`），无一本地推送。
- 九个实例按台账 RULE-1 全量重建，逐能力功能验证全过。
- gateway provider 的 `api_base` 已由**用户手动**改到九个新 URL（两个 demo 零改动）。
- 文档：本日志 + `音频能力_基座实例台账.md`（x2v3 世代复现清单/URL/验证结论）
  + `音频能力_示例模型与CLONE清单_2026-07-24.md`（已切到 x2v3，whisper-large-v3 改挂 fw 基座）。

**周一继续**
1. **代码同步到 beclab**：`feat/audio-stt` 目前只推了 `mine`，尚未推 `origin`(beclab)。
2. **可选规范化**：qwen/pyannote 的 `audio-python` 也统一成脚本、自检统一走它
   （它们现在不受影响，但改了就得重出镜像 + 重建 6 个实例，不急着动）。
3. **⚠️ 公网 URL 铁律**：cloned-app 的公网 entrance URL **AI 查不到**，**必须问用户要**。
   title+env 精确复现则 hash 不变、URL 也不变（本轮只有改了 title 的 fw 两个实例换了 URL）。

---

## 二之七、07-27：长音频瓶颈定位 + pyannote `audio-stt-test3`

起因：`audiominutesx2demo` 对 3h22m 音频 DIAR / ENHANCE 超时（1h05m 不超时，超时都发生在
用时过 5 分钟之后——平台 Envoy `stream_idle_timeout` 硬顶 300s）。

**引擎侧改动（只动 pyannote 基座，其余三个基座镜像未变）**
- `diar.py`：分段/嵌入批量化。pyannote 两级推理默认 `batch_size=1`，分段是 10s 窗 1s 跳，
  3 小时片子就是上万次小前向、GPU 在其间空转。批量按显存档位由 chart 推导
  （`DIAR_SEG_BATCH`/`DIAR_EMB_BATCH`：≥16Gi→32、≥8Gi→16、≥4Gi→8、CPU→1），
  属性用防御式探测（pyannote 改过名字），**CUDA OOM 单向回退到 1 并记住**（慢胜过 500）。
- `enhance.py`：推理包进 `torch.no_grad()`（被丢弃的 autograd 图原本是显存主要开销）、
  每窗计时日志、可选 `ENHANCE_AMP` 混精（默认关，掩蔽模型 fp16 有下溢风险）、
  新增 `format=wav|flac|ogg`（默认 wav 不变；编码器缺失依次回退 FLAC→WAV，响应带 `X-Audio-Format`）。
- `bases/pyannote/Dockerfile`：构建期打印 libsndfile 可用格式。实测镜像内 **libsndfile 1.2.2，
  FLAC / OGG-OPUS / OGG-VORBIS 全 True**，所以 Opus 真的可用。
- 全项目注释精简成单行；被删掉但有价值的知识挪进 `audio-engines/README.md`
  （diar_stream 线协议 + 三档延迟预设表、fasterwhisper 构建三个坑、`audio-python` 必须是脚本）。

**镜像**：`lovehunter9/audio-pyannote:audio-stt-test3` @ `sha256:ebc5bbda…`（CI 出，未本地推）。
其余三个基座这轮只改了注释，镜像未重打、chart tag 未动。

**RULE-1 循环**：卸载 4 个 pyannote 实例 → clone 副本随卸载自动消失 → 删基座 chart →
重打包上传 → 按台账 title+env 重 clone 四个。**app-name 与公网 URL 全部不变**
（`b8411d`/`084328`/`4eafad`/`09a372`），gateway provider 零改动。

**实测（引擎侧计时来自新加的日志，与 HTTP 总时长分离）**

| 能力 | 音频 | 引擎耗时 | HTTP 总时长（公网边缘） | 推算 3h22m 引擎耗时 |
|---|---|---|---|---|
| diar（4Gi→batch 8, cuda） | 60s | 3.1 / 4.3s | 13.9s | — |
| diar | 300s | 16.8 / 17.7s | 63.3s | **≈11.5 分钟** |
| enhance（2Gi→60s 窗, cuda） | 300s | **1.0s**（6 窗 × 0.2s） | 63.3s | **≈40 秒** |

`format` 实测（60s 音频，本地解码校验时长/采样率/峰值均正确）：
WAV 1875KB → FLAC 851KB（45%，无损、峰值逐位一致）→ OGG/Opus 181KB（**9.7%**）。

**结论（关键）**
- **ENHANCE 从来不是算力瓶颈**，3h22m 引擎只需约 40 秒；卡的是 payload——整段 16k 单声道 WAV
  约 388MB 要在 gateway/nginx 里整体缓冲。`format=ogg` 把响应压到约 1/10（≈38MB），
  `flac` 约 1/2.2。这条路子对 3h22m 是**对症**的。
- **DIAR 仍差一截**：批量确认生效（日志 `segmentation_batch_size = 8`、`loaded on cuda`），
  但 3h22m 引擎侧仍约 11.5 分钟 > 300s 硬顶。单靠调大 batch（8→16/32）要 2.3 倍加速，
  不太可能补上；下一步要么给 diar 更大显存档位再测，要么改成异步作业（提交→轮询）
  或走一条 WS 流式（有数据就不触发 idle timeout）。**这属于设计决策，等用户定。**

**功能回归**：VAD（15s，参数回显正常）、Embed（512 维）、Diar（1min 23 段 / 5min 109 段）、
Enhance（三种格式 + 300s 多窗），四个 `/v1/models` 自报正确。

**demo 侧接线（`audiominutesx2demo-app/server.js`，仅本地代码，镜像未出）**
- `gwAudioEnhance` 现在带 `format=ogg`，并按响应 `Content-Type` 决定落盘扩展名后返回真实路径
  （旧引擎不认 `format` 就回 WAV、缺编码器回 FLAC，ffmpeg 都能嗅探，向后兼容）。
- 加一行 `降噪增强返回 <文件> <大小>` 日志——这次要盯的就是返回体大小。
- 上游请求早就被 `transcodeToFit` 压到 16–64kbps mp3（3h22m 约 19kbps），返回后又统一转 64k mp3
  给下游，所以 Opus 这一层在既有链路里几乎不损失什么。
- **本地实测**（临时 DATA_DIR，base 直指 enhance 实例，未碰用户 config）：5 分钟音频
  返回 `…-enh.ogg` **0.9MB**（同片 WAV 是 9.6MB，**10.7 倍**），ffmpeg 转 mp3 成功、流水线继续
  （随后 diar 404 是因为 base 故意指向 enhance 实例，属预期）。推算 3h22m：约 36MB 取代约 388MB。
- `audiostudiox2demo` 的 enhance 在浏览器侧调用，本轮未动。
- demo 镜像：经用户当场授权本地打推 `lovehunter9/audiominutesx2demo:1.0.1`（linux/amd64 单架构，
  与 1.0.0 一致；arm 机器上走 QEMU 交叉编译，约 17 分钟，其中推送 13 分钟）。
  已核验镜像内 `server.js` 含 `ENHANCE_FORMAT` / `enhanceExt` / 新日志。
  chart `audiominutesx2demo` 的 `image` 指到 1.0.1 并重打包，**chart 版本仍锁 1.0.0**。
  部署由用户手动（保留数据卸载 → 删旧 chart → 上传新 chart → 重新安装）。
- 兼容性实测（新 pyannote 镜像，回答「不传 format 会不会坏」）：
  不带 `format` → 200 `audio/wav` + `x-audio-format: wav/pcm_16`，解码正常，与改动前一致；
  `format=mp3` → 400 `format must be one of flac, ogg, wav`；`format=opus` → `ogg/opus`（别名）。
  **决策：默认永远 WAV，不按体积自动降级**——静默换容器只在大文件上现形，是最难查的一类回归；
  而「只知道 WAV」的调用方在这个长度上本来就是超时，C 方案不让他多亏什么。
  `format` 的可选值与默认值由 `/v1/models` 的 endpoint description 自述，调用方可自行发现。

---

## 二之八、07-27 晚：改成**异步任务制**（四个基座已出镜像并重建实例）

### 缘起与思路（与老板对齐后的方向）

超时不再靠"把同步请求撑住"来绕，而是让**每个 HTTP 调用都变短**：能力接口发起任务并立刻返回，
再用通用任务接口查状态/进度。这样 60s（llm-init `ResponseHeaderTimeout`）和 300s
（Envoy `stream_idle_timeout`）两条线都碰不到，音频时长只受 OOM 和磁盘限制。

**先前那版"流式保活"外壳（`respond.py` + 四处接线）已整体回退**，工作区回到 `6f6ef26`；
异步任务制取代它。上周把推理挪进 `asyncio.to_thread` 那笔修复**不是白做的**——事件循环空着
正是本方案的前提（任务在工作线程跑，`/v1/models`、`/metrics`、任务查询照常应答）。

### 分期（用户决定）

1. **本轮（已完成代码）：只改基座**，改到可以"封代码"。同步接口表现与之前完全一致，用户正好可验。
2. 下一轮：改 llm-gateway + 两个 DEMO，正式测异步接口。

### 兼容性：opt-in，不传就是老行为

开关是 **multipart 表单字段 `async=1`**，不是 HTTP 头——因为网关 `AudioPassthrough` 只透传
Content-Type 和 body，**自定义头会被丢掉**，表单字段才活得下来。因此**提交路径网关零改动**
（202 + 任务 JSON 原样透传）；不传 `async` 时行为逐字节不变，硬要用老接口的用户零代价。

批量与异步是两个维度：`segments`、pyannote 批量、faster-whisper `BatchedInferencePipeline`
全部保留不动——批量是加速/质量，异步只决定"谁等"。

### llm-init：**零改动**（已验证）

`internal/dataplane/mount.go:108` 是 `mux.Handle("/v1/", instrumented)`，前缀通吃、不限方法，
`/v1/audio/tasks/...` 的 GET/DELETE 自动转发；端点清单本来就是基座自报。符合"llm-init 只改必须改"。

### 任务 API（`wrapper/tasks.py`，新增）

- `POST /v1/audio/<cap>` + `async=1` → `202 {"task":{id,cap,model,status,poll,result_url}}`
- `GET /v1/audio/tasks/{id}` → `queued`→`running`→`succeeded|failed|canceled`，
  `progress{ratio,stage,done,total}`，成功后 JSON 结果内嵌
- `GET /v1/audio/tasks/{id}/result` → enhance 给音频字节（响应头与同步路径一致），其余给同一份 JSON
- `DELETE /v1/audio/tasks/{id}` → 运行中在下一个检查点取消；已结束则丢结果
- `GET /v1/audio/tasks` → 全部在册任务（给人排障用）
- 五个能力端点 + 任务端点全部进 `/v1/models` 自报；nemo（纯流式）不挂任务 API

**单工作线程 FIFO**：同步与异步走同一个 worker（一实例=一模型=一块时分 GPU），所以不再需要
每能力的 `asyncio.Lock`（已删 4 处）。同步请求现在排在运行中的任务之后——与它原本排在
`_infer_lock` 之后完全等价，不是回归。

**进度是真的**：diar 用 pyannote 自己的 `hook=`（阶段名 + done/total，防御式回退到无 hook）、
enhance 按窗口、whisper 按音频位置（迭代生成器时报告）、批量按 segment 序号；vad/embed 本来就快，只给 0→1。

**取消是真的**：`ctx.checkpoint()` 在窗口/分段/hook 间隙抛 `Cancelled`（实测 20s 的活 2s 内落地）。

### qwen 基座两处专门处理

- WS 流式不走任务队列，但和离线任务共用同一份 vLLM → 加共享 `threading.Lock` 闸门
  （`asyncio.Lock` 跨不了工作线程），三处流式推理调用都套上。
- **ASR 启动卡死**：只让 vLLM 捕获 `1,2,4,8` 四个 CUDA graph 形状（默认原本约 80 个），
  推理这里恒为 batch 1，丢掉的形状本来也不会被 replay，**大幅缩短持 vGPU 锁的窗口**。
  字段名从 `CompilationConfig` **探测**而非假定（`cudagraph_capture_sizes` → `capture_sizes` → 放弃），
  探测失败就完全不动；`VLLM_ENFORCE_EAGER=1` 是彻底关图的逃生阀。
  ⚠ 诚实说明：这是**缩短暴露窗口 + 可自愈**，不是能证明的根治。

### 通用加载看门狗（`wrapper/watchdog.py`，新增）

引擎容器没有任何 k8s 探针，加载卡死没人发现 → `LOAD_TIMEOUT_S`（默认 1800s）内既没 ready
也没报错，就非零退出（70）让 k8s 重建。**加载失败并带原因的不退**（`/v1/models` 能看到原因，
比 CrashLoop 好查）。fasterwhisper 在 Dockerfile 里把默认值抬到 5400s——
`openai/whisper-large-v3` 首次要做 CT2 转换（数 GB 一次性工作），不能被误判成卡死。

### 一个自己发现的落盘泄漏

chart 把宿主 `appData/data` 挂到引擎的 `/tmp`，所以任务结果/上传是落在**宿主盘**上；
进程崩溃后这些文件永远没人清。→ 上传临时文件加 `upload-` 前缀、结果加 `task-` 前缀，
runner 启动时清扫上一次运行遗留的同名前缀文件（超过 TTL 的）。

### 本地验证（无 GPU，全部通过）

- `/tmp/task_smoke.py`（22 项）：自报、同步三态（成功/500 原文/二进制头）、202、中途进度、
  长任务期间 `/v1/models` 与 `/metrics` 照常 200、终态内嵌结果、`/result`、失败→409、
  取消、丢弃→404、队列满→503、同步排队、列表不留同步任务残留。
- `/tmp/caps_smoke.py`（七能力九端点）：**断言"异步结果 == 同步结果"逐字节相等**
  （含 whisper `verbose_json` / `text/plain`、align 单条+批量、qwen 单条+批量、enhance 二进制）。
- `/tmp/misc_smoke.py`：看门狗四种情形（卡死→70、正常→存活、失败→存活、`=0` 关闭）、
  vLLM 字段探测六种情形（新名/旧名/未知/导入失败/覆盖/eager 优先）。
- 阻塞扫描器 `/tmp/audit_block.py`：`async def` 内**0 处**阻塞调用；`ruff --select F,E9` 全绿。
- 修掉两个测试暴露出的真 bug：① 终态先翻 status 后补 `finished`，客户端能读到"终态但信息不全"
  → 改成信息齐了最后翻 status；② 看门狗轮询固定 5s 会盖过更短的超时值 → 按剩余时间取小。

### 注释规范

全项目再扫一遍：**连续注释块 0 处超过一行**（`.py` / `Dockerfile` / `.sh` 全覆盖，有扫描脚本）。
删掉的都是复述代码的，留下的每一行都在讲"为什么这么写"或"这里有坑"。

### chart（四个都改了，**版本号仍锁 1.0.0**）

- tag 各自 +1：qwen `test6`、pyannote `test5`、fasterwhisper `test5`、nemo `test3`。
- 新增可选 env 透传（**不设就完全不渲染**，已用 `helm template` 双向验证）：
  四个都有 `LOAD_TIMEOUT_S`、`TASK_TTL_S`（nemo 只有前者），qwen 另有
  `VLLM_CAPTURE_SIZES`、`VLLM_ENFORCE_EAGER`。理由：这两处改动我在本地无法真验，留逃生阀。
- 四个 `.tgz` 已重打包。**镜像 CI 出齐之前不要上传/clone**，否则 ImagePullBackOff。

### 出镜像 + 重建实例（当晚收尾，23:1x–23:3x）

提交 `92c930d`（`feat/audio-stt`，904+/264−，14 文件）推 `mine`，四个 CI 一次派发全绿：
nemo 34s（crane append）、pyannote 5m9s、fasterwhisper 5m9s、qwen 9m21s。
镜像：`lovehunter9/audio-qwen:audio-stt-test6`、`audio-pyannote:audio-stt-test5`、
`audio-fasterwhisper:audio-stt-test5`、`audio-nemo:audio-stt-test3`。

RULE-1 一遍：9 个实例 uninstall（clone 的 chart 副本随卸载自动消失，`market delete <clone>`
会报 "not found in source 'upload'"，属正常，不用管）→ 删 4 个基座 chart → 上传 4 个新 `.tgz`
→ 按台账逐字重 clone 9 个。**9 个 app-name 与台账完全一致**（`9319c8`/`e71e2d`/`4ba5ad`/
`ffa81e`/`b8411d`/`084328`/`4eafad`/`09a372`/`1dc21c`）→ 公网 URL 全部不变，gateway 零改动。

### ⚠ 当晚卡住：九个实例全部 `downloadFailed`，卡在**内部镜像站还没同步新 tag**

报错原文（`olares-cli market status <app>`）：

```
download failed after 6 attempts: failed to resolve reference
"mirrors-internal.joinolares.cn/lovehunter9/audio-qwen:audio-stt-test6": not found
```

**定位**：节点不直连 Docker Hub，镜像被改写成内部镜像站去拉。实测该站上
`lovehunter9/audio-qwen:audio-stt-test4/test5` = **200**，四个新 tag（qwen test6、
pyannote test5、fw test5、nemo test3）全 **404**；Hub 侧四个新 tag 都在、都是 public
（用户已在 Hub 页面确认，推送时间 16–23 分钟前）。**所以不是 CI、不是 chart、不是权限，
是这个站按延迟同步 Hub**（3 小时前推的 pyannote `test4` 已在其中），AI 无手段触发同步。

**明天以"开工"为号，起手就三步**

1. 探镜像站（一条命令，200 才继续）：
   `curl -s -o /dev/null -w "%{http_code}\n" https://mirrors-internal.joinolares.cn/v2/lovehunter9/audio-qwen/manifests/audio-stt-test6 -H "Accept: application/vnd.oci.image.index.v1+json"`
2. 九个实例**重试安装即可**（title/env 没变、app-name 没变、URL 没变，**不用重新 clone**）。
3. 起来后确认：同步路径与之前逐字一致、`/v1/models` 里有 `/v1/audio/tasks/{id}`
   （后者出现即证明新 wrapper 真的进了镜像）。

23:47 又整发了一轮 clone（赌镜像站是否已同步），仍失败。**明天若还是 404，按下面四条线查，
别再靠等**：

1. **摸清改写规则**：看一个正在跑的 pod 的 `image` 实际值 vs chart 里写的值
   （`olares-cli cluster container ...`），确认是不是所有镜像都被改写成
   `mirrors-internal.joinolares.cn/...`，还是只有 `docker.io/*` 被改写。
   这是判断"能不能绕"的前提。
2. **换非 docker.io registry**：若只代理 docker.io，则开发期镜像推 GHCR
   （`ghcr.io/lovehunter9/audio-*`，设为 public）、chart 直接指 ghcr，彻底绕开同步延迟。
   这是最想要的长期解，成本只有 CI 里加一个 login/push target。
3. **节点侧预置**（用户有节点权限，AI 没有）：在节点上
   `ctr -n k8s.io images pull docker.io/lovehunter9/audio-qwen:audio-stt-test6` 再
   `ctr -n k8s.io images tag` 成镜像站那个引用；chart 是 `imagePullPolicy: IfNotPresent`，
   本地有就不会去拉。属于应急手段，不是常态。
4. **平台侧是否有强制同步/预热入口**（问 Olares 那边）。AI 手上没有这个开关。

⚠ 有个反例值得先解释清楚：`audiominutesx2demo:1.0.1` 是**今天本地推上 Hub 的**，却装成功了
（pod 已跑 3h）。说明这条路并非不通，很可能就是"推上去到镜像站可见"之间有几小时窗口。
先量出这个窗口的量级，再决定走 2 还是走 3。

**再往后**：llm-gateway + 两个 DEMO 接异步（提交路径已确认无需改网关，轮询用
`model=<id>` 路由，计费在 202 那一次）。

### 遗留（已与用户确认接受）

- 任务不持久：Pod 重启即丢，轮询回 404，调用方按"重新提交"处理。
- 计费/审计：网关侧提交时记一次（202 也是 2xx），轮询与取结果不计费不落 trace。
- 上传仍受前端 nginx `client_max_body_size` 32MB 限制（本轮不动）。
- 网关透传是整体缓冲的，取 388MB WAV 结果会在网关造成同等内存峰值 →
  `format=ogg`（3h22m 388MB→38MB）仍是必需，不是临时补丁。

---

## 二之九、07-28：**四个基座全部改成"依赖层 + 追加 wrapper"**（tag 统一 `stt-async-test2`）

### 为什么动构建方式（起因是昨晚那个坑）

昨晚九个实例全部 `downloadFailed`，根因是内部镜像站还没同步新 tag。追下去发现真正的
放大器是**我们每改一行 wrapper 就重打整镜像**：`pip install` 不可复现，于是每次都产生
几个 GB 的**全新层摘要**，镜像站必须把这几 GB 从 Hub 抄完，节点才能开始装 → 几小时。
而 nemo 一直是 30 秒出镜像、镜像站几乎立刻可用，因为它走 `crane append`：只往
已存在的基础镜像上加一层小文件，全程在 registry 内完成，不拉、不解包、不跑。

老板定的方向：**四个基座统一成 nemo 那种打法**。

### 怎么拆的（每个基座一分为二）

| 文件 | 内容 | 多久重打一次 |
| --- | --- | --- |
| `bases/<base>/deps.Dockerfile` | `FROM` 上游引擎镜像 + `pip install` + **import 断言** + 定 `audio-python` | 只有改依赖时 |
| `bases/<base>/append.env` | 平台、以及额外 `ENV`（如 fw 的 `LOAD_TIMEOUT_S=5400`） | — |

- 依赖镜像发布成 **`<同一仓库>:deps-<deps.Dockerfile 的内容哈希>`**（`scripts/deps-image.sh`）。
  **哈希当 tag = 不可能用到过期依赖**：改了配方，CI 要找的那个 tag 根本还不存在，于是它自己
  造一次；没改就直接复用。不用人记得"要不要重打依赖"。
- 运行时镜像 = 依赖镜像 + 一层 wrapper，`scripts/append-image.sh` 用 crane 追加，
  `AUDIO_BASE`/`PYTHONPATH`/`WRAPPER_PORT`/`WORKDIR`/`CMD`/`LABEL` 由 `crane mutate` 写入。
- CI（`build-image.yml`）新增一步"这个基座的 deps 镜像在不在"：不在才清盘、起 buildx、造依赖；
  在就直接追加。清盘那 1.5 分钟也只在造依赖时付。
- 三个基座的 `Dockerfile` 已删除。**最终镜像与 chart 用法逐字不变**：还是
  `audio-python -m wrapper.app`、`/app/wrapper`、同一套 env，chart 一个字都不用改。

### 效果（本轮 CI 实测）

| 基座 | 造依赖（一次性） | 追加 wrapper | 以后每次改代码 |
| --- | --- | --- | --- |
| fasterwhisper | 2m45s | **6s** | ~30s，镜像站增量约 164KB |
| pyannote | 3m49s | **10s** | 同上 |
| qwen | 6m23s | **12s** | 同上 |
| nemo | 无需（上游自带全部依赖） | 12s | 同上 |

### 九个实例全量重建：一次成功，**且推翻了昨晚的一个判断**

RULE-1 走完（uninstall 9 → 删副本与 4 个基座 chart → 上传 4 个新 `.tgz` → 按台账逐字 clone 9）：
**九个全部 running，无一 `downloadFailed`**。nemo 21s、pyannote 四个各约 30s、qwen 两个共 2m11s、
fw 两个共 1m。app-name 与台账逐字一致 → 九个公网 URL 全不变，gateway 零改动。

⚠ **教训：`mirrors-internal` 的匿名 manifest 探测不能当判据。** 今天 clone 前探，
fw/pyannote/qwen 的 `stt-async-test2` 全是 404，我据此以为又得等；可 `audio-fasterwhisper:audio-stt-test5`
（07-25 起一直正常拉取、跑了一整天的 tag）**探出来也是 404**——同一引用连探十次稳定 404。
所以那个 404 只说明"匿名探测这条路看不到"，**不代表节点拉不到**。正确做法：直接 clone 看结果，
`market status` 的 `downloadFailed` 才是判据。昨晚的失败是当时镜像站真的还没同步，不是长期不通。

镜像站侧的收益仍然成立：nemo 的新 tag 推上去十几分钟就能探到 200（追加层复用早已同步的上游层）。
本轮 fw/pyannote/qwen 的**依赖层是全新摘要**，属最后一次付这个成本；从下一轮起改 wrapper
只有 164KB 增量。

### 自验（公网，实例侧）

- 三个实例 `/v1/models` 自报里任务四端点齐全：`GET /v1/audio/tasks`、`/{id}`、`/{id}/result`、
  `DELETE /{id}`；WS 只出现在 qwen、`/v1/audio/translations` 只出现在 fw，与设计一致
  （纯流式的 nemo `diar_stream` 不报任务端点）。
- qwen pod 实际镜像确认为 `docker.io/lovehunter9/audio-qwen:stt-async-test2`。
- VAD 实跑一次 `async=1`：202 + `queued` → 轮询 `succeeded`，`progress={stage: done, ratio: 1.0}`，
  结果 JSON 与同步返回一致。

### 代价与补偿（说清楚，别以后当坑）

import 断言从"每次构建都跑"变成"改依赖时才跑"。补偿两条：
1. `tests/`（GPU-free，引擎全 stub）覆盖 wrapper 那一半——七个能力模块全导入、同步/异步等价、
   看门狗、阻塞扫描；
2. 追加完**回读镜像配置**（`crane config`）断言 `AUDIO_BASE`/`PYTHONPATH`/`WRAPPER_PORT`/
   `WORKDIR`/`CMD` 都对——因为这些现在是手写进 config 的，不再有 Dockerfile 兜着。
   引擎侧依赖真出问题，只可能发生在改依赖配方那一刻，而那时断言照旧会跑。

### 本轮镜像与 chart

四个基座 tag **统一 `stt-async-test2`**（含 nemo，从此版本号取齐）。四个 chart 的
`ENGINE_IMAGE` 默认值同步改成 `stt-async-test2`，重新打包，**chart 版本号仍锁 1.0.0**。

### 同日下午：llm-init 跟上上游 main（`v1.3.3-test1`）

上游 `beclab/llm-init` 的 main 已经走了 12 个提交（主要是 translate/MTran：`/translate`、
`/detect`、按模型的语言目录、仪表盘上的 Translate Base URL）。我们的分支是 `feat/audio-stt`
（单个提交 = 去特殊化后的通用 `ENGINE_KIND=audio` 反代；PR #46 的 `stt-audio-base` 是它之前
的一版，未含去特殊化）。

rebase 到 `c9b2c19`，**只有一处冲突**：`internal/config/config_test.go`，两边在同一位置各自
新增测试函数（main 的 `TestLoad_SupportsTranslate`/`…TranslateLanguagesSeedsExtension`
与我们的 `TestLoad_SupportsAudioKeys`），两边都保留即可。自动合并的四个文件人工复核过，
两条线没有互相踩：

- `modelspec.go`：main 把 `translate.SupportsKey` 加进 `allSupports`，我们的
  `allAudioSupports`（14 个裸能力键）并列，`knownSupports` 取两者并集。
- `config_enums.go`：`EngineAudio` / `ModelAudio` 与 translate 无交集。
- `endpoints.go` / `dashboard.js`：我们的引擎自报目录与 main 的 translate UI 各自成块。

构建、`go vet`、`go test ./...` 全绿（20 个包）。镜像 `lovehunter9/llm-init:v1.3.3-test1`。

⚠ 本机 Docker Desktop 的坑：`docker buildx build …` 直接报
`Internal Server Error for API route … /_ping`，必须写成
`docker --context=default buildx build …` 才能构建（buildx v0.10.5 与 desktop-linux
context 不对付）。

四个 chart 的 llm-init 镜像换成 `v1.3.3-test1`，RULE-1 再走一遍：九个实例全部 running、
app-name 逐字一致、URL 不变。自验：`/v1/models` 任务端点仍在，VAD 异步 202 →
`succeeded`（`stage=done, ratio=1.0`）、同步路径返回一致。

### 同日下午：llm-gateway 两个分支也 rebase 到最新 main

main 走了 7 个提交：Market-managed shared app identity、`POST /providers/:id/sync-models`
（上游模型动态发现）、Olares syncer 去掉 shared entrance 归一化函数。

两个分支的关系（别再搞混）：

- `stt-audio-base` = **要交 PR 的那个**（PR #12），两个提交：audio data plane + lint 清理。
- `feat/audio-base` = 前者 **+ 两个不进 PR 的提交**：`translate_temp.go`（demo 用的
  `/v1/translate` 等四个临时代理）和 `deploy/manual/audio_mode_rebase_21_to_23.sql`
  （测试库一次性回绕脚本）。

结果比预期顺：

1. **同步 main**：本地 main 推到 `44639f8`。
2. **migration 序号不用动**：main 最高仍是 `000022`（新增的 7 个提交没带 migration），
   我们的 `000023_audio_mode` 依旧不撞；全仓库也没有硬编码"最新版本号"的地方
   （`000022` 只出现在注释里）。那个回绕 SQL 的说明（"main 现在拥有 21+22，音频改成 23"）
   **仍然准确，无需改**。
3. **main 里还没有 `/translate`**：精确查过（`"/translate"`、`/v1/translate`、
   `supports_translate`、`ModeTranslate` 全无命中）。llm-init 侧已有 MTran 那一套，
   但网关侧的正式设计还没落地 → **我们的 TEMP 代理还得留着**。
4. **两个分支都零冲突** rebase 成功（`stt-audio-base` → `a245194`，
   `feat/audio-base` 用 `git rebase --onto` 接到新 PR 分支之上 → `59772ab`）。
   构建 + `go vet` + `go test ./...`（37 个包）在两个分支上都全绿；
   lint 配置双方都没动过。
5. 语义复核：我们在 `olares_sync.go` 里挂的 `maybeSeedModel` 仍在 upsert 成功那条
   分支上，main 改的同步逻辑没把它绕开。

⚠ **发现一个新 main 带来的交互，待定**：`sync-models` 端点把上游 `/models` 里
DB 还没有的模型一律按 **`mode=chat`** 插入（`providers.ModeChat` 硬编码），
两边都有的行保持不动。所以对音频 provider：syncer 已经播种过的行不会被它改坏，
但**如果先点了 Sync**，音频模型会以 `mode=chat` 落库，被能力闸门拒掉。
我们的引擎其实在 `/v1/models` 里自报了 `mode`，所以正解是让该端点读这个字段——
要不要现在做（会扩大 PR #12 的范围），等老板定。

**两个分支都还没 push**（rebase 后要 force-push 才能更新 PR #12），等老板发话。

---

## 三、锁定的关键决策（勿再动摇）

- **4 个基座 = 4 个运行时家族**（qwen / fasterwhisper / pyannote / nemo），一家族一镜像；NeMo 必须隔离。
- **Whisper 归 fasterwhisper 基座**；qwen 基座纯 qwen-asr（其底层引擎即 vLLM，走进程内 `Qwen3ASRModel.LLM`，**非** `vllm serve`）。
- **sentinel 握手 = 铁律**，照抄 llamacpp，不动。
- **契约只两个必须**：`/v1/models`（探活+自报）、`/metrics`（gpu_*）。引擎 `/health` 不接任何探针（llama-server 有但没人调）。k8s 探针只在 llm-init 打 `/livez`。
- **功能一个不许少**：批量 segments、single、response_format、流式 roll、锁全保留；离线走真 `asr.transcribe()`，**严禁用 stream 假代 non-stream**。
- **代码只写 CI 版**，本地「偷偷打」复用同一套 `deps.Dockerfile` + `append.env` 推 `lovehunter9`（因 main 空、CI 只从 main/tags 发布）。
- **构建方式统一为"依赖层 + 追加 wrapper"**：依赖镜像 tag = 配方内容哈希，运行时镜像 = 依赖 + 一层 wrapper（crane，registry 内完成）。改 wrapper 不得重打依赖。
- **dev 版本号序列：`audio-stt-test1`, test2, …**
- **图片版本锁**：chart 版本号非用户指示不得改（现 1.0.0）。
- **异步是 opt-in**：`async=1`（表单字段，非 HTTP 头，因为网关不透传自定义头）；
  不传就与老行为逐字节一致。批量（`segments`/引擎内批处理）与异步是两个维度，都保留。
- **同步与异步共用一个 worker**：一实例 = 一模型 = 一块时分 GPU，不再有每能力的推理锁。

---

## 四、相关地址
- 引擎工程（Private）：https://github.com/beclab/audio-engines （分支 `feat/audio-stt`）
- llm-init 去特殊化 PR：https://github.com/beclab/llm-init/pull/46
- llm-gateway audio PR：https://github.com/beclab/llm-gateway/pull/12
- 示例模型/CLONE 清单：`音频能力_示例模型与CLONE清单_2026-07-24.md`

# 音频能力 · 示例模型 / 基座 / CLONE 清单 — 2026-07-24

## 相关地址

**代码 PR**
- llm-init（`ENGINE_KIND=audio` 反代基座）：https://github.com/beclab/llm-init/pull/46
- llm-gateway（audio data plane：mode=audio + MODEL_SUPPORTS）：https://github.com/beclab/llm-gateway/pull/12

**引擎工程**（wrapper 与四个基座镜像，CI 出镜像）
- https://github.com/beclab/audio-engines （分支 `feat/audio-stt`）

**4 个基座 chart**（本仓库 `app-zips` 下，x2v3 世代）
- `audioqwenbasex2v3` / `audiofasterwhisperbasex2v3` / `audiopyannotebasex2v3` / `audionemobasex2v3`

> 目的：每个音频能力，我们**示例了哪些模型**、跑在**哪个基座 chart**、用**什么 ENV** 去 clone。
> 数据源：`音频能力_基座实例台账.md`（RULE-1 re-clone 的复现清单）。
> 口径（RULE-2）：clone 表单**只有 5 项**——`title` + `MODEL_SOURCE` + `MODEL_NAME` +
> `MODEL_SUPPORTS` + `AUDIO_REQUIRED_GPU_MEMORY`，其余一律不 `--env`。
> gated 仓库（pyannote diar/embed、nvidia sortformer）靠用户 Olares HuggingFace 集成的 token
> **自动注入 HF_TOKEN，不 `--env`**。
> 4 个基座 chart 均指向 llm-init 镜像 `docker.io/lovehunter9/llm-init:v1.3.2-audiobase7`；
> 引擎镜像见 `音频能力_基座实例台账.md`（wrapper 已烤进镜像，运行期不再 pip）。

---

## 一、能力 → 示例模型 / 基座 / CLONE ENV

### 1. `stt` — 离线转写（整段/分段 OpenAI `/v1/audio/transcriptions`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| Qwen3-ASR-1.7B | `audioqwenbasex2v3` | `hf://Qwen/Qwen3-ASR-1.7B` | `Qwen/Qwen3-ASR-1.7B` | `stt,stt_stream` | `12Gi` |
| Whisper Large V3 | `audiofasterwhisperbasex2v3` | `hf://openai/whisper-large-v3` | `openai/whisper-large-v3` | `stt` | `8Gi` |
| Faster-Whisper Large V3 | `audiofasterwhisperbasex2v3` | `hf://Systran/faster-whisper-large-v3` | `Systran/faster-whisper-large-v3` | `stt` | `6Gi` |

> faster-whisper 在时分 vGPU 上比 vLLM-Whisper 快得多（CUDA 调用少，不被 GPU 锁节流），是 stt 首选。
> `openai/whisper-large-v3` 是 transformers 格式，CTranslate2 读不了；基座会在**首次加载时**
> 自动转成 CT2 落到共享 HF 缓存（实测约 33 秒），之后重启和兄弟实例直接复用，用户无需换模型 id。

### 2. `stt_stream` — 流式 ASR（WebSocket `/v1/audio/stream`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| Qwen3-ASR-1.7B | `audioqwenbasex2v3` | `hf://Qwen/Qwen3-ASR-1.7B` | `Qwen/Qwen3-ASR-1.7B` | `stt,stt_stream` | `12Gi` |

> 与上面的 stt 是**同一个实例、同一次模型加载**（`MODEL_SUPPORTS=stt,stt_stream`），同时暴露离线+流式。

### 3. `align` — 强制对齐（词/字级时间戳 `/v1/audio/align`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| Qwen3-ForcedAligner-0.6B | `audioqwenbasex2v3` | `hf://Qwen/Qwen3-ForcedAligner-0.6B` | `Qwen/Qwen3-ForcedAligner-0.6B` | `align` | `4Gi` |

### 4. `vad` — 语音活动检测（`/v1/audio/vad`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| Silero VAD v5 | `audiopyannotebasex2v3` | `hf://onnx-community/silero-vad` | `silero-v5` | `vad` | `0`（CPU） |

### 5. `diar` — 说话人分离（`/v1/audio/diarization`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| pyannote community-1 | `audiopyannotebasex2v3` | `hf://pyannote/speaker-diarization-community-1` | `pyannote-community-1` | `diar` | `4Gi` |

> gated 仓库 → 需用户 HF 集成 token（自动注入 HF_TOKEN）。

### 6. `speaker_embed` — 声纹 / x-vector（`/v1/audio/embeddings`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| pyannote embedding | `audiopyannotebasex2v3` | `hf://pyannote/embedding` | `pyannote-embedding` | `speaker_embed` | `0`（CPU） |

> gated 仓库 → 需 HF 集成 token。

### 7. `enhance` — 语音增强 / 降噪（`/v1/audio/enhance`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| SpeechBrain MTL-MIMIC Voicebank | `audiopyannotebasex2v3` | `hf://speechbrain/mtl-mimic-voicebank` | `mtl-mimic-voicebank` | `enhance` | `2Gi` |

### 8. `diar_stream` — 流式说话人分离（WebSocket `/v1/audio/diarize/stream`）
| 示例模型 | 基座 chart | MODEL_SOURCE | MODEL_NAME | MODEL_SUPPORTS | GPU |
|---|---|---|---|---|---|
| NVIDIA Sortformer 4spk v2.1 | `audionemobasex2v3` | `hf://nvidia/diar_streaming_sortformer_4spk-v2.1` | `diar-streaming-sortformer` | `diar_stream` | `6Gi` |

> gated 仓库 → 需 HF 集成 token。

---

## 二、基座 chart ↔ 能力覆盖一览

| 基座 chart | 引擎 | 覆盖能力（示例中已用） |
|---|---|---|
| `audioqwenbasex2v3` | qwen-asr（进程内 vLLM） | `stt`、`stt_stream`、`align` |
| `audiofasterwhisperbasex2v3` | faster-whisper (CTranslate2) | `stt` |
| `audiopyannotebasex2v3` | pyannote / torch / speechbrain | `vad`、`diar`、`speaker_embed`、`enhance` |
| `audionemobasex2v3` | NVIDIA NeMo | `diar_stream` |

> 一个实例 = 一个模型；能力由 `MODEL_SUPPORTS` 决定。qwen 基座上 `stt,stt_stream` 是同一次加载，
> 但 `align` 用的是另一个模型，**与 stt 家族互斥**，必须单独 clone 一个实例。
> fw 基座的 `stt` 实例同时提供 `/v1/audio/translations`（语音直出英文）。

---

## 三、通用 CLONE 配方（RULE-2 五项）

```bash
olares-cli market clone <chart> -s upload --title "<TITLE>" \
  --env MODEL_SOURCE=<hf://...> \
  --env MODEL_NAME=<...> \
  --env MODEL_SUPPORTS=<能力,逗号分隔> \
  --env AUDIO_REQUIRED_GPU_MEMORY=<如 12Gi / 4Gi / 0> \
  --watch
```

- `title` 精确复现则 app hash / 公网 URL 不变（无需重指 provider）。
- 除这 5 项外**不要**再传别的 `--env`（gated 仓库的 HF_TOKEN 由 Olares 集成自动注入）。
- 完整 re-clone 周期（uninstall→delete→helm package→upload→逐个 clone）见 `音频能力_基座实例台账.md`。

---

## 四、附注：`translate`（不在音频基座内）

文本翻译 `translate`（`/translate`、`/translate/batch`）是**另一条线**：模型 `llamacpphymt218bggufv3`
（llamacpp + gguf），走 **llm-init 的另一分支**、`mode=chat` + `supports=translate`，**不是** audiobase 基座，
也不占用上面 4 个 chart。此处仅备注，避免与音频能力混淆。

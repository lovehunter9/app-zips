# 音频能力 · TTS 调研与产出（本波搁置）— 2026-07-24

> 决定：**TTS 系（tts / tts_clone）这一波先不做**（今天开会定的）。
> 本文件记录今天已完成的调研与代码产出，方便日后直接接着干。
> 今天对 **llm-init 的改动已按要求撤回**（见文末，附可一键复原的两行）。

---

## 1. 结论速记

- **CosyVoice 优先级最高**：一套引擎同时覆盖 `tts` + `tts_clone`，还能再扩 instruct/流式/跨语种。
- **有现成可直接用的引擎镜像**（OpenAI 兼容），无需自建镜像 → 应走**引擎直连**，比"自建镜像+wrapper+llm-init 下模型"省很多。
- 已写好**两个基座 chart 的第一版**（Kokoro=tts、CosyVoice=tts_clone），本地校验通过；**保留在仓库**（未删），随时可继续。

---

## 2. CosyVoice 能力面（调研）

纯**语音生成**模型，与我们已实现的"听懂"类能力（stt / stt_stream / align / vad / diar / diar_stream / enhance / speaker_embed / translate）**零重叠**。原生覆盖：

- `tts`（普通合成）、`tts_clone`（3 秒参考音零样本克隆）——目标两项
- 跨语种克隆（cross-lingual，同音色跨语言）
- instruct/可控合成（自然语言控情感/方言/语速/角色 + 细粒度 `[laughter]`/`[breath]`/重音）→ 可扩 `tts_instruct`
- 流式合成（bi-streaming，~150ms 首包）→ 可扩 `tts_stream`
- 语言面：9 语言 + 18+ 中文方言
- **不做**真·多人对话一次成型（`tts_dialogue`）——那留给 Dia / VibeVoice / MOSS 类

## 3. 候选引擎镜像（调研）

**首选：`ghcr.io/hsiang-han/cosyvoice3-api:latest`（纯净 OpenAI 引擎，最贴我们架构）**
- `POST /v1/audio/speech`（JSON）= **tts**
- `POST /v1/audio/speech/clone`（form：`input`+`prompt_text`+`prompt_wav`，单次上传参考音）= **tts_clone**
- 另有 `/v1/voices/register`（注册后按名复用）、`stream:true`→PCM、`/v1/models`、`/health`、`/docs`
- **明确支持 RTX 50 系（Blackwell）**，正合我们时分 GPU；**FP16 仅 4GB+ 显存**
- 模型走 HF：`MODEL_DIR`=HF 模型 id、`HF_ENDPOINT` 可配（对得上我们的 HF 镜像 env）；缓存路径 `/root/.cache/models`
- 模型 = **Fun-CosyVoice3-0.5B**（比 CosyVoice2 更新更好）；Apache-2.0

次选：`neosun/cosyvoice`（功能多：音色管理/流式/WebUI/内置 Fun-ASR），但克隆是"先注册再按 voice_id 用"、无单次一次性克隆端点，且更重（端口 8188）。做基座不如首选干净。

其它可留意：`diogod2r/omnivoice-fastapi`（模型是 OmniVoice，非 CosyVoice；但恰好自带 `/v1/audio/speech` + `/v1/audio/clone` + `/v1/audio/design`，600+ 语言）。

## 4. 已产出的代码（**保留在仓库，未删**）

两个基座 chart（复制 `audiofasterwhisperbasexv3` 单能力骨架改造，`helm template` + wrapper `py_compile` 均通过）：

- `audiokokorobasexv3/`（**tts**，Kokoro-82M）：复用 pyannote/torch 镜像，`kokoro.py` 首启 pip 装 `kokoro`，从共享 HF 缓存加载，提供 `POST /v1/audio/speech`。
  - 开放项：espeak-ng（misaki g2p 多语种回退，镜像可能没带、运行时无 root 装）。
- `audiocosyvoicebasexv3/`（**tts + tts_clone**，CosyVoice2-0.5B）：目前是"自建镜像+wrapper（`cosyvoice.py`：`/v1/audio/speech` + `/v1/audio/clone`）+ llm-init 下模型"的**第一版**。
  - **计划变更**（若继续）：改成**引擎直连 `hsiang-han/cosyvoice3-api`（Fun-CosyVoice3-0.5B）**：
    1. `engine.yaml` 换该镜像、去 wrapper 与 sentinel 等待、挂持久卷到 `/root/.cache/models`、注入 `HF_ENDPOINT`；
    2. llm-init `audioPathSupport` 的 tts_clone 路径用引擎真实的 `/v1/audio/speech/clone`；
    3. 镜像镜像到 `beclab/` 再用。

## 5. llm-init 今天的改动（**已撤回**，下面两行是复原用）

今天在 `feat/audio-base` 分支对 llm-init 做过两处（均未提交，现已 `git checkout` 撤回）。日后要恢复 TTS 路由时，把这两行加回即可：

- `internal/adapter/proxy/adapter.go` → `audioPathSupport` map 内新增：
  ```go
  "/v1/audio/clone":          "tts_clone",
  ```
  （注：`"/v1/audio/speech": "tts"` 在 rebase 后**已存在**，无需再加。若走引擎直连镜像，clone 路径应改为 `"/v1/audio/speech/clone": "tts_clone"`。）

- `internal/controlplane/endpoints.go` → 音频 catalog 数组内新增：
  ```go
  {"tts_clone", mPOST, "/v1/audio/clone", "Zero-shot voice-clone TTS (text + reference audio)."},
  ```
  （注：`{"tts", mPOST, "/v1/audio/speech", ...}` 已存在。）

- `knownAudioSupports`（`internal/config/config_enums.go`）**早已含** `tts / tts_clone / tts_dialogue`，无需改。

# WORK LOG — Audio Studio X Demo + audiolabxv3(2026-06-29)

日期:2026-06-29(周一)
状态:**模型侧两处改完并两次全量重建上线;Demo 改完已构建,镜像出 1.0.2 推送测试机**。
明日:在测试机上按 `audiostudioxdemo-docs/测试用例集.md` 跑;大问题再回本地 8080。

---

## 0. 今天做了什么(摘要)

1. **模型侧 `audiolabxv3/templates/wrappers.yaml`**
   - `enhance.py`:加**服务端内部分块**(滑窗逐块 + overlap-add 交叉淡入),长音频峰值显存只受单窗约束,1h 不再 OOM;对外仍「整段一次」。短片走原路径无回归。窗长/重叠 env:`ENHANCE_CHUNK_S`(默认 120s)、`ENHANCE_OVERLAP_S`(默认 1s)。
   - `translate.py`:修 NLLB「整段只翻第一句→译文截断」。改为**非AI按句切分(`。？！；`/`?!`,无标点超长串按 220 字硬切)→ ctranslate2 批量翻译 → 拼回**(CJK 目标用 "" 拼,其余用空格)。接口不变(`text` 进 / `translation` 出),逐句正是 NLLB 官方用法。
2. **Demo `audiostudioxdemo-app/web/src/App.tsx`**
   - 删死代码 `diarizeChunked` 及其专属辅助(`embedClip`/`DIAR_*`/`STITCH_SIM`/`embedModel`),清理无用 import `concatToWav`。
   - Enhance 阶段去客户端分块,**始终整段一次** `POST /v1/audio/enhance`(引擎已自分块)。
   - `npm run build` 通过,lint 无报错。
3. **两次全量重建(老规矩)**:见 §1。
4. **测试资产**:写了 `测试用例集.md`(阶段 0~7),并据测试反馈修正了 Translate 依赖/呈现预期;生成测试素材 A/B/C。
5. **镜像**:Demo 出 `lovehunter9/audiostudioxdemo:1.0.2`(linux/amd64)推送;chart `audiostudioxdemo` 同步到 1.0.2。

---

## 1. audiolabxv3 重建记录(关键结论:URL 可保持不变)

> 铁律:模型/wrapper 任何改动都要全量重建(同版本 chart 必须先 `market delete` 再 `upload`;删 chart 前先卸载所有 clone)。流程:卸载 7 → 删 chart 1.0.0 → 重传新 tgz → 重新 clone 7(title 照抄)。

**今日两次重建**(第一次:enhance.py + Enhance 上 GPU;第二次:translate.py 按句切分)。

**重大结论(纠正旧认知)**:**title + env 完全一致 ⇒ clone 名/hash 完全复现 ⇒ public URL 不变**。
- 以前以为「同 title 也变 URL」,实为当时连带改了 env/chart 内容所致。
- 第一次重建:6 个能力 env 没变 → 名字复现、URL 不变;**只有 Enhance 因把 `AUDIO_REQUIRED_GPU_MEMORY` 从 `0`→`2Gi`(env 变了)**,hash `1c4d10`→`d616f5`。
- 第二次重建:7 个 title+env 全没变(Enhance 仍 2Gi)→ **7 个全部原样复现,网关零改动**。

**当前实例台账(均 running)**:

| 能力 | title(照抄) | clone 名 | GPU |
|---|---|---|---|
| STT(faster-whisper) | `Audio Lab X V3 STT` | `audiolabxv3b2b539` | 6Gi |
| STT(Qwen3-ASR/vLLM) | `Audio Lab X V3 Qwen3-ASR` | `audiolabxv3a0bbb6` | 12Gi |
| Diar | `Audio Lab X V3 Diar` | `audiolabxv34c7e4e` | 4Gi |
| VAD | `Audio Lab X VAD` | `audiolabxv306a333` | 0(CPU) |
| Translate | `Audio Lab X V3 Translate` | `audiolabxv38ab6b2` | 0(CPU) |
| Embed | `Audio Lab X V3 Embed` | `audiolabxv35db0f3` | 0(CPU) |
| **Enhance** | `AudioLabX Enhance` | `audiolabxv3d616f5`(原 `1c4d10`) | **2Gi(新上 GPU)** |

- Enhance 已实测 `speechbrain/mtl-mimic-voicebank loaded as 'waveform' on cuda:0`。
- Translate 已实测 `nllb ct2 loaded ... on cpu`,无 traceback。
- **GPU 配额**:整卡 ~24Gi;现 Qwen12+STT6+Diar4+Enhance2 = **24Gi 顶满**,再加 GPU 实例需先腾。
- chart auto:`MODEL_ENGINE` 按模型名自动选(不传)、`GPU_CORE_UTILIZATION_POLICY=disable` 对 GPU 引擎自动写死(不传)。clone 只填 4 个 env:`MODEL_SOURCE/MODEL_NAME/MODEL_MODE/AUDIO_REQUIRED_GPU_MEMORY`。
- 网关:**只有 Enhance 的 public URL 变了**(`d616f5`),需用户取新 URL 后在网关重注册 enhance provider;其余 6 个 base_url 不动。(用户表示自行处理 Enhance 网关。)

> 落盘同步:`audiolabxv3-CLONE.md`、skill `.cursor/skills/audiolabxv3-instances/SKILL.md` 台账与配方均已更新(并修正配方表:不传 MODEL_ENGINE、Enhance GPU 2Gi)。

---

## 2. Demo 改动细节

- `web/src/App.tsx`
  - Stage 0 Enhance:删 `ENH_WHOLE_MAX/ENH_CHUNK/ENH_CONC` 分块分支,统一整段 `audioMultipart("enhance", original, …)`。
  - 删 `diarizeChunked` 整函数 + `embedClip` + `DIAR_CHUNK_LEN/OVERLAP/CONCURRENCY/WHOLE_MAX` + `STITCH_SIM` + `embedModel`;`embedSlice`/`cosine` 仍在用,保留。
  - import 去掉 `concatToWav`。
- 其余融合/分段开关逻辑沿用上周成果(VAD/DIAR×STT 始终融合、Qwen 非AI对齐开关仅在无 VAD/DIAR 时可用、分段发送/分段翻译开关 + 并发滑杆)。

---

## 3. 测试资产

- **用例集**:`audiostudioxdemo-docs/测试用例集.md`(阶段 0 连接→1 单能力→2 Qwen 时间戳开关→3 VAD/DIAR×STT 融合→4 分段发送/翻译开关→5 长音频→6 VAD 中文歌→7 UI 稳定性 + 反馈模板)。
  - 修正:Translate 不能单跑(依赖 STT,加 1.4b 负向);译文是否上时间轴只取决于「分段翻译」开关(关=整段一块、开不开 VAD/DIAR 都一样);整段翻译截断已随 translate.py 修复。
- **测试素材**(`~/Downloads/audioxdemo/`,16k 单声道 wav):
  - `A_中文双人对话.wav`(95s,Tingting+Meijia 两嗓音模拟双人,含数字/地名)
  - `B_jfk_english.wav`(11s,英文 sanity)
  - `C_带噪语音.wav`(95s,A 叠粉噪,Enhance 前后对比)
  - 注:A 是 TTS 合成,音色差比真人小,Diar 偶尔并说话人属正常;需要可再造音色差更大的。

---

## 4. 镜像 / 部署

- Demo 镜像:`docker.io/lovehunter9/audiostudioxdemo:1.0.2`(linux/amd64,build + push)。
- chart `audiostudioxdemo` → `Chart.yaml`/`values.yaml` 同步 1.0.2(image 指向 1.0.2)。
- audiolabxv3 chart 包:`audiolabxv3-1.0.0.tgz`(已含 enhance 分块 + translate 按句切分;旧包 `.bak-*` 备份)。
- 本地调试:`audiostudioxdemo-app/` 下 `node server.js`(8080),已构建 `web/dist`。

---

## 5. 明日 / 待办

- [ ] 测试机用 1.0.2 镜像,按用例集跑全功能;重点回归:3.1 VAD×STT 融合、5.1 长音频 Enhance(整段、上 GPU 提速)、1.4/3.5 整段翻译不截断、2.x Qwen 时间戳开关。
- [ ] Enhance 网关 provider 用新 URL(`d616f5`)重注册(用户处理)。
- [ ] (历史待办)DIAR 提速 `diar.py` batch_size;VAD 中文歌曲 silero 参数调优(`vad.py`);OlaresManifest clone 字段审计。
- [ ] 大问题回本地 8080 复现再修。

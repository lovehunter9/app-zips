# audiolabxv3 / llm-gateway 工作日志 — 2026-07-02

> 承接 07-01：STT 流式经 LLM Gateway 的最后一公里。**端到端打通并提交**。

---

## 一、前端镜像 v2.0.6-test1（含 nginx WS 头修复）

- `docker buildx build --builder multiarch --platform linux/amd64 -f deploy/frontend.Dockerfile
  -t lovehunter9/llm-gateway-frontend:v2.0.6-test1 --push .` → 成功，manifest `sha256:2cc606c0…`。
- 内容即 07-01 改好的 `deploy/frontend.nginx.conf.template`：`location /v1/` 补 `proxy_set_header Upgrade $http_upgrade;`
  + `Connection "upgrade";`（照 `/console/api/` 写法）。配置烤进镜像，故必须重建。
- chart `llmgatewayv3/templates/llmgateway.yaml` 前端已指向 test1、后端仍 test7。**用户手动滚上去。**

## 二、端到端 WS 测试 — 通过

- 环境（`/tmp` 被重启清空，已按 recipe 重建）：`/tmp/wsvenv`（websockets16/numpy/soundfile，
  国内慢网用清华镜像装）、`/tmp/ws_gw_client.py`（**改为 env 传参** `GW_URL/GW_KEY/GW_COOKIE`，
  不再硬编码过期凭据）、样本 `/tmp/asr_en.wav`(11s JFK)、`/tmp/asr_zh.wav`(15s，非语音测试音)。
- 命令：`GW_URL=https://cdc01162… GW_KEY=sk-… GW_COOKIE='auth_token=…' /tmp/wsvenv/bin/python /tmp/ws_gw_client.py /tmp/asr_en.wav`
- 结果：`[ready]` → 多条渐进 `[partial]` → `[FINAL] (English) "…ask not what your country can do
  for you; ask what you can do for your country."` **完全正确。**
- 首次尝试回 **502**：此时 stt_stream 实例被暂停（用户手动恢复，等 vLLM 加载）。这正是 handler
  「先 dial 上游、上游死/冷启即回干净 502」设计的体现——不是 bug。实例恢复后即通过。
- 中文样本返回空文本：`sample15s.wav` 是 audiocheck 测试音（非中文语音），非链路问题（partial/final
  正常流转）。多语种能力 M2 直连阶段已验。

## 三、提交

- **llm-gateway** `aaa594e` — `feat(backend): add stt_stream WebSocket data plane passthrough`
  （dispatcher.go/spend/model.go/spend_record.go/router.go/audio_stream.go + nginx 模板 + go.mod/go.sum，
  8 files，329+/1-）。分支 `feat/market-real-llm-gateway-supported`。
- **app-zips** 本次提交：`llmgatewayv3/templates/llmgateway.yaml`（前端 test1）、
  `audiolabxv3-docs/register-stt-stream-provider.js`、WORK_LOG_2026-07-01/02、SKILL 台账补记。

## 四、状态与下一步

- 网关 STT 流式：**闭环完成**（后端 test7 + 前端 test1 + provider `stt-stream-audiolabxv3` 已在 DB）。
- 下一步（07-01 遗留 pending）：
  1. `demo` — 流式滚动字幕视图（audiolabxv3 demo 侧）。
  2. `rebuild8` — 重建原 8 个离线 clone（当前 UNINSTALLED）。
- 提醒：`/tmp` 测试资产会随宿主重启丢失，按本日志 recipe 重建；cookie/KEY 会过期，需用户新给。

---

## 五、长流式加固（滚动重置 = 实现B）+ 服务端保活修复

- **问题**：Qwen3-ASR 流式 state 持续累积音频上下文 → vLLM 编码器缓存（启动日志确认 budget
  **8192 tokens**）在约 10 分钟连续流后溢出 → 会话中断。会议/演讲/有声书都动辄 >10min，必须解决。
- **修复（`audiolabxv3/templates/wrappers.yaml` → `stream.py`）**：
  1. **主动滚动重置**：每 `STREAM_ROLL_SEC`（默认 240s）`finish_streaming_transcribe` 当前段，
     把其文本折进 committed `prefix`（`_join` 保证跨接缝单调、仅英文词间补空格），再
     `init_streaming_state` 开新 state → 编码器缓存占用归零 → 时长事实上无限。
  2. **兜底**：捕获 `encoder cache/exceeds/pre-allocated` 异常 → 强制 roll → 重试该 chunk 一次。
  3. 推理已用 `asyncio.to_thread` + 全局 `_infer_lock` 卸到线程池（循环不被推理堵死）。
- **顺带修 1011**：`uvicorn.run(..., ws_ping_interval=None, ws_ping_timeout=None)`。猛灌帧压测下
  默认 20s 保活 ping 会把健康会话误杀（`CLOSE 1011 keepalive ping timeout`）；字幕是客户端驱动，
  改用 WebSocketDisconnect/TCP reset。
- **重装**（RULE 1 全流程）：uninstall→delete→重打 tgz→clone；hash/URL 复用不变
  （`audiolabxv30f9f88` / `https://d123f7e6…`，env 未变）。
- **实测（经网关，合成长音频，`/tmp/ws_gw_client.py` 需 `proxy=None` 绕沙箱 SOCKS）**：
  - 300s（跨 1 次 roll）：601 partial → FINAL，零报错，文本跨 roll 单调累积。
  - 720s 旧代码：~560s 处被 1011 切断，但**无 encoder-cache 报错**（证明 roll 生效）。
  - 720s 加固后：**1443 partial → FINAL，零报错**，干净越过旧 ~10min/600s 崩溃点。
  - 用户随后用 1h05m 真实英文有声书线上实测，滚动字幕正常（2:09 已定 59 句）。

## 六、Demo 1.0.6（实时字幕）

- 新增「实时字幕」tab：麦克风实时 + 文件模拟直播；播放驱动送帧（按 `media.currentTime`），
  边放边出**滚动字幕**；视频叠字幕 / 音频下方出字幕。
- **进度条可随意拖动**：`seeked`（250ms 防抖）→ 关旧会话、在拖到点重开新流式会话、清屏从该点
  出字幕；`wsRef` 身份守卫防止旧会话回调污染新会话。取舍：拖动会清掉旧时间线字幕（流式无法缝合）。
- 流式模型**只从网关获取**（下拉框 + `(无 stt_stream 模型 — 先刷新)` 占位），不再预猜厂商名。
- `server.js`：WebSocket 升级代理 + 服务端注入 Bearer/cookie（浏览器 WS 不能设头 → `__base/__key/__cookie`
  query 透传，`server.on("upgrade")` 预解析 stash 到 `req._wsctl`）。
- **镜像**：`docker.io/lovehunter9/audiostudioxdemo:1.0.6`（linux/amd64，build+push 完成，
  imagetools 确认 amd64）；chart `audiostudioxdemo` 同步 1.0.6（Chart.yaml version+appVersion、values.yaml）。

## 七、提交

- **app-zips** `48a7d04` — `audiostudioxdemo 1.0.6 + audiolabxv3: STT 流式(实时字幕)端到端`
  （8 files：wrappers.yaml、audiolabxv3-1.0.0.tgz、server.js、App.tsx、stream.tsx(新)、
  Chart.yaml、values.yaml、SKILL 台账；767+/40-）。

## 八、能力盘点（15 个 MODEL_CAPABILITY，已集成 8）

- 已接入：`stt · stt_stream · vad · diar · translate · embed · enhance · align`。
- 未集成 7：`tts`(M3) · `tts_clone`(M3) · `tts_dialogue`(M4候选) · `diar_stream`(M4候选) ·
  `audio_llm`(P0) · `audio_s2s`(M4候选) · `sound_fx`(P2)。
- 下一步方向讨论中（用户指出：实时会议/字幕不止需要 stt_stream，diar_stream 等流式伴随能力也需要）。
